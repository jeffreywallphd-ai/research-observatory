# W1.A08.T02 resumed acceptance closure

This append-only supplement preserves the original task-start worksheet,
pre-submission findings and captures. Original claim base remains
`46e88d171795b76a2f1588d69756f4184c01a89b`. Supported activation/reopen is
committed at `0e21bdde`; W1.A08 is ACTIVE and W1.A08.T02 is IN_PROGRESS.

## Authority and exact predecessor

The five immutable ECR-0007/W1.A08.T02 criteria still govern. W1.A09 was
independently approved and adopted at `1a73207c`. The actual return check
recomputed the unchanged PAUSED W1.A08 record SHA-256 as
`3856a53b1f7079f4b80469b03f6651c00e8ee84dc9d9f0711e40600bbf03d506`,
with null parent lease and W1 PAUSED/amendment-hold, before explicit activation.
Its approved presentation 1.6 overlay resolves preflight F01; historical 1.5
authority strings and stale observations are not rewritten. Accepted
ADR-0004/0005/0006 and the current approved style guide sections 5, 6 and 9 apply.

## Narrow implementation and proof

- **F02 / criteria 2–4:** shared Panel bodies currently declare 12 px gaps but
  leave browser paragraph margins additive. Add reusable, low-specificity
  paragraph margin ownership at the shared flow boundary, not per-page fixes.
  Measure visible direct-child margins, first/last edges and actual inter-child
  distances in single-column Panel bodies. Preserve explicit non-paragraph
  composition margins and avoid interpreting a multi-column grid as a stack.
  Actual populated Task Center and another consumer must be observed. Negative
  tests keep canonical declared gaps while injecting additive margins; missing,
  malformed, nonfinite and substituted measurements must fail.
- **UX01 / criteria 3–4:** the sidebar's viewport-height cap truncates its actual
  surface before long content/footer. Remove the cap rather than add spacers,
  duplicate backgrounds or a new layout system. Ordinary document scrolling
  is acceptable if navigation remains reachable. Measure sidebar, shell-body,
  main and footer rectangles on short/long pages, both themes, expanded
  navigation and scroll positions. Wide cases require actual sidebar/footer
  equality; 720x450 requires stacked navigation before content, without overlap
  or horizontal document escape. Add real Tab/Shift+Tab traversal of expanded
  navigation; programmatic focus is not keyboard proof.
- **Criteria 1, 4 and 5:** wire these observations into the existing built React
  product qualification matrix and validator. Reuse exact manifest/source,
  renderer/font, state, capture persistence and Git authentication. Regenerate
  the full 48 workspace plus six designated-state product/reference comparisons
  against effective presentation 1.6, retaining the original capture bundle.
  Independent review must inspect representative captures and replay findings.

The read-only preflight by `agent:/root/t03_acceptance_review` confirmed these
two holes and recommended the above minimum geometry and keyboard cases. It
made no implementation changes, executed no replay, and did not approve closure.

## Test-first sequence and risk boundaries

Implement measurement/negative-test support, then run focused actual-browser
tests against unchanged product CSS to preserve a failing reproduction before
the correction. Apply the smallest shared CSS changes, rebuild and rerun those
tests. Run affected Python checker/capture tests, shared-component tests,
renderer lint/types/tests/build, style analysis, complete affected product frame,
reference/workflow/accessibility/visual checks and unchanged performance budgets.
The existing clean qualification checkout is at `024bd59f` with no changes;
update only through an explicit non-destructive checkout of the tested source
when it is time for performance/capture proof, preserving all old artifacts.

No domain, migration, native process, folder, credential, storage, token,
reference-publication or scholarly-authority code changes are intended. Real
principal proof is the actual built React product in pinned Chromium with
explicit local adapters, plus the existing real file/Git capture boundary.
This is not native authentication or packaged Windows startup proof. No
ordinary account policy, vault, credentials or projects are accessed. The
protected untracked witness remains untouched and no remote mutation occurs.

Separate independent task, W1.A08.S01 and amendment-exit review/adoption remain
required. Complete repository/Windows release qualification remains W1 exit
work. The requested maximized startup and right-side testing policy is a
separate inert proposal for the next lawful UX amendment, not this task's scope.
