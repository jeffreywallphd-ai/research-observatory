# T01 verification selection and retained outcomes

Tested product candidate: `5e91432244b6e3a458b14297e009d7c020790c7e`.
Independent pre-rebind checkpoint: `893de39f3ca8287a6e2bb7e84003d9d787d2f32b`.
This record supports task review; it is not task/slice/amendment or W1 approval.

## Selected passing checks

- Sixteen selected Python tests at the committed candidate passed in 81.803s:
  ten workflow-contract/source/no-Git/witness tests plus six active-reference,
  historical1.5 authority, missing-witness orchestration and token/route tests.
- Changed Python lint and `ui_conformance.py` typing passed. Desktop lint and
  TypeScript passed; Audit & Lineage passed all ten component tests.
- Functional product (four artifacts) and reference fixture (42 artifacts)
  rebuilt successfully. The broader command runner also independently passed
  shared-component verification, desktop lint/typecheck/full renderer tests and
  both builds before reaching the separate native lint failure below.
- Reference checks passed: 109 tokens; 33 routes and 547 required regions;
  fourteen workflows, 139 browser cases and 390 keyboard cases; 33-page
  accessibility with 66 responsive/visual cases; complete baseline history and
  66 actual-build visual observations. The five exact JSON reports are retained
  under `W1.A09.T01.*-conformance-01.json`.
- The existing `desktop_app_check.py --built-frame-only` path passed with no
  errors, no unexpected requests, all fourteen semantic1.5 profiles, and the
  existing functional renderer/security-state/reflow/style-surface contracts.
  Its complete report is retained as `product-frame-conformance-01.json`.
  Its local adapters do not prove real native project creation/dialog behavior.
- Real Core/services/SQLite predecessor reopen passed: accepted revision and
  prior history, exact stage/supporting return and all sixteen immutable settings
  rows are unchanged, with zero aggregate/migration rows. Before/after-02 proofs
  retain the synthetic plaintext limitation; no encrypted/native qualification
  is inferred. Original generated semantic payloads/schema/fixtures and token
  transport are unchanged in the claim-base-to-candidate Git diff.
- `planctl ecr validate ECR-0008 --require-approved` passed after publication.
  Reference generator, complete hashes/inventory and exact publication authority
  pass; all 66 capture images were independently authenticated/re-rendered.

## Retained nonpassing attempts and follow-up

The initial default `desktop_app_check.py` invocation selected its full command
plan, broader than T01's changed presentation boundary. It exited 1 after native
Clippy exited 101. Preserve its complete `product-conformance-01.json` report:

| Unchanged source | Diagnostic | Qualification follow-up |
|---|---|---|
| `supervisor.rs:1372` | `nonminimal_bool` in revisit precondition | Preserve logic; replay with the next affected native contract task and before T04 qualification. |
| `supervisor.rs:1376` | `nonminimal_bool` in completion/evidence equivalence | Same native contract boundary; no warning suppression or policy weakening. |
| `application_lock.rs:1336` | `needless_borrow` in a test helper | Replay with native lock/dialog work and before T04 qualification. |
| `application_lock.rs:1346` | `needless_borrow` in a test helper | Same test-only boundary; keep all assertions. |

Both source files are byte-identical to the T01 claim base. The narrow existing
built-frame-only check was selected after inspecting the broad failure; it does
not turn that failed command into a pass. No native warning is waived, no test
is deleted, and full native/W1 qualification remains unavailable until closure.
Independent task review must assess this scope classification. Native fixes are
not silently added to this presentation-only task.

The first focused `pnpm exec vitest` attempt failed because that invocation could
not find the executable shim. Running the installed `vitest.mjs` with the pinned
Node executable passed all ten selected tests; the ordinary full command runner
also subsequently ran desktop tests successfully. No dependency or toolchain
version was changed to obtain a pass.

Earlier unborn-Git fixture, sandbox ownership and checkpoint-wrapper HEAD-change
failures remain disclosed in the task-start worksheet and independent checkpoint.
Their successful stronger replays do not erase their original observations.

## Scope and deferred coverage

The changed boundary is exact governed presentation publication/consumer binding
and a verification-only semantic compatibility witness. Test selection therefore
covers actual Git/proposal/source tampering, preserved real data, rendering,
build roles, routes, keyboard/reflow and stable semantic identities. No runtime
Git dependency, semantic migration or token-system replacement was added.

T02 retains actual native/Core route, narrative, startup/readiness and retry
repair. T03 retains real protected folder selection, default-location preselection
in dev/production, no-overwrite and cancellation/race proof. T04 and returned A08
retain native/package integration and full-height-sidebar/effective-spacing
qualification. The complete repository/Windows matrix and W1 release gate remain
pending. Passing the existing built renderer contracts does not close A08's
separately demonstrated styling findings or the blind study's native UX defects.

Owner-requested synthetic usability-study/diagnostic artifacts and the T03
preselection acceptance note are included truthfully in the base-to-candidate
evidence inventory. They are separate authorized preparation/evidence, not a
claim that T03 was implemented early or that a blind test qualified W1.
