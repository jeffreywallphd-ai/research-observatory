# W2 implemented-workspace qualification maintenance

Predecessor: `36085017c1c4f92e3e08bd1c2587f1bb9296ee1c`.
Independent consumer review found two active presentation assertions still bound
to 1.6. Reproduction additionally exposed the qualification adapter's obsolete
nine-workspace inventory, while the implemented registry already contains eleven.

This bounded evidence-control repair updates the active assertions to approved
1.7 and extends the exact matrix to Imports and Sources in registry order. It
observes a selected retained draft with source-record rows and loaded provider
configuration with enabled controls. Synthetic response fixtures live within the
existing capture-input inventory and pass through the real generated decoders.
They establish renderer behavior only, not native intake, protected storage,
provider availability or scholarly facts. Missing summary results remain absent.
Fixture UUIDs and record-key digests are explicit synthetic placeholders with
consistent request/response identity; no source-byte hash validation is claimed.

All prior workspaces and designated boundaries retain their semantic geometry,
focus, panel/shell layout, containment, motion, renderer and request controls.
The current inventory is 66 workspace cases plus six designated cases, or 144
optional comparison images. Historical bundles and the 66-entry reference
baseline remain immutable. No product scope, security authority, approved
reference, semantic 1.5 profile or release criterion changes; no supplemental
refactoring. Independent control review is required before local integration.

Selected regressions: current reference assertions; exact matrix and negative
coverage; absent/unloaded representative-state witnesses; complete built-product
runtime qualification; fixture/capture inventory and affected lint/type checks.
Prior failures remain in local development evidence; passing reference checks
do not establish product-frame completion.

Preflight finding F-WORKSPACE-02 exposed hidden-content false positives in the
first added witnesses. Required draft/table content is now scoped to the same
selected nonbusy preview; provider controls share their configuration card.
Each required element must have rendered geometry and visible ancestors.
Hidden, display-none, invisible and transparent-content regressions precede
qualification; existing checks are not relaxed.

The resumed full runtime check also exposed three current Intent response doubles
missing the required egress-policy projection. They now explicitly remain
local-only with no approved destinations; historical fixtures are untouched.
Import tables use the shared DataTable scroll primitive, so semantic measurement
now includes that existing container as well as the hand-authored table region.
