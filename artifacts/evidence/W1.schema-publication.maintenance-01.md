# W1 standalone schema publication maintenance 01

Predecessor: `5d67af864d52244d5d4b929079bf383c50f7077c`. This bounded
automation/public-contract maintenance follows `workflow-efficiency.md` bounded
maintenance; it is not a task claim, amendment, approval, or release decision.
Independent commit-bound control review and fresh candidate verification remain
required before integration. No supplemental refactoring allowance is claimed.

## Observed failure and missed acceptance

The isolated W1 full-union attempt selected 37 commands but stopped after seven
PASS results and `foundation:build-manifest` FAIL. The remaining 29 commands were
not executed. Both standalone model-gateway schemas lacked the stable `$id`
required by the unchanged build-manifest consumer; they also lacked their
approved Draft 2020-12 declaration. Raw Pydantic schema equality tests had
preserved validation rules but missed standalone publication identity.

Retained, unchanged evidence:

- `artifacts/tmp/W1.full-qualification-01.json`, SHA-256
  `568628af507be5b3e67f63d289f816f5111a2daf7d4df17e8c2d6d983c05dc53`.
- `.local/q/w1-final-01/artifacts/tmp/W1.full-qualification-01.execution.json`,
  SHA-256 `f0399340bd0a29ca496b26f0a22cbf9e91f951fcd74dd416fe00e9c9635cd01e`.
- `artifacts/tmp/W1.full-qualification-01.log`, SHA-256
  `06d001a2ea6520c457b498764e470b35f57597d62c725555ddf3427e23a26437`.

## Intended delta and invariant boundaries

Only `tools/core_api_contract.py`, the two generated
`packages/contracts/model-gateway/{model-manifest,routing-policy}.schema.json`
files, `tests/contracts/test_model_registry_contracts.py`,
`tests/contracts/test_model_routing_contracts.py`, and this note change.
The exact predecessor commit binds their original tracked bytes.

The exporter adds root `$schema` and canonical `$id` metadata, using the existing
model-gateway URI convention and Draft 2020-12 authority from ADR-0021,
`packages/contracts/README.md`, and CAP-07.S01 sections 4, 5, 10 and 13.
All existing references in these two documents are local `#/$defs` references.
Every validation keyword, definition, alias, default and data constraint remains
exactly equal to the Core-owned schema. No Core model, wire version, policy,
authorization, runtime, schema inventory, build-manifest rule, threshold,
OpenAPI, TypeScript artifact, or approved experience changes. No ordinary
research/project/vault data is used.

The missed acceptance is now explicit: generation must publish both canonical
identities and the dialect while preserving the entire owned validation schema
and deterministic bytes; published schemas must retain valid-fixture acceptance
and malformed/authorization-flag rejection; the existing manifest consumer must
accept the complete inventory and still reject inventory drift.

## Regression-first development proof

Before exporter/schema edits, the new generator regression, both strengthened
portable-schema tests, and unchanged build-manifest consumer reproduced the
defect: five tests ran, five failure reports (including two generator subtests),
with the primitive-alias test passing. After the minimal edits, all six selected
tests passed in 1.415 seconds; the additional test preserves inventory-drift denial.

- RED: `artifacts/tmp/W1.schema-publication.red-01.log`, SHA-256
  `38a61f708a091b095db15f457cabfd44a0b0a1813eba9662eefde3829544c94e`.
- GREEN: `artifacts/tmp/W1.schema-publication.green-01.log`, SHA-256
  `8e81e1766a3f1825829720b20554fd7b07189db62abb720e24a3bbb1d6d4a79a`.
- Exact generator check: `artifacts/tmp/W1.schema-publication.generator-01.log`,
  SHA-256 `d0de6df4695ae12810b382025f30caee94c9e9bc81887720850df6345026980f`.
- Initial three-file quality selection: format/lint PASS, type discovery FAIL
  with 22 import-not-found errors because the Core package root was omitted;
  `artifacts/tmp/W1.schema-publication.quality-01.log`, SHA-256
  `7b322442145cfa54d7f3fc45c82ae04c291e94fb36b87dc47d85492f69fdcca3`.
- Corrected selection includes the unchanged Core package entry point;
  format/lint/types all PASS without any source, configuration, or rule change:
  `artifacts/tmp/W1.schema-publication.quality-02.log`, SHA-256
  `61312d91b58e9cf4dfe39d628721f826230216918181282f6f3352b5a604c073`.

Portable command forms (local runtime paths are omitted here):

```text
python -B -X utf8 -m unittest -v tests.contracts.test_model_registry_contracts tests.contracts.test_model_routing_contracts.ModelRoutingContractTests.test_portable_policy_matches_core_and_never_accepts_authorization_flags tests.foundation.test_build_manifest.BuildManifestTests.test_clean_build_manifest_is_deterministic_and_complete tests.foundation.test_build_manifest.BuildManifestTests.test_schema_inventory_drift_is_rejected
python -B -X utf8 tools/core_api_contract.py --check
```

The RED command omitted the final inventory-drift test. Scoped quality invokes
unchanged `tools.quality_check.execute_quality` over the exporter, the two changed
test files, and `services/core-api/src/research_observatory_core/__init__.py`;
its report retains the exact format/lint/type commands and outcomes.
Generator `--check` checks all four generated artifacts byte-for-byte, including
the unchanged OpenAPI and TypeScript outputs. `git diff --check` also passed.

These are development results, not committed-candidate qualification or approval.
The build-manifest unit uses its existing deterministic Git fixture; real
committed Git publication remains for fresh candidate verification. Full W1
profiles, benchmarks, native/principal, packaging and signing checks are deferred
to their existing qualification producers, not waived or represented as passed.
No native/build/benchmark/signing execution or Git mutation occurred here.
