# G1 recording maintenance — bounded scope

Predecessor: `592efbd4dcbae9fad1a7c3f3f54460bcd4893a89`.
Status: implementation/verification pending; not a gate decision.

The retired, unexecuted migration reservation is already recognized by
`taskctl`, but supplemental recovery validation still opens its former
bootstrap payload. Restore that retirement boundary: validate the immutable
descriptor, approval, packet, review, ECR and Git bindings; omit only the retired
payload read. Invalid present projections must fail without reading that payload.
Pre-append/live evidence authentication remains unchanged.

Risk: evidence/control integrity (expanded independent review). Changed code is
limited to the existing supplement validator and its focused regression. No
schema, controller, release criterion, approval, product or security change.
This is restoration, not supplemental refactoring or a new execution lease.

Selected proof: guarded terminal S01/S02 validation, malformed retirement
denials, existing synthetic pre-append byte/metadata tamper checks, and one
repository-aware atomic persistence/CAS exercise on an ignored backlog copy.
No canonical mutation, retired witness content access, broad fixture repair,
completed product suite replay, G1 approval or W2 execution is authorized here.
Record outcomes and exact candidate append-only after verification; obtain
independent control review before integration.
