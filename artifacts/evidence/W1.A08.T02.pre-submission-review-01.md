# Independent pre-submission review — W1.A08.T02

Disposition: **BLOCKED**. Reviewer: `agent:t01-r02-independent-reviewer`
(`/root/t01_r02_independent_review`). Delivered 2026-09-05. This retained record
transcribes the actual independent reviewer disposition; it is not a frozen
R01 task-control review or an approval.

Reviewed producer `024bd59f22bc707c7a6442e40e82ac2616b49e17` and capture delivery
`607f1965c6567de64c4d848304addddb54fca529`.

## W1.A08.T02-preflight-F01 — P1, blocking

Inherited visual baseline has stale observations. T02 criterion 5 requires
affected UI-reference and visual checks to pass. ECR-0007 sections 8 and 11
prohibit baseline rewrites or a new reference ID within W1.A08.

Independent Git inspection confirmed that `3130c373` changed only baseline
reference identity/package metadata; every current screenshot entry remains
identical to its 1.4 predecessor. `a6967261` materially changed the audit page.
Independent pinned-browser replay reproduced these hashes:

| Source | Light SHA-256 | Dark SHA-256 |
|---|---|---|
| Current approved source | `016e03260dab4dfb45980811af5ff3b2fac32da2763f8140cdd756244bf51b89` | `87484d023003c7669a177d79903699c2524ff2aa7ea50e6be199fe7fb7a70b4c` |
| Historical `a6967261^` | `58ade2cec2b66631ff180725dd62b59e3240534cf6ce3dd77462952c28e6a755` | `b946ba4d3aceed28530527fd79271ae3ded8f08a82e54b3c0044cf2c30a630cc` |

Historical hashes exactly equal the current expected baseline entries.
`artifacts/tmp/w1-a08-t02-audit-visual-diagnostic.py`, with and without
`--historical`, reproduces the comparison. The reviewer independently performed
the equivalent replay in memory, without writing screenshots.

Required resolution: preserve failure and immutable history; obtain a separately
reviewed, explicitly human-approved reference/amendment decision establishing a
new visual identity and truthful observations. Lawfully activate it and replay
qualification. GOV-MAINT-0010 expressly preserved observations; neither it nor a
generic maintenance correction authorizes weakening the same-ID prohibition.
This is stale-baseline failure, not a T02 rendering regression or acceptable
rendering variance.

## W1.A08.T02-preflight-F02 — P2, blocking

Effective content spacing escapes geometry checks. T02 criteria 3 and 4 cover
rhythm, panels, density and independent visual disposition. ECR-0007 specifically
identifies browser-default child margins as an existing defect.

At `packages/ui-components/src/styles.css:29`, `.ro-panel > div` declares a
12 px grid gap without clearing child margins. Task Center ordinary paragraphs
retain 14 px top/bottom margins: adjacent paragraph separation is 40 px and the
last paragraph-to-actions separation is 26 px. Excessive whitespace is visible
in retained capture
`W1.A08.T02.captures-01/2512de22bebc7cc71986860b693bfe748a81f5df5a29fe08097b370b1e7a2eff.png`.

Reproduce with `inline_product_index(repo)`, the existing
`tests/desktop/fixtures/task_center_interactions.js` adapter and
`core_workflow_catalog_json(repo)`; navigate Local projects, open
`C:/Research/study-one`, then Task Center at 1440x900. Measure the children of
`[data-task-center-workspace] .ro-panel > div`:

```javascript
const children = [...element.children];
children.map((child, i) => ({
  marginStart: getComputedStyle(child).marginBlockStart,
  marginEnd: getComputedStyle(child).marginBlockEnd,
  precedingDistance: i
    ? child.getBoundingClientRect().top -
      children[i - 1].getBoundingClientRect().bottom
    : null
}));
```

Required resolution: one reusable content-flow correction plus live measurements
and negative tests for child margins/effective spacing, not page-specific
patches. Generate new commit-bound captures and retain the original bundle.
This is a geometry-check false pass, not approved composition variance: the
validator checks computed gap but misses additive child margins.

## Independently verified results and permissible variance

The reviewer independently passed capture authentication for all 108 PNGs,
27 style-evidence tests and 14 desktop tests. The inspected performance report
binds the clean producer and retains all 12 browser samples with passing unchanged
budgets.

All eight light-theme workspaces and representative dark/narrow task, audit,
lock and modal captures show coherent palette, typography, borders and focus.
Different mock data, implemented navigation, page composition and recorded scroll
positions are acceptable; pixel identity is not required. These variances do not
waive either open finding.

The reviewer made no implementation, baseline, workflow or ledger changes.
