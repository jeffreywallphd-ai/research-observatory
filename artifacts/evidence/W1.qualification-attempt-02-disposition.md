# W1 qualification attempt 02: stopped, not passed

Candidate: `4a698f61ae83e99aae4329af3db87e97af7dd378`.
The full-profile producer was explicitly stopped after actual native testing
reproduced a disqualifying status/recovery defect. This is an operator
cancellation, not evidence that every check ran or that foundation unit tests
failed an assertion. The unmodified raw report retains its ERROR disposition.

- Full report: `artifacts/tmp/W1.full-qualification-02.json`, SHA256
  `a226a8d5623cf6c0c1356014e249458efd847c8b022830b9936eeeb14b494702`.
- Log: `artifacts/tmp/W1.full-qualification-02.log`, SHA256
  `6c4a70daf9fa0746863b5948fa854049589d9ebe1fd7231df26995abf3b38508`.
- Elapsed: 1629.3300671000034 seconds. Foundation unit execution was interrupted;
  subsequent commands were not executed. Completed command PASS lines remain
  partial results, not accepted full-Wave qualification.
- The exact owned verification root was checked against its live producer
  report, executable and command before stopping it. Existing Job Object
  containment retained input locks, terminated remaining owned descendants and
  confirmed zero active processes (`EMPTY_CONFIRMED`). No broad process stop or
  user application termination was used.

## Native defect and bounded successful observations

Two actual isolated React/Tauri/Core lifecycles reproduced the same issue.
In the controlled second run, Alt+Space alone from healthy No-login mode opened
the Windows system menu; within the next observation the renderer reported
application-lock status unavailable, claimed manual lock and offered Windows
unlock. Escape dismissed the menu but not the fail-closed screen. No unlock,
recovery, authentication or security-setting action was attempted. Both runs
closed normally and their contained process trees drained. Process success does
not make these UI failures pass.

- First lifecycle report SHA256:
  `6cd80cd394e00765cf533eb33a334a37094998e4849bcc7d67612a7301eeee08`.
- Second lifecycle report SHA256:
  `4619a93e37a574d8e1ca272e18553e962727dfc1d240f2cd46e7d2d5f709fc63`.
- Direct operator observations: `artifacts/tmp/W1.native-observations-02.md`.
  This is an observation note, not an automated assertion or independent review.

Before the second failure, actual synthetic project creation, explicit open,
explicit workflow start, supporting Model Center detour/return and project close
worked. The real Windows folder chooser opened at the preselected fixture parent
and cancellation retained the form. Positive changed-folder selection was not
performed. No ordinary profile, project, credential or research data was used.

Linked correction W1.C04.T01 inherits W1.A05.T04. It must restore truthful
mode/recovery presentation and investigate native status delivery without
extending the timeout or relaxing the fail-closed latch. Fresh final-candidate
qualification and independent Wave review remain outstanding. G1 is not ready.
