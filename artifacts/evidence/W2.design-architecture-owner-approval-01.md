# W2 design and architecture — owner approval

Recorded 2026-09-19T16:19:10Z. Actor: `human:repository-owner`.

The owner replied **Approved** to the decision-complete request in
[the gate handoff](W2.initialization-gate-handoff-01.md). This accepts ADR-0027,
ADR-0028 and ADR-0029, and reference `RO-UI-ACADEMIC-MINIMAL-1.7`, exactly as
reviewed at `67bc9e3b6215504e78fa30dbf41cc0a664977679`.
Proposal package SHA-256:
`abdb30a94510ceb6452019133f62189a38c77deda3ecebdf0d0dabdfdeebff6f`.

Authority permits normal ADR acceptance/index updates, reference publication
and affected material-plan bindings. The strict publication mapping changes
approval metadata and consequent generated hashes only; it preserves all other
proposal bytes, including historical proposal-status prose. The active
`APPROVAL.yaml` controls status. The original
reference 1.6 and its approvals remain recoverable in immutable Git history;
retained preview images are historical, not 1.7 validation evidence. The unused
illustration absent from the approved text-only proposal leaves the active
inventory, but remains recoverable from the predecessor commit.

This is **not** W2 execution or release approval. W2 stays PENDING without a
campaign or claim until the final rebound packet is committed, validated,
independently reviewed and separately approved at one immutable commit. No
task/slice acceptance, threshold relaxation, new privacy exception, push, real
credential use or external deployment is authorized. W1/G1 limitations remain.

Implementation qualification and the exact ADR-0026 inherited 1.5 workflow
catalog binding are unchanged. No completed W1 product suite needs replay merely
to record this authority and publish the reference.

The machine-readable [reference approval](../../planning/reference-approvals/RO-UI-ACADEMIC-MINIMAL-1.7.json)
records this same human decision; it is not a second approval or execution gate.
