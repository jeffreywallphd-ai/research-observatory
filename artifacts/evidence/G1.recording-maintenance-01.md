# G1 recording maintenance — bounded scope

Predecessor: `592efbd4dcbae9fad1a7c3f3f54460bcd4893a89`.
Status: implementation/verification pending; not a gate decision.

The retired, unexecuted migration reservation is already recognized by
`taskctl`, but supplemental recovery validation still opens its former
bootstrap payload. Restore that retirement boundary: validate the immutable
descriptor, approval, packet, review, ECR and Git bindings; omit only the retired
payload read. Invalid present projections must fail without reading that payload.
Pre-append/live evidence authentication remains unchanged.

Risk: evidence/control integrity (expanded independent review). Changed code is
limited to the existing supplement validator and its focused regression. No
schema, controller, release criterion, approval, product or security change.
This is restoration, not supplemental refactoring or a new execution lease.

Selected proof: guarded terminal S01/S02 validation, malformed retirement
denials, existing synthetic pre-append byte/metadata tamper checks, and one
repository-aware atomic persistence/CAS exercise on an ignored backlog copy.
No canonical mutation, retired witness content access, broad fixture repair,
completed product suite replay, G1 approval or W2 execution is authorized here.
Record outcomes and exact candidate append-only after verification; obtain
independent control review before integration.

## Verification at the committed candidate

Candidate: `ea4db5d26d76fcaba037c6453cc55827a31d5045`.

- The guarded terminal regression first failed in 12 subcases before the fix,
  intercepting access before the protected witness was opened. After the fix,
  both supplements and ten malformed projections passed (1 test, 16.073 s).
- The unchanged synthetic pre-append byte/metadata tamper regression passed
  (1 test, 7.727 s). This tests synthetic inputs, not historical private bytes.
- Ruff lint/format passed for both changed Python files. Mypy passed for both
  with process-local `MYPYPATH=tests/foundation`. Initial unconfigured unittest
  and mypy commands failed import resolution; corrected invocation, not weakened
  code/assertions, resolved that setup issue.
- The real repository-aware writer proof passed: canonical validation returned
  zero errors; `command_gate_approve` persisted a synthetic approval only in an
  ignored copy; `save_validated` rejected a stale CAS without overwriting it.
  Live canonical bytes remained identical. The protected witness was never read.
  `fixtureW2Unchanged` in the report means only the fixture W2 **Wave record**;
  gate-derived task/slice transitions are confined to the disposable copy.

Proof producer `artifacts/tmp/G1-persistence-check-01.py`, SHA-256
`5185394a17c738b28885583a9b58395de3870bc1abc8ed04db63e2f22e4a6b50`.
Result `artifacts/tmp/G1-persistence-check-01.json`, SHA-256
`50d24666cb55aef5172293f5716ff819fbfd93b44c40026f21368cf1cf94b883`.
The producer and raw report remain local. No completed product suite was rerun.

Independent reviewer `/root/w1_continuation_preflight` found no blocking code
findings at this candidate; final disposition must also authenticate the result
above. This note does not approve G1 or replace independent full-Wave qualification.

Final disposition: **APPROVED for bounded control scope** after the reviewer
authenticated both hashes and the completed persistence proof. No remaining
material findings. See [independent record](G1.recording-review-R01.json).
