# Independent W1.A08.T02 implementation preflight at 3432d6cd

Reviewer: `agent:/root/t03_acceptance_review`. This is a retained adverse advisory,
not a formal task submission/review or an approval. The reviewer independently
passed the four focused tests and build validation, and authenticated seven
reviewed inputs to candidate blobs. Shared CSS changes were judged narrow and
appropriate; no product regression was demonstrated.

Two P2 acceptance-coverage gaps were reported:

1. **UX01 short-page proof:** the case labelled `short-empty-home` actually
   scrolls. Independent actual Chromium observations were 994 px document height
   at 1440x900 and 1015 px at 1280x720, with footer tops 950.984/971.984. All
   recorded workspace cases were tall too. Add a genuinely short state or a
   clearly labelled shared-shell fixture; assert it fits and its footer reaches
   the viewport bottom before checking sidebar/body/footer equality.
2. **F02 missing observations:** the valid Projects/light/1440 report has four
   Panel bodies. Replacing only `panelFlow` with `[]` still passes the validator.
   Bind the complete observed body count and require its matching measurements;
   add missing, truncated and invalid-count negatives for non-Task-Center cases.

Immediate cause: the new first test used a state name as evidence of shortness,
and the list validator lacked a consumer inventory guard. The retained adverse
CSS reproduction remains valid but did not cover these two verifier omissions.
No threshold, original capture or historical report is to be weakened or replaced.
Captures from producer 3432d6cd remain historical; regenerate after correction.

## Remediation observations

The missing-inventory negative was run before changing the collector/validator:
one test failed because omitted Projects-style flow data still yielded no
errors. The corrected check requires a positive, integer observed body count and
an exact measurement-list length; absence, empty/truncated lists, booleans and
wrong counts now fail. The actual collector observes that count independently
of the returned flow list. No product CSS changed during this remediation.

The misleading initial-home label is corrected to `initial-empty-home`. A new
explicit minimal shared-shell CSS consumer fixture asserts document height at
or below the viewport, footer bottom at the viewport bottom, and actual
sidebar/body/footer relationships at all three dimensions and both themes.
Its first run exposed three stale viewport-height observations immediately
after resize (900 requested / 720 observed, and 450 requested / 720 observed).
The test now uses the existing qualification convention of two animation frames
before sampling. It does not poll for an expected geometry or relax tolerances.
All six focused tests passed afterward. A deliberately content-sized shell
demonstrates that footer-to-viewport fill cannot be inferred from touching edges.

The original 108-image preflight bundle was committed as evidence-only delivery
`57bf82d4b8ccfa2b2442094133b9acb737066ad7` and independently of the new checker
changes authenticated using its original producer. Manifest canonical SHA-256:
`210fd4a518b6da6ab3e18436075c5b18d4fa30ac91ebd9d34110e2009188a350`.
This authenticates its historical bytes, not closure of these two omissions.
