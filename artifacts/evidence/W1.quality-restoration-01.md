# W1 governed-quality restoration

Predecessor: `cb1c256f59ac7f3b42eab664980b9ed442b4981b`.
W1.C02.T01 is DONE, independently approved and integrated into local main.
The campaign checkout is retained; ordinary execution remains PAUSED while this
bounded control repair runs. No original task, approval or adverse evidence is
reopened or rewritten.

Route: [bounded maintenance](../../docs/automation/workflow-efficiency.md#bounded-maintenance).
The existing exact quality command failed with three formatting failures and
144 typing errors in 16 files; lint passed all 226 governed files. The retained
observation is [W1.C02.T01 preflight-02](W1.C02.T01.preflight-02.json).
This is restoration of required checks, not optional structural cleanup or an
allocation of unknown supplemental refactoring capacity.

The fresh predecessor run reproduced exactly those failures before source edits:
`artifacts/tmp/W1.quality-restoration-predecessor-01.json`. Raw machine-local argv
stays ignored; no new account/path data is copied into tracked evidence.

## Intended delta and invariants

- Keep the quality runner, type/lint rules, governed roots, benchmark thresholds,
  product behavior and all existing assertions unchanged.
- Make the three imported test directories explicit packages; append only their
  marker files to the quality inventory. Keep tool imports under the established
  bare-module convention, without packaging tools or changing namespace policy.
- Correct only reported fixture typing: explicit non-null/type assertions,
  accurately typed heterogeneous inputs, variable-length adapter tuples,
  non-shadowing fixture names and test-lifetime method patches. Keep malicious
  payloads and immutable-mapping mutation attempts intact.
- Retain concrete FastAPI fixtures and complete the existing recalculation fake's
  delegation contract; do not weaken application or repository protocols.
- Production/control edits are limited to the actual datetime parameter type,
  the reported list/set variable collision and dictionary annotation, plus the
  three formatter corrections. No authority or runtime semantics change.

Independent preflight by `agent:/root/quality_control_review` found no route or
proposal-level blocker. That is not approval of a future implementation. Patches
in helpers must remain active through their callers; preserve bound originals
and coroutine behavior. Avoid broad casts on production outputs.

## Verification and disposition

Capture a fresh predecessor quality failure, then require the unchanged complete
governed-quality command to pass. Run affected existing AI/service/desktop import
discovery and registry/routing, recalculation, planning-approval and quality-control
tests, with real protected persistence where touched. Preserve negative cases
and compare discovered coverage. Commit and verify the exact candidate, then
obtain independent control review before local integration.

Full W1 profile/native/security/packaging qualification remains separate. The
134.022 ms gateway p95 failure remains open; no latency, slice, Wave or G1 approval
is implied. The protected witness remains filename/status-only, and the excluded
sibling checkout remains untouched.

## Development observations and selected closure

The first repair run retained eight residual typing errors and format/lint
failures in `artifacts/tmp/W1.quality-restoration-development-01.json`.
Explicit service packages exposed two bare sibling imports; these now use
`tests.service` without changing native-helper behavior. The next unchanged
quality run passed formatting, lint and typing for all 229 governed files
(`artifacts/tmp/W1.quality-restoration-development-02.json`). These are development
observations, not committed-candidate qualification.

Candidate verification selects the unchanged complete quality runner; AI and
registry/routing contracts; protected routing functional cases (excluding only
the separately failing named latency benchmark); affected service repository,
gateway and registry tests; selective-recalculation service/graph/end-to-end;
quality, Wave-approval and additive-inventory controls; UI-conformance tests
using the annotated fixtures; and explicit discovery of affected desktop/service
packages. The probe-build test uses a synthetic build result, not native UI.
All original assertions remain, with additional non-null/type assertions where
required. Full W1 qualification and the retained latency finding remain separate.

Independent pre-commit review found that a new initialization assertion in the
decode-scope cancellation test could fail before signaling its parent, causing
a hang. Initialization now signals in `finally`, propagates an already-failed
child before cancellation, and uses bounded startup with unconditional cleanup.
Candidate checks include deliberate null-decoder injection, which must report
the assertion promptly, plus the unchanged successful cancellation assertions.

The prospective commit guard rejected the existing synthetic token in the
recalculation API fixture as a generic API key. The fixture now uses a repeated
synthetic character of the same 64-character length; the guard, scanner and
authentication behavior remain unchanged. No commit bypass or policy change.

Direct CLI characterization of the canonical native-helper import failed with
`ModuleNotFoundError` before fixture-path validation. The helper now explicitly
adds the repository package root. A new isolated-process regression stops at
nonexistent paths after imports, before provider construction or project/vault
access. It is separate from the mocked-build fixture. Development of that
regression also exposed its initial placement under a mocked subprocess fixture
and a missing tools import; both are corrected before candidate verification.
