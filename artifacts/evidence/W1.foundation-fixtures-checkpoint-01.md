# W1 historical fixture maintenance checkpoint

Predecessor: `39558eb0183a8196a03ecc1578e00b3b4f4ee568`.

This is a bounded test-only maintenance checkpoint, **not integration or W1
qualification approval**. Product code, controllers, schemas, approved scope,
historical approvals and release criteria are unchanged.

Historical tests now use owned temporary repositories and an explicitly synthetic
external-input fixture. The helper changes only reviewed expected-digest operands
inside test-only validator clones. Actual hashes, Git ancestry, approval checks,
atomic persistence, crash recovery and denials remain active. The protected local
witness is not read, copied, hashed or committed. New helper files are included in
the existing quality inventory.

Focused diagnostics covered the repaired historical boundaries. Latest results:

- Diagnostic 17: 33/33 tests passed; process group drained.
- Diagnostic 18: 14/14 tests passed, including the corrected packet descriptor
  and 13 fixture-safety tests; process group drained.
- Formatting, lint and typing passed for all 233 governed Python files.
- Independent scoped review found no material coverage weakening. These are
  pre-commit diagnostics/review, not committed-candidate qualification.

One unresolved test is explicitly deferred under the owner's time limit:
`test_canonical_cascade_passes_real_semantic_persistence`. Its temporary worktree
conflicts with an immutable historical resume binding. Independent assessment
found no existing narrow relocation route. Do not rewrite history, bypass the
validator, skip this test or report it as passing. This is a fixture-qualification
blocker, not evidence of an application defect.

Raw diagnostics and adverse rounds remain in ignored local evidence. Full W1
qualification remains incomplete. Keep local main unchanged; proceed with
unaffected product qualification, without treating this deferral as release
approval or a change to acceptance criteria.
