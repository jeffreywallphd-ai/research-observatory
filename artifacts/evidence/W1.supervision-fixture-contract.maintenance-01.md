# W1 supervision-fixture contract maintenance

Predecessor: `c0bb3a6b5e7e21531b0634fd1c3030596fd10717`.
Route: bounded verification-control maintenance; independent review required.

CAP-01.S03/CAP-01.S04 require strict compatible supervision and genuine failure
stimuli. CAP-07.S01 added `models.catalog.read` and `models.catalog.refresh` to
both production Core and native supervision. The two example fixtures still
omitted those capabilities, so their purported compatible stimuli would fail
before exercising the intended lifecycle boundary.

## Exact scope and invariants

Add only the two missing literal entries to the health expectation in
`apps/desktop/src-tauri/examples/supervision_check.rs` and both messages in
`apps/desktop/src-tauri/examples/supervisor_fixture.rs`. Their predecessor raw
SHA-256 values are respectively
`83b64c4ea634856629b7c71a6646f33c5ac22c11cd5fa792a2832bb93a41aa9a`
and `04554ab540a803bbc5926b392169c81209e6d5449b96dbefea35a1e29c2a4e1f`.

Production contracts, exact-list compatibility checks, authentication,
failure modes, process containment, samples, budgets and historical evidence
are unchanged. No product authority, refactoring or acceptance expansion.

## Checks and remaining qualification

`tests/desktop/test_supervision_fixture_contract.py` checks exact ordered parity
among Core, the production supervisor and all three example message lists.
Register that new test in `quality-scope.json`; no check or root is removed.
Before edits: the production pair passed; all three stale expectations failed.
After edits: all three tests, Ruff lint and format checks passed.
The first combined scoped type check omitted the imported Core module and failed
module resolution. Including that unchanged dependency passed the existing
format/lint/type commands; no import suppression or configuration change.

- Red: `artifacts/tmp/W1.supervision-fixture-contract.red-01.json`, SHA-256
  `663da28a96a5497cf5639e7e5417f4074f5fc9d437176df9205a4b53118682ba`.
- Development green: `artifacts/tmp/W1.supervision-fixture-contract.green-01.json`,
  SHA-256 `bfc21f6d92fed40d0e9d6ee7be82ff636ae03a25a07486e9ef1cbd04c7294258`.

These are pre-commit source checks, not actual native execution. Commit-bound
regression, Rust example compilation and independent control review precede
integration. Fresh full W1 and real packaged supervision/performance remain
required; ordinary-profile/signing permission is not supplied by this note.
