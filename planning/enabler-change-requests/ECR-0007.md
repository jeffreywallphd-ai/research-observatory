---
document_type: enabler-change-request
record_schema_version: 4.0-proposal
change_request_id: ECR-0007
title: Align production styling with Academic Minimal 1.5
status: amendment-pending
execution_state: non-executable
classification: product-scope-security-experience
target_wave: W1
proposed_amendment_id: W1.A08
bootstrap_unit: W1.A08.B00
authorized_task_ids:
  - W1.A08.T01
  - W1.A08.T02
requested_by: repository-owner
prepared_by: codex
---

# ECR-0007 — Align production styling with Academic Minimal 1.5

Status: R01 changes requested; strict-descendant R02 remediation pending
independent exact-commit review and explicit human approval; non-executable.

Proposed authority: `W1.A08`, ordered after adopted `W1.A01`–`W1.A03`, the
terminal `W1.A04` reservation superseded by `GOV-MIG-0001`, and adopted
`W1.A05`–`W1.A07`.

Classification: the conservative v4
`product-scope-security-experience` envelope. This packet authorizes a bounded
production-styling correction and a live-product conformance boundary. It
reaffirms the already approved `RO-UI-ACADEMIC-MINIMAL-1.5` reference unchanged
and does not reserve or materialize a new reference.

## 1. Decision requested

Approve one append-only W1 amendment owned by `CAP-01`, with one mixed
contribution and two ordered tasks:

1. `W1.A08.T01` consolidates the shared production styling foundation and
   brings every currently implemented W1 desktop surface into general visual
   and interaction alignment with Academic Minimal 1.5.
2. `W1.A08.T02` adds a deterministic live-product styling-conformance boundary
   so the actual React/Tauri frontend—not a copied reference fixture—is checked
   for reusable-style discipline, computed geometry, responsive behavior,
   themes, accessibility, and representative visual composition.

Conformance is deliberately not pixel-identical reproduction. Normative tokens,
semantic states, control geometry, spacing rhythm, content hierarchy,
responsive behavior, interaction states, and accessibility remain binding.
Illustrative copy, data, and exact page composition remain non-normative, and
small rendering differences that preserve those contracts are acceptable.

The currently implemented surfaces in scope are the application shell;
lock/recovery and service-boundary states; Projects; Project Home; Project
Settings; Application Settings / Security & sign-in; Research Intent; Audit &
Lineage including controlled recalculation; Task Center; Diagnostics; dialogs;
tables; forms; notices; and shared status/evidence panels. Later-Wave pages are
not pulled into W1.

## 2. Initiation assessment

### 2.1 Product and best-practice fit

The Vision and approved reference call for a quiet academic workbench with
consistent hierarchy, restrained density, predictable spacing, and reusable
semantic components. A small token-driven layout vocabulary is the sustainable
implementation fit: shared stack, cluster, grid, panel, form, notice, table,
dialog, and page-region rules should own common geometry, while page selectors
should express only genuine composition differences.

The selected direction keeps CSS close to the component boundary, uses logical
properties and semantic custom properties, favors low-specificity reusable
classes, and tests the built product at observable layout and interaction
boundaries. It does not introduce a CSS framework, CSS-in-JS runtime, or a
second token source.

### 2.2 Tested implementation baseline

The production frontend already imports the canonical reference token file
through `@research-observatory/ui-tokens` and has reusable typography, button,
field, table, dialog, notification, badge, boundary-state, and panel components.
Existing renderer tests cover substantial keyboard, focus, responsive,
reduced-motion, theme, workflow, and application-state behavior. These are
strong reusable boundaries.

The visible implementation nevertheless has a second page-specific styling
layer. `apps/desktop/src/app.css` and the component stylesheet contain roughly
three hundred selector blocks, including repeated control, card, table, notice,
and layout declarations. Several common containers rely on browser-default
child margins instead of an explicit content-flow contract. Production shell
geometry also diverges from the approved reference: for example, the sidebar
declares an 18 rem minimum around a 15 rem token, while the main region uses a
separate 80 rem width and lacks the reference page-content centering contract.
Those differences explain the inconsistent padding, margins, alignment, and
visual rhythm observed by the repository owner.

The current UI conformance profile validates the generated
`apps/desktop/dist` reference fixture. The real functional React product is
built separately in `apps/desktop/product-dist`; it is source-hash-bound and
covered by interaction tests, but it is not compared to the reference at the
same computed-geometry or representative screenshot boundary. A copied
reference page can therefore pass visual regression while the live product
drifts.

### 2.3 Refactor disposition

`W1.A08.T01` is bounded technical-debt refactoring plus defect correction. It
consolidates existing production styles and component composition without
changing accepted architecture or restructuring completed capability outcomes.
Its L estimate is 5 of the immutable 194-point approved-W1 baseline, or 2.6%,
within the standard 15% limit. `W1.A08.T02` is new verification/product-quality
work and is not counted as refactoring. No major refactor is authorized.

## 3. Frozen authority and stopped boundary

The immutable W1 base is packet commit
`594e63be501711d67d17a4aef176bb9b6a8748be`, approved by record commit
`901eb5c1351fa32c7173a5f0cebc2fdf9ddb1701`. Adopted `W1.A01`–`W1.A03`
and `W1.A05`–`W1.A07` remain unchanged. `W1.A04/ECR-0003` remains a
terminal reservation superseded by `GOV-MIG-0001` and is not executable.

At packet preparation W1 is `PAUSED` at Wave scope, with no active task in
`IN_PROGRESS` or `REVIEW`, no active recovery hold, and no active amendment
campaign. `CAP-07.S01.T02` is `BLOCKED` before product work, specifically to
preserve this quiescent experience-change boundary. Ordinary W1 work remains
held until W1.A08 is independently completed and adopted.

## 4. Governed experience disposition

`RO-UI-ACADEMIC-MINIMAL-1.5` remains the exact approved experience authority.
This amendment restores and strengthens production conformance to that
reference; it does not change the reference package, its approval, its page or
workflow contracts, or its illustrative/non-normative boundary. The ECR binds
the existing reference manifest, approval, style guide, semantic token file,
and reference component/layout stylesheet by hash.

The implementation must preserve the reference's 4 px spacing scale, 64 px top
bar, 240/72 px navigation widths, 28/20/16 px responsive page padding, 16 px
standard grid gap, 24–32 px section rhythm, 16/20 px card padding, 40/44 px
standard/primary controls, typography hierarchy, quiet border-led elevation,
visible focus, non-color states, logical reading order, zoom/reflow, both
themes, and reduced-motion behavior where those contracts apply. Page-specific
composition may differ when the product's real data and controls require it.

## 5. Bounded execution plan

### W1.A08.B00 — append-only bootstrap

After exact human approval, the bootstrap may append only W1.A08 and submit
its own evidence for independent review. It must:

1. authenticate the exact W1 base, predecessor amendments, W1.A04 terminal
   reservation, `GOV-MIG-0001`, immutable `MI-0001`, approved ECR-0007 packet,
   and W1.A08 approval record;
2. append W1.A08 in `APPROVED` state with only the hash-bound S01, T01, and T02
   inventory;
3. keep W1 paused, keep `CAP-07.S01.T02` blocked, and leave every ordinary task
   otherwise unchanged;
4. change no product, design-reference, architecture, schema, or release path;
   and
5. receive independent control/experience review before materialization.

### W1.A08.T01 — consolidate and align production styling (L, refactor)

- Establish reusable semantic layout and content-flow primitives in the shared
  UI component/style boundary for page regions, stacks, clusters, grids,
  panels/cards, forms, notices, tables, dialogs, and action rows.
- Replace materially repeated production declarations with those primitives or
  shared custom properties. Keep page-specific selectors only for real
  composition needs; do not introduce inline styling or a second token source.
- Align the application shell and all currently implemented W1 surfaces to the
  approved spacing, density, typography, radii, border hierarchy, control
  geometry, responsive layout, theme, focus, and reduced-motion character.
- Preserve product semantics, state transitions, data boundaries, routes,
  accessible names, focus order, keyboard behavior, and reference bytes.
- Add focused component and renderer characterization/interaction tests before
  or with production changes and obtain independent commit-bound review.

### W1.A08.T02 — qualify live-product styling conformance (M, new work)

- Build and exercise the real `apps/desktop/product-dist` React frontend with
  deterministic local adapters and representative states for every currently
  implemented W1 surface; a copied reference HTML fixture is not sufficient.
- Add a stable style-reuse check that detects inline declarations, unexplained
  raw geometry outside the approved token vocabulary, and materially repeated
  declaration groups while allowing documented composition-specific rules.
- Measure computed shell, page, section, panel, form, table, dialog, and action
  geometry at the controlled desktop, compact-desktop, and 200%-effective
  viewports in light and dark themes with reduced motion. Assert semantic
  tolerances rather than pixel identity.
- Capture deterministic representative product/reference views for independent
  visual review, with accessibility and interaction assertions remaining the
  deciding contracts. Record any accepted rendering variance explicitly.
- Run the affected renderer, shared-component, build, UI-reference,
  accessibility, visual, and performance checks; obtain independent task,
  slice, and amendment-exit review.

## 6. Dependencies and execution order

Both tasks depend on independently approved `W1.A08.B00`. T01 additionally
depends on completed `CAP-01.S02`, the existing desktop design-system and
accessibility foundation. T02 depends on T01 so conformance is measured against
the consolidated implementation rather than freezing known drift.

S01 receives one independent experience/accessibility/maintainability review.
Amendment exit records a W1 experience/quality integration checkpoint. Adoption
leaves W1 paused; ordinary resume and `CAP-07.S01.T02` recovery are later
explicit transitions.

## 7. Verification and completion

Risk-selected task checks cover shared CSS/component contracts, live renderer
states, production build integrity, style reuse, computed geometry, responsive
layout, light/dark themes, focus and keyboard behavior, zoom/reflow,
reduced-motion, accessibility, and representative visual composition. Existing
domain, persistence, service, security, and model-gateway suites are replayed
only where changed-path impact makes failure credible. Full affected/repository,
packaging, performance, and Windows-x64 qualification remains due at W1 exit.

The amendment is complete only when all current W1 product surfaces exhibit a
coherent Academic Minimal hierarchy and spacing rhythm; common geometry is
owned once where reuse is effective; live-product conformance cannot pass by
rendering copied reference HTML; no valid interaction/accessibility behavior
regresses; T01, T02, S01, and amendment exit are independently approved with
all blocking findings closed; and the experience/quality checkpoint is recorded
while W1 remains paused.

## 8. Rollback and recovery

- Before approval, reject, defer, or withdraw this inert packet; W1 remains at
  the current recoverable pause and no product or reference authority changes.
- A failed B00 review preserves its immutable attempt and uses strict-descendant
  bootstrap resubmission; no product task materializes.
- A failed task, slice, or exit review preserves adverse history and uses
  bounded remediation that replays the open styling, accessibility, or
  maintainability risk boundary.
- Styling changes remain source-controlled and reversible. Failed build,
  interaction, accessibility, visual, responsive, or performance checks prevent
  adoption; they do not authorize a reference-baseline rewrite or weakened
  threshold.
- If conformance requires changing the approved reference, accepted
  architecture, or product semantics, stop and prepare a separate explicit
  decision rather than expanding W1.A08.

## 9. R01 finding and strict-descendant remediation

Independent review R01 of candidate
`3d868e579ba23a833f3a71650cd77f5f71c632b3` returned
`CHANGES_REQUESTED`. Its immutable ledger is
`planning/enabler-change-requests/ECR-0007.review-R01.json`, introduced by
commit `5423e4c918ad6aa71653e04a36545674e78c7423`, with SHA-256
`bb500ffc2fe59fc8a1603d21fd81ce669e89087147fde0b0acc4a9fa032baad1`.
Finding `ECR-0007-R01-F01` established that the packet bound
`assets/tokens.css` to checkout-specific CRLF bytes instead of the immutable,
approved Git blob.

The bounded Tier-2 governance-maintenance candidate
`7ac7f5d999da871716b30640ec1c8f2115398c6c` corrected the generic v4
governed-file validator so clean tracked files use Git-canonical bytes, pending
untracked or materially changed files use local bytes, and exact-commit
validation remains fail closed. Its independent no-finding review is
`artifacts/evidence/governance-maintenance-ecr-bound-file-canonicalization-1.0.review-R01.md`,
introduced by commit `4eaa413b23d72c52d2229574d9fbb76ef6ea7338`, with SHA-256
`68afcaab1bab656f680cdde536c99f53d53149e5246870913fa2ee4561e1d701`.
That maintenance increment grants no ECR or product authority.

The first live review-site regeneration then exposed a second raw-byte consumer
in `plan_review_site.py`. Supplement candidate
`fe9e5345595e12fe63887b343034bcec5796ab80` made that generator reuse the
same Git-canonical validator and added an integration regression. Its R01
review, committed at `1942ab0ca231aaafd2c47aef92d11a1314b72af9`, correctly
requested an evidence-only clarification because broad integration-preview
checks used pending ECR bytes. Strict-descendant correction
`e8c27fcf12a8fdc2889c3385922a7f974e658333` separated exact-candidate proof
from that provisional paired replay. Independent R02, introduced at
`f5e55a84e5f0ced0bb175c35e4dafcbdcf9c7ad9`, approved the supplement and
closed `GOV-MAINT-ECR-CANON-1.1-R01-F01` with no new finding. The approved R02
ledger SHA-256 is
`ba152b70811e38bf0a23280b8f9acc77b41a02580cc17cb72e6472c116aa49c1`.
All maintenance records remain append-only and grant no amendment execution.

This R02 proposal replaces only the governed token digest with the
repository-stable approved SHA-256
`e6aa1ebf847e983f4f5c9d20ad0e753716737cdfbedf617df2292bf510bebfa5`,
regenerates the hash-bound review projection, and preserves every other scope,
budget, experience, rollback, and execution boundary. Human approval remains
unavailable until an independent R02 reviewer closes `ECR-0007-R01-F01` for
the exact strict-descendant candidate with no open blocking finding.

## 10. Alternatives

1. **Consolidate shared styles and add live-product conformance — selected.**
   This fixes the observed product quality and closes the verification gap that
   allowed drift.
2. **Patch spacing page by page.** Rejected because it would deepen duplicate
   styling and make later surfaces less consistent.
3. **Require exact pixel equality with reference mockups.** Rejected because
   mock content and composition are illustrative; exact equality would be
   brittle and would conflict with the requested general, not identical,
   alignment.
4. **Adopt a new styling framework.** Rejected as unnecessary foundational
   churn for a token and component system that already has sound boundaries.
5. **Defer to W6 or release qualification.** Available but not recommended;
   W1 would establish weak styling patterns and a misleading visual gate for
   every later desktop surface.

## 11. Non-goals

No governed-reference edit or new reference ID; no product workflow, domain,
security, authentication, privacy, storage, service, model-provider, or release
behavior change; no later-Wave page implementation; no CSS framework or
CSS-in-JS runtime; no pixel-perfect requirement; no deletion or weakening of a
valid test; no major refactor; no W1 qualification or G1 approval; no local-main
integration before completed review; no remote push, external effect,
destructive action, or substantial spend.

## 12. Exact approval condition

Human approval is legally available only after the remediated proposal, packet,
schema, and review page are committed as one strict-descendant R02 candidate;
all v4 authority, planning, reference, and refactor-budget checks pass; and an
independent experience/accessibility/architecture reviewer records `APPROVED`
for that exact candidate, explicitly closes `ECR-0007-R01-F01`, and leaves no
open blocking finding.

Required human statement:

> Approve ECR-0007 as W1.A08 at packet commit `<40-character SHA>`, authorizing
> only bootstrap W1.A08.B00, contribution W1.A08.S01, and tasks W1.A08.T01 and
> W1.A08.T02 as hash-bound in the packet. Reaffirm the already approved
> RO-UI-ACADEMIC-MINIMAL-1.5 reference unchanged and authorize bounded
> production styling alignment plus live-product conformance. Keep W1 paused
> and CAP-07.S01.T02 blocked until W1.A08 adoption and explicit ordinary resume
> and task recovery.

That statement authorizes creation of a new immutable
`planning/wave-amendment-approvals/W1.A08.json` record. It does not append the
amendment, execute B00 or a product task, approve a task or slice, adopt W1.A08,
resume W1, recover T02, qualify W1, approve G1, merge to main, or push remotely.
