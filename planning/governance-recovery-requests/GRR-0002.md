---
document_type: governance-recovery-request
record_schema_version: 2.0-recovery-proposal
recovery_request_id: GRR-0002
title: Restore repeatable ECR chaining and the post-resume T03 recovery transition
status: pending-human-approval
execution_state: non-executable
classification: control-plane-recovery
target_wave: W1
bootstrap_unit: GRR-0002.B00
required_change_request: ECR-0003
required_amendment: W1.A04
prepared_by: codex
---

# GRR-0002 — Restore repeatable ECR chaining and the post-resume T03 recovery transition

Status: pending independent packet review and exact-commit human approval; non-executable.

W1 is paused at the unrecovered `CAP-02.S04.T03` boundary. `W1.A03` is adopted, `HOLD-W1-GRR-0001` was lawfully released, W1 was explicitly resumed, and both possible recovery invocations then failed closed without changing T03 or the backlog. The tested controller cannot satisfy its own post-resume compare-and-swap contract, and the current ECR validator cannot represent the next repair amendment after the released GRR-0001 hold.

## 1. Decision requested

Approve only the bounded governance bootstrap `GRR-0002.B00`.

The bootstrap may install `HOLD-W1-GRR-0002` and repair the reusable GRR/ECR control plane so the next inert proposal `ECR-0003 / W1.A04` can be represented, independently reviewed, and separately approved. GRR-0002 does not approve W1.A04, change the T03 recovery command, recover or approve T03, resume W1, approve G1, merge to main, or push remotely.

The later W1.A04 packet must authorize only the minimum correction that separates:

- the immutable Wave resume boundary recorded before the resume-state commit; and
- the current committed `HEAD` used for stale-writer compare-and-swap identity.

It must also require a real isolated-Git sequence test that performs pause, resume, commit, and recovery through the actual command and persistence stack.

## 2. Trigger evidence

### GRR-0002-F01 — Critical: the approved recovery transition is unsatisfiable

At the exact adopted and resumed state:

1. W1 records `campaign.base_sha = bf27fddd8e1732feadc698cd77f540ea9e1c7a0e`, the clean commit supplied to `wave resume`.
2. The resume transition changes `planning/backlog.yaml`; its validated state is committed at `54d03055b1c6d9b702859d5fefbbac1e38de39df` so the worktree is clean.
3. Recovery with `--base-sha 54d03055...` fails with `Exact T03 recovery base must match the resumed W1 compare-and-swap boundary`.
4. Recovery with `--base-sha bf27fddd...` fails earlier with `Base SHA must equal the current Git HEAD`.
5. No supported command advances the recorded campaign base after the resume commit. A Git commit also cannot contain its own final SHA as a field without changing that SHA.

Both denials occurred before persistence. `CAP-02.S04.T03` remains `BLOCKED`, has no `recovery_control`, evidence, or review, and the current backlog matches the committed W1 pause state.

### GRR-0002-F02 — High: the focused test suite missed the required state/commit sequence

The lawful-transition and real-persistence tests construct an already-active Wave whose `campaign.base_sha` equals the current test `HEAD`. They do not invoke `wave resume`, persist that state, commit it, and then invoke the real recovery command. The mocked identity path therefore proves atomic mutation once its preconditions are artificially co-satisfiable, but not that the repository workflow can ever reach those preconditions.

### GRR-0002-F03 — Critical: the ordinary ECR lane cannot represent the repair

The v2 ECR semantic validator requires a matching recovery hold whose post-bootstrap boundary already names the exact proposed ECR, amendment, and task IDs. The only existing hold is released and names `ECR-0002 / W1.A03 / W1.A03.T01`. `ECR-0003 / W1.A04` therefore cannot pass canonical validation or append through `taskctl amendment bootstrap-submit`. Editing the controller or backlog directly would bypass the append-only authority lane.

## 3. Frozen boundary

- Branch: `codex/w1-windows-local-runtime`.
- Pause record: `7c67ac9fdd39c4db2322cefd9ab5d603865d0efd`.
- W1: `PAUSED`, scope `wave`, category `infeasible`, no lease.
- `CAP-02.S04.T03`: `BLOCKED`, exact historical product candidate `59079efccc122a7d56a9f18efc20030851bf32a9`, no recovery/evidence/review.
- W1 authority: base approval plus adopted W1.A01, W1.A02, and W1.A03.
- W1 checkpoints: W1.CP01 and W1.CP02 retained.
- `HOLD-W1-GRR-0001`: `RELEASED`; it must not be reopened or rewritten.
- G1: `PENDING`.

## 4. Bounded bootstrap scope

`GRR-0002.B00` is control-plane-only. It may:

1. add a second, sequential recovery authority and active `HOLD-W1-GRR-0002` without replacing or reactivating GRR-0001;
2. add a new versioned ECR schema/validator path for the next amendment while preserving byte-valid historical ECR-0001 and ECR-0002 validation;
3. make GRR/ECR/amendment identities and matching active-hold ownership generic and consecutive rather than hard-coded to the first recovery;
4. add exact-commit, canonical-path, review-independence, stale-writer, ordering, and atomic-no-write tests for a released predecessor hold followed by a new active hold; and
5. produce a decision-complete stopped-recovery handoff for GRR-0002.

It may not implement or execute the later T03 recovery correction. That work belongs only to separately reviewed and human-approved `ECR-0003 / W1.A04`.

## 5. Required later amendment boundary

After B00 is independently approved, the controller may freeze—but not approve or execute—`ECR-0003 / W1.A04` with exactly one proposed task, `W1.A04.T01`.

That task must:

- preserve the recorded resume boundary and current-HEAD compare-and-swap identity as distinct, hash-bound concepts;
- keep wrong/stale/dirty/rewritten/duplicate recovery attempts byte-stable;
- prove the actual pause/resume/commit/recover sequence in isolated Git rather than mocking the contradictory identities into equality;
- retain W1.A03, GRR-0001, W1.CP02, the historical T03 lineage, product/reference bytes, privacy controls, and release gates unchanged; and
- stop only at T03 `IN_PROGRESS`, after which ordinary commit-bound evidence and independent privacy/task review remain required.

## 6. Verification and independent review

Before B00 may be approved, evidence must cover the exact approval-to-candidate diff; second-GRR and next-ECR identity binding; released-predecessor/active-successor hold behavior; canonical path safety; append-only review/remediation; backlog schema/semantic validation; generated views and review pages; Python quality; Git hygiene; and zero product/reference changes.

GRR-0002 needs an independent control/security packet review at its exact clean packet commit. B00 then needs a separate independent commit-bound review. ECR-0003/W1.A04 needs its own independent packet review and exact-commit human approval. None of these approvals substitute for another.

## 7. Alternatives

1. **Approve bounded GRR-0002.B00 — recommended.** Restores a reusable legal lane and preserves every prior authority and product byte.
2. **Keep W1 paused indefinitely.** Safe, but T03 and all dependent W1 work remain blocked.
3. **Replay or rewrite the resume/T03 history.** Rejected because it would obscure the demonstrated controller defect and disturb truthful historical lineage.
4. **Directly patch taskctl or the backlog.** Rejected because the ordinary ECR lane cannot authorize that patch and direct mutation would bypass the repository’s recovery policy.
5. **Reuse or reopen GRR-0001.** Rejected because GRR-0001 is terminally released and its packet, hold, B00/B01 attempts, reviews, and release record are immutable.

## 8. Rollback and recovery

- Before approval, reject or defer this inert packet; W1 stays paused and T03 stays blocked.
- During B00, any failed check or adverse review preserves the new hold and uses append-only strict-descendant remediation.
- After B00, reject, defer, or withdraw ECR-0003 without resuming W1 or releasing the hold.
- After W1.A04 adoption, release HOLD-W1-GRR-0002 only after its bound security checkpoint; then explicitly resume W1 before retrying exact T03 recovery.
- Never delete, rewrite, or synthesize a GRR, hold, ECR, amendment, checkpoint, task block/recovery record, finding, review, evidence record, or Git object.

## 9. Exact approval condition

Approval becomes legally available only after the proposal, packet, review page, exact file hashes, current W1/A01/A02/A03 authority, stopped boundary, and trigger evidence are committed at one clean immutable commit and an independent control/security reviewer returns `APPROVED` for that exact commit.

Required human statement:

> Approve GRR-0002 at packet commit `<40-character SHA>`, authorizing only bootstrap unit GRR-0002.B00 and its hash-bound governance/controller/test scope. Keep W1 paused and CAP-02.S04.T03 blocked. Require independent B00 approval and a separately reviewed and explicitly approved ECR-0003/W1.A04 before any T03 recovery correction, W1 resume, or ordinary task execution.
