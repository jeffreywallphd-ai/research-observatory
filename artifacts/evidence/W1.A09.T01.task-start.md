# W1.A09.T01 acceptance closure

Claim base: `92175839dc92a736a6d5ad0330d13efb382701f1`; owner `codex`.
Authority: immutable ECR-0008 packet at `2160cc9917282d80848189dc9669fe3827979246`,
owner approval introduced at `fcf627b4c83af81d0c3472526477bc7aa68d819c`,
ADR-0004 and ADR-0026. The exact backlog criteria govern, not this worksheet.

| Acceptance boundary | Invariant and proof |
|---|---|
| Exact approved publication | Preserve the reviewed 56-file proposal; only enumerated approval/derived-hash substitutions. Authenticate Git package and strict approval ancestry. Keep inert proposal unchanged. |
| Semantic compatibility | Freeze original 1.5 catalog/page contracts and source authority before publication. Fixed generator pins remain authority; adjacent manifest cannot supply replacement acceptable hashes. Compare every generated semantic payload, schema and fixture byte-for-byte. No runtime Git or migration. |
| Exact witness | Bind actual approved presentation package/commit and original semantic identities/hashes. Compare raw canonical text after only root identity/version substitutions; reject duplicates, unknown/nested changes, reordered arrays, changed purpose/output/checkpoints and missing/stale/substituted/unapproved witness. Independent checkpoint binds the witness digest, not vice versa. |
| Real predecessor boundary | Before source changes, create and close a disposable 1.5 project using actual Core services/SQLite with accepted intent, primary stage and supporting handoff. Retain exact projections and immutable setting rows; reopen in a fresh process after the bridge, compare IDs/hashes/history and exact return authority. This plaintext test fixture is not encrypted/native qualification. |
| Fresh visual truth | Existing pinned renderer freshly captures all 66 observations, unchanged engine/fonts/viewports/tolerances. Explain every changed capture, preserve historical baselines and adverse findings. Original approval strictly precedes publication, which strictly precedes baseline. |
| Consumer order | Independent control/experience checkpoint before enumerated presentation consumer rebind. Semantic1.5 and token1.4 identities stay unchanged. A08 remains PAUSED and its effective-reference overlay changes only at qualified A09 adoption. |

First checks are predecessor capture and source/generator characterization, then
adversarial compatibility tests before bridge implementation. Reference capture
cannot precede its lawful publication; exact package/authority checks precede it.
Read-only independent preflight requested from `agent:t01-r02-independent-reviewer`.
No new authority gate discovered. Native dialog, project-route/readiness fixes,
returned A08 spacing/sidebar, and full Windows/W1 qualification remain deferred to
their approved tasks; this task does not claim those outcomes.

Safety: only synthetic disposable project data; no installed application/profile
changes. The unrelated protected `W1.A04.B00.json` witness remains untouched.

Initial characterization (before-01) passed real persisted draft/selection/stage
creation. Inspection correctly found its intent was still draft: it does not
close the accepted-intent criterion. Retain it and add a stronger predecessor
with accepted intent and prior revision history before any source replacement.
