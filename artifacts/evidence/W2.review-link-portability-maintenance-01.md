# W2 historical review-link portability maintenance

Observed candidate: `9cd11940a84a5ea088f099db2a42cf4e6da73ed3`.
Planning-review qualification rejected ECR-0004, ECR-0008, GRR-0001 and GRR-0002:
their retained generated pages differed from deterministic regeneration in the
implementation checkout. The generator embedded the local checkout root in
source-file URLs. Retaining older generated bytes avoided a new path disclosure
but did not satisfy the exact-regeneration contract.

The bounded correction emits validated, URL-encoded repository-relative source
links using the existing depth-1 page convention. It changes only generated
navigation destinations: historical packet/approval/proposal bytes, displayed
hashes and commits, immutable Git-source descriptors, authority, and the exact
regeneration checker are unchanged. It does not rewrite or relabel old evidence,
add a privacy exception, or consume the optional runner-progress allocation.

Two focused regressions were added before the generator change. Both failed on
checkout-dependent file URLs. After the correction, development replay passed
both tests: governed-experience links are root-independent and safely encoded,
unsafe relative paths are rejected, and recovery links resolve to the same six
synthetic sources under two different roots without changing source bytes or
historical identities. Only isolated synthetic fixture sites were generated;
the canonical review site was not regenerated during this increment.

Focused lint/format checks cover the generator and affected test file. The main
workflow must regenerate the canonical site once, commit the consolidated
candidate, run affected qualification and obtain independent commit-bound
control review. Development checks here are not that qualifying disposition.

Pre-commit inspection also found two unchanged historical workspace-path lines
elsewhere in ECR-0008's generated body. Independent exact-retention review bound
the staged successor to the fixed accepted baseline and parent above: only the
generated link row changed; both retained lines keep identical bytes and logical
fields. Full-byte pinned credential scans of baseline and successor found zero
matches. The cumulative registry remains ignored local state; prior admissions,
preservation policy, scanner and hook protections remain unchanged. This is not
permission for new disclosures, a product approval, or remote publication.
