# W1.A09.T03 — owner requirement and acceptance preparation

This is a pre-claim requirement note, not a completed task-start pass, task claim,
approval replacement or permission to execute ahead of T01/T02. The immutable
backlog/task packet remains authoritative; complete the actual acceptance-closure
pass after claiming T03. Preserve this owner direction in that pass and review.

## Owner direction, 2026-09-05

“Can you ensure that in dev and production that a default folder is pre-selected
for projects. I want to limit the user's need to think about folder structures.
Please add that to T03. Please continue working, following the workflow and
AGENTS.md.”

Interpretation: the new-project form should already have a usable, visible
default parent location, not merely offer a starting directory after the user
opens a picker. The ordinary path should require a friendly project name,
research details and explicit Create—not directory typing or a mandatory folder
choice. Keep Change folder available. Opening an existing project still requires
selection of the actual project; a parent container is not an openable project.

## Required acceptance coverage

- One native resolution policy supplies the same default in dev and production
  for the same Windows user. Do not derive it from checkout, working directory,
  executable location, debug flags, hard-coded usernames or an app-identifier
  variation used for the test harness.
- Show the chosen parent and generated complete destination before explicit
  Create. The existing shared DirectoryPickerField/Change folder interaction,
  slug stability, collision handling and read-only path preview remain.
- A valid current form selection is not replaced by late default resolution.
  Cancel preserves the prior selection and form; lock, close and stale/unmounted
  results retain the existing protected-generation behavior.
- Default discovery, form mount and folder selection must not create/open a
  project, silently grant access, migrate data or transmit anything. Any required
  directory provisioning must have an explicit authorized mutation boundary.
- Do not silently choose cloud-synced, redirected/network, protected or unusable
  storage merely because it appears as Documents. Retain local-first, current-user,
  path validation and no-overwrite guarantees. Do not claim that a local path can
  prove absence of every external backup/sync agent.
- Cover fresh user, existing default, missing/unavailable/denied location,
  Unicode/spaces, redirected/synced candidate, dev/release parity, cancellation,
  override persistence during the form, late responses and app lock/close. A
  native failure must provide safe recovery, never an editable-path workaround.
- Real Windows/native/Core creation must succeed from the default without
  requiring a folder dialog, then close/reopen the resulting synthetic project.

## Authority and implementation boundary

ECR-0008's approved UX addendum already permits a native known-folder initial
suggestion or validated selected location; its exact 1.6 new-project reference
shows a selected parent, Change folder and a destination preview. The existing
Core contract requires an existing parent. Those facts do not automatically
authorize creating a dedicated parent container on form mount, a new broad
filesystem command or a modified frozen reference package.

Read-only independent preflight has been requested to distinguish compatible
selection-policy detail from any new native/provisioning or governed-experience
contract. The exact default and any necessary supplemental authority must be
resolved before T03 product edits. Do not satisfy this note with only a picker
starting-folder hint, and do not weaken the immutable approval checks to add it.

Unchanged work continues at dependency-eligible T01. This request does not
authorize automatic project opening, privacy changes, a second style/token
system, researcher-data moves or W1 passing.

## Selected policy and independent preflight, 2026-09-05

Read-only source review by `t01_r02_independent_review` found the following
bounded policy compatible with the existing-parent contract. This is advisory
acceptance preparation, not a commit-bound task approval or executed proof.

- Resolve Windows `FOLDERID_LocalAppData` and the existing `Research Observatory`
  product parent, identically in dev and production. This is the project parent,
  never its `security` or `security/profile-default` subtree. Normal Core actor
  initialization already uses this product parent; default discovery must not
  initialize a credential store or create a directory itself.
- A narrowly named read-only native resolver takes no caller-supplied path and
  returns an available validated location or a bounded failure. Bind it to the
  local main window and protected-action generation. Keep the purpose-specific
  folder picker request unchanged; discovery is not a fabricated user-selection
  event and grants no general filesystem operation.
- Validate existence, locality, canonical/reparse boundaries and existing Core
  installation exclusions. Missing, denied or unsuitable defaults leave safe
  recovery through Change folder; do not silently fall back to Documents,
  an account root, repository, temporary directory or the LocalAppData root.
- Core readiness alone does **not** prove actor/vault initialization succeeded:
  `main.py` can continue with no local actor after initialization failure. The
  resolver attests only to an existing validated location. Preserve ordinary
  Create authority checks; do not inspect secrets to infer stronger readiness.
- Preserve no-overwrite behavior, including a project named “Security” colliding
  with the existing sibling rather than entering, enumerating or replacing it.
  An unusual installation overlapping the candidate remains excluded.

Add explicit acceptance cases for no filesystem mutation before Create,
identical dev/production resolution, late resolution losing to a user choice,
lock/unmount races, missing/redirected/installation-excluded parents and the
security-name collision. Verify the normal fresh-user Core startup establishes
an eligible parent and actual default-based creation works. If that is not true,
do not conceal it with resolver-side provisioning: report the unmet case and
bind any needed provisioning through existing append-only authority.

## Claimed acceptance-closure pass, 2026-09-05

Taskctl claimed T03 for `codex` from
`6730524c7fe19e2ba6f2500f367d8e6e5689bb2c` after T02's independent R01
approval. The seven immutable criteria, ECR-0008.execution-01 exact inventory,
its UX addendum, approved presentation 1.6, ADR-0004/0018 and existing Core
path/package contract govern this implementation. No reference, catalog,
privacy policy, Core schema or amendment authority changes are intended.

Current input inventory: `ProjectsWorkspace.tsx` has the parent-directory,
child-directory-name and open-root editable inputs. Other implemented renderer
workspaces use current project identity or read-only diagnostic paths; those
are not directory-entry controls. The creation/operation recovery and native
contract fixes at the claim base are the predecessor to preserve.

| Acceptance boundary | Failure/invariant and planned proof |
|---|---|
| AC1 shared composition | One exported accessible field for parent/open, no path/name input or browser enumeration; static package/inventory tests followed by real built-renderer interaction. |
| AC2 destination | Friendly-name ASCII slug, reserved-device prefix, bounded stable random fallback; preserve selected parent verbatim and freeze edits during mutation. Unit vectors plus real Core collision/no-overwrite and unchanged request on retry. |
| AC3 native authority | Exact purpose/optional-string request, exact tagged path-free failures, main-window caller, existing local canonical directory. Request/response substitutions, filesystem/reparse/install denials and real system-dialog proof. A lexical renderer decoder does not establish filesystem authority. |
| AC4 selection lifetime | Single renderer/native admission; cancel keeps value/details, focus only returns to the still-owning form, discarded late result does not release native admission early. Promise/barrier tests and real keyboard/Escape. No Core mutation on mount/default/selection/cancel. |
| AC5 protected generation | One dedicated STA owns COM/dialog/message-only cancellation window; lock/policy/close invalidates, dismisses and discards results. No Core mutex across Show, no cross-thread raw COM pointer, no main-thread join. Native barrier/fault tests cover before registration, before/during Show, after result and cleanup. |
| AC6 principal boundary | Actual Windows dialog owner/selection/cancel/lock/close/focus and actual native/default/Core creation/reopen in isolated synthetic fixtures. Doubles never replace this proof. Existing Core package, stable-directory, locks and atomic staging/recovery remain unchanged. |
| AC7 governed styling | Reuse field/button/description/stack primitives plus one wrapping selectable location rule. Both-theme long-path geometry/focus, reduced motion and 720x450 built renderer without horizontal page overflow; no A08 sidebar correction before return. |
| Owner default clarification | Read-only zero-argument native known-folder resolver, existing LocalAppData/Research Observatory parent, same dev/release policy. Explicit unavailable recovery; never provision, inspect secrets or fall back to another directory. Late defaults lose to a user choice. Fresh-user initialization and default-based creation require separate actual proof. |

First tests: shared field lacks an editable path and has label/help/output/button
associations; exact decoder negative outcomes cannot smuggle paths; slug bounds,
Unicode-only/reserved names and stable preview. The native author will introduce
lifetime/path tests before implementation where practical. Native dialog UI
tests necessarily follow a compiled adapter but must remain actual Windows
observations, not merely a mock enum returning selected.

Independent read-only preflight by `agent:/root/t01_r02_independent_review`
is incorporated: sticky cancellation before HWND registration, same-STA Close
(not PostThreadMessage), reservation retained through real cleanup, policy
generation changes as well as manual/idle lock, asynchronous main close, and
strict result suppression. That agent becomes native implementation author and
cannot provide this task's independent disposition.

Selected checks are affected component/renderer contract and interaction tests,
native library/races, protected-source tests, real Windows/native/Core fixtures,
types/lint/build and folder geometry. Adapt existing interaction harnesses to
select through the new control without deleting their mutation/lock/recovery
assertions. Full repository/Windows matrix remains W1; accumulated packaged
qualification remains T04, but T03's explicit real-dialog criteria cannot be
deferred. No migration or scholarly-semantic change is applicable. No mandatory
authority gate was found in this pass; unresolved actual-platform proof is not
claimed passed.

## Implementation observations and retained adverse checks

- The first selected renderer test run failed as intended: the shared field and
  adapter did not exist, and the Projects assertion found the three editable
  directory inputs. After implementation, 27 tests across seven selected files
  pass. A JSX fixture initially contained two literal backslashes instead of
  one; correcting the fixture expression preserved its Unicode-path assertion.
  Both package type checks caught an optional-ref `undefined` mismatch; the
  component now passes the standard null ref, without weakening type settings.
- Early browser replays correctly refused the stale product manifest while the
  native author was editing build inputs. Coordinated fresh builds retained
  that check. The existing Projects recovery and Task Center interactions pass
  after selecting through the real shared buttons with an explicitly labelled
  native double. The old invalid-editable-parent validation probe now uses an
  invalid display-name control character; invalid picker paths are separately
  rejected before native calls. The no-transport/retained-entry assertion stays.
- The new collision fixture omitted the required `retryable` problem field;
  the generated decoder correctly rejected it. Adding the exact existing
  contract field made the collision/stable-retry browser scenario pass. No Core
  contract or error decoder was changed to accept the incomplete fixture.
- Read-only adversarial review by `agent:/root/t01_publication_checkpoint`
  identified an actual default-ownership defect in the first implementation:
  opening the chooser prematurely counted as a selected override. The added
  browser regression reproduced the stuck default-discovery state after first
  cancellation. The fix separates pending selection from completed ownership,
  suppresses adoption while choosing, and repeats only the read-only lookup
  after cancellation/failure without a prior parent. Genuine selected overrides
  still win. Both response orderings pass; unavailable/failed outcomes share
  that closure coverage. No project mutation is replayed.
- Added failing-before vectors for C1 control characters and a joined
  destination beyond the unchanged 4096-unit open-root limit. The lexical
  decoder now agrees with native control rejection; the form cannot create a
  destination that would exceed its later open contract. It gives safe change
  name/folder help, not an editable fallback or truncated path.
- The native author's initial symbolic-link fixture received Windows error
  1314 (privilege unavailable). A real non-elevated junction now exercises the
  reparse denial, not a skipped or mocked pass. The new native fixture's initial
  PID/counter reuse pattern was also identified for correction before adoption;
  this does not repair the separately retained legacy test-isolation issue.

These are preliminary mutable-source observations. The task remains in
progress; exact commit-bound checks, real Windows dialog/default/Core proof,
independent task review and all broader qualification remain due.

## Resumed acceptance checks, 2026-09-05

Implemented-directory inventory (functional `apps/desktop/src/app`, excluding
test fixtures and non-runtime reference pages):

| Former input / current output | Implementation and closure |
|---|---|
| New-project parent directory | `ProjectsWorkspace`: shared `DirectoryPickerField`, native default plus Change folder; no editable input. |
| New-project child directory name | `ProjectsWorkspace`: derived stable name and labelled complete destination `output`; no separate editable directory field. |
| Existing-project root | `ProjectsWorkspace`: shared `DirectoryPickerField`, explicit Open after selection; no editable input. |
| Support-export destination | `DiagnosticsWorkspace`: existing read-only diagnostic paragraph, not a directory input; remains an output. |

The full functional-app directory/folder/path scan found no other implemented
directory-entry control. The browser probe asserts the three former input IDs
are absent, and component tests check shared label/help/output/button semantics.

- The resumed native C1 regression first failed (12 passed, one failed); using
  the language control-character predicate closes the renderer/native mismatch.
  Nineteen focused picker tests now pass, including actual unshown COM/STA and
  message-only-window cleanup, and actual acquired lock-manager fixtures for
  protected cancellation, late selection suppression and None-mode no-op.
  These fixtures do not claim a visible dialog, Tauri IPC or authentication.
- A Vitest invocation incorrectly rooted at the repository recursively found
  private publication/qualification copies with missing dependencies. It is an
  invalid-scope run, not accepted product evidence. Running from the two actual
  package roots passes 28 desktop and five shared-component tests; both package
  type checks pass. No private-copy dependencies or ignore rules were changed.
- The source-bound built-renderer script passes six cancel/failure/default-order
  races, chosen-default precedence, protected unmount/late-result discard,
  keyboard cancellation/focus and stable collision retry. Six long-path layouts
  cover both themes at 1440x900, 1280x720 and 720x450 with no horizontal overflow.
  Its initial synthetic problem used `about:blank`, which the unchanged generated
  decoder correctly rejected. Correcting the fixture to the canonical problem
  URN—not weakening the decoder—made the scenario pass.
- A real supervised Core fixture starts with the complete product/security/vault
  chain absent. Core itself provisions it, creates a SQLCipher project, preserves
  its bytes on collision, closes/reopens it and reopens in a new Core process
  using the retained DPAPI vault. Eighteen unchanged lifecycle tests also pass,
  including reparse/install exclusions, exclusive locks and atomic recovery.
  The first fixture attempts exposed runner errors: Windows text-pipe CRLF was
  rejected by the strict LF authentication record, the venv launcher PID differed
  from the real child, and creation correctly returned the contracted HTTP 200
  rather than the runner's assumed 201. The runner now uses LF, the actual base
  interpreter and the existing endpoint contract. No Core behavior was altered.
- The shared native setup refactor made one old security-source assertion's
  literal extra borrow obsolete. Its failure was reproduced; the replacement
  follows production app-local-data resolution into shared setup and actual
  lock acquisition, without changing native behavior or adding a needless
  borrow. Expanded checks include both new directory bridges and asynchronous
  close cleanup. All four focused source-boundary tests pass.
- Independent advisory review requires a shared actual Tauri-builder fixture
  rather than counting the earlier synthetic-generation dialog probe as AC5/6.
  The bounded integration-harness-only entry substitutes fixture policy/vault/
  WebView/default parent, confines selections and Core operations to the fixture,
  and denies support export and authentication changes. Production has no
  override/activation switch/new IPC. Separate debug/release real Shell resolver
  and default-based Core proof remain due; no real user policy/vault is read.

The retained `*.precommit.json` reports are working-source observations, not a
claim that the claim-base commit contains this implementation. Final checks must
be rebound to the committed candidate before submission. Actual Windows UI and
independent review remain open; no task transition is justified yet.

## Native and harness boundary findings, resumed 2026-09-05

- The integration example originally lacked the Windows Common Controls v6
  manifest and failed loading `TaskDialogIndirect`. The shared probe build
  helper now embeds and verifies the existing asInvoker/Common Controls
  requirements and binds source inputs before/after building. This is an
  example-build correction, not a production build-policy change. A separate
  harness pre-Ready state access was corrected to capture native state only
  after Tauri Ready; neither failure is treated as product qualification.
- Actual Shell initialization rejected a valid mixed-separator fixture path
  with E_INVALIDARG at SHCreateItemFromParsingName. A real unshown-COM regression
  passed for backslashes and failed for mixed separators before the fix. Only
  slash-to-backslash UTF-16 normalization at the Shell call boundary was added;
  existing path and identity validation remains. All 20 focused picker tests
  then passed. The real visible chooser subsequently opened, selected Unicode
  paths and cancelled with focus/form preservation.
- Actual visible lock and main-close checks exposed a defect missed by manually
  pumped unshown-COM tests: the renderer cleared the draft on lock and Core
  stopped, but the Shell modal remained open. Main close also waited until
  operator Escape. The retained path-free source/binary-bound adverse record is
  `W1.A09.T03.native-ui-observations-01.json`. No credential action was taken.
  These failures must be replayed on the correction; they are not passing AC5/6.
- The separate actual-Core proof harness initially used read-only directory
  sharing, which blocked unchanged Core staged publication. Real regressions
  demonstrate that read/write sharing with delete denied permits publication
  and prevents parent rename, but cannot prevent an in-place reparse by a
  concurrent same-account writer. Accepted ADR-0017/0018 exclude that adversary;
  the harness explicitly claims identity observation, not host-account
  isolation. Before/after identity checks, narrow subprocess environment,
  no-follow database reads, exact native hash/manifest binding, exclusive
  reports and terminate-only handling after observed drift were independently
  reviewed. Fourteen regressions and the fresh safeguards-04 report pass; the
  actual default-parent complete mode remains unrun at this checkpoint.

No task/release approval is inferred from these working-source checks. T03 stays
in progress while native modal cancellation and actual default closure finish.

## Further principal-boundary findings, 2026-09-05

- Two cancellation corrections passed narrow unshown tests but failed actual
  visible lock replay. SendNotifyMessage delivery, the Close wrapper and an
  actual-modal WM_NULL post returned success while the dialog remained open.
  `W1.A09.T03.native-ui-observations-02.json` preserves c05/c07 failures and the
  c06 observer-timeout non-proof. The form cleared, but the modal exposed its
  fixture path until operator Escape. No credentials were invoked. Diagnostic
  wrapper success must not be misrepresented as the exact raw COM HRESULT.
- The first complete debug/release default proof resolved the expected actual
  Shell parent, and fresh fixture create/reopen/collision passed, but actual
  default creation failed. The retained diagnostic rounds 01 through 04 narrow
  the failure to Core's canonical-parent guard: a newly created synthetic
  staging/state directory resolves into a Package/LocalCache alias despite
  no symlink/junction and equal file identity. This occurs before database/key
  creation. Core validation remains unchanged; no passing actual-default
  result is claimed. Fresh control proof also passes with two newly created
  parent chains and restart/reopen of both projects.
- A separate read-only process comparison found both approved venv and base
  launchers execute identical Python 3.14.6 image/DLL bytes and report no
  package identity. A venv-only switch or Store-interpreter diagnosis is not
  supported by that evidence. Actual serving-child and launch/mapping facts
  must be checked before selecting a remedy; no real vault is used.
- Inspection of `verification-precommit-01.json` found its native fixture
  filter selected zero tests. That individual check is not evidence of fixture
  safety, despite exit zero and the preliminary report's aggregate `ok` field.
  The correction uses the actual harness module filter and rejects zero-test
  Rust runs. Existing report bytes remain unchanged; a new report must prove
  the six fixture tests actually executed.

## Native cancellation closure replay, 2026-09-05

The installed Shell implementation showed why an arbitrary application-window
callback was not completing `IFileDialog::Close`: its close flag was consumed
through Shell callback completion, not the private cancellation callback. This
is a runtime-specific diagnostic inference, not a portable contract. Production
uses only documented `WM_CLOSE`, queued to the current-STA/current-process
validated exact `IOleWindow` dialog HWND; no private offset/message is shipped.
Sticky cancellation and late-result discard remain in place.

On frozen picker source `b93741c0`, the real visible c08 manual-lock and c09
main-close replays pass without operator cancellation: Show returns cancelled,
the pending guard clears, Core stops, and the dialog disappears. Lock also
clears the draft, path and project form. c10 ordinary keyboard cancellation and
Unicode folder selection pass on the same binary, preserving focus/form state
and leaving the synthetic selected folder empty. The full build/source binding
and filtered observations are retained in `native-ui-build-03.json` and
`native-ui-observations-03.json` under this task's evidence prefix. Prior failed
observations remain unchanged. No authentication action was performed.

Actual serving-child metadata before imports, after startup and after a read-only
catalog request matches the expected Python image/DLL and process classifications:
medium integrity, no package/AppContainer identity and disabled token virtualization.
This does not explain or excuse the earlier Package/LocalCache alias. A normal
owner-launched comparison has been requested while other verification continues;
no Core guard, activation policy or security control was weakened.

## Explicit owner stop and resume checkpoint, 2026-09-05 17:58 UTC

The owner requested stopping within three minutes. All synthetic GUI instances
and owned test processes have exited. No commit, task transition, local-main
integration or remote operation was performed during this resumed increment.
The authoritative T03 claim remains IN_PROGRESS at base
`6730524c7fe19e2ba6f2500f367d8e6e5689bb2c`; inspect/renew its existing lease on
resume rather than claiming a new task. WIP and every adverse report are retained.

Completed since the earlier checkpoint:

- Actual native c08 lock, c09 main-close and c10 cancellation/Unicode selection
  pass within the explicitly isolated scope recorded in observations-03. Their
  debug binary is `d099d4987be6e082cad54db82ec3ff605f39fd64a2ea83b9417c001c931ad67b`.
- The corrected verification filter really ran all six native fixture tests.
  The 18-command `verification-precommit-02.json` is nevertheless FAIL because
  the new hidden-HWND regression assumed the first retrieved message was the
  cancellation message. All other commands returned zero. Do not reclassify
  that report as a pass.
- The native author deterministically reproduced the same hidden-test failure
  by delivering unrelated message `0x805d` first, then corrected only that test
  to continue until WM_CLOSE/watchdog. One focused harness replay passes; the
  repeated production/harness subsets and post-edit Clippy remain unrun.
  Latest picker source is
  `d92245fed1ccc744d664b9615278d1b723eb7afc2b74abf18deb2527a2b87388`.
  The production portion is unchanged from the passing actual UI candidate;
  do not claim that observations-03 executed this later test-source revision.

Resume in this order, staying within T03:

1. Replay the affected native test subsets/Clippy, preserving the failed report
   and recording the test-only delta and new source hashes.
2. Close the independent advisory P2 finding in
   `tests/service/test_native_project_contract.py::build_project_probe`: it
   currently hashes the product manifest but does not itself validate the
   current renderer sources/assets before and after Cargo. Add the existing
   `product_build_errors` validation plus exact product source/artifact input
   binding and stale/tampered build-negative tests. This correction was
   announced but NOT IMPLEMENTED before the stop. Prior explicit fresh builds
   and visible observations are not allegations of tampering, but do not
   substitute for the missing enforced build boundary.
3. Check whether the owner has supplied the normal-launch comparison. The
   requested command was the repository Python executing
   `artifacts/evidence/W1.A09.T03.default-failure-diagnostic-01.py --attempt 5`.
   Its fixed report `W1.A09.T03.default-failure-diagnostic-05.json` did not exist
   at this stop. The owner was told this could wait until resume. Do not repeat
   unexplained actual-default writes, substitute a LocalCache alias, weaken
   canonicality, or launch ordinary frozen Core against the real vault.
4. Complete source/commit-bound renderer, actual default/Core and native build
   proof, obtain independent T03 disposition, then proceed to dependency-eligible
   T04. The advisory reviewer `t03_independent_preflight` found no other
   confirmed production blocker in its completed read-only scope, but did not
   approve the task and did not execute tests.

Use scripted tests for routine regressions. Reserve authorized Computer Use for
the short real Windows dialog smoke surface that scripts/doubles cannot prove;
ask again if that authorization is later withdrawn. The eventual blind novice
retest remains distinct and must not inherit project context.

The final full review-site refresh exceeded the remaining stop window and was
interrupted (session exited 1); regenerate `tools/plan_review_site.py --repo .`
on resume before relying on its worksheet projection. The source checkpoint is
saved, but current generated pages are not claimed fully refreshed or validated.

## Resumed closure checks, 2026-09-05 21:40 UTC

The owner resumed W1 and explicitly renewed Computer Use permission for isolated
test windows/dialogs. T03's existing lease was renewed through taskctl; no new
task was claimed, and no task approval, commit, main integration or remote
operation is implied by the following working-source evidence.

- The independent native replay ran three production and three harness subsets,
  each 25/25 with no ignored tests, and both library Clippy checks passed on
  unchanged picker `d92245fe`. `native-replay-02.json` is the passing scoped
  record. `native-replay-01.json` retains the expanded test-target Clippy failure:
  two existing needless borrows in unchanged application-lock test code. The
  earlier verification-precommit-02 failure remains unchanged too.
- The manifest-only build binding P2 was reproduced before correction: four
  missing-denial failures and one missing-renderer-hash error in six tests.
  The existing helper now validates the real product inventory before trusting
  manifest paths and binds exact renderer sources, assets, native inputs,
  manifest and validators before/after Cargo. Stable manifest rereads and
  post-validation hash checks reject interval changes. Eight regressions and
  Ruff pass, independently replayed in build-binding-review-01.json.
  The retained pre-correction helper hash from native-ui-build-03.json is
  `2c91dc218cccb1618627d7d1743d230459cf8d8ee9a558b0e89db17f2e68d10e`.
- Review identified an inherited Cargo output-target assumption. Two further
  failing-before tests showed that missing or alternate compiler output could
  attest the usual stale binary. The helper now requires Cargo's JSON
  compiler-artifact event to identify the exact expected executable before
  PE verification/hash recording. All ten tests and Ruff pass in
  build-binding-regressions-02.json; actual debug/release compilation passed
  with both output paths confirmed in native-builds-05.json. This is a bounded
  evidence-helper correction, not new product/security authority or a new
  controller. Endpoint checks do not claim atomicity against hostile
  same-account change-and-restore writes.
- Fresh product build, validated native-ui-build-04.json (161 bound inputs),
  and real c11 manual-lock, c12 main-close, c13 keyboard cancellation/Unicode
  selection/explicit-open observations pass in native-ui-observations-04.json.
  Both lifecycle interruptions dismissed the owned modal without operator
  cancellation. Lock cleared the draft/path/form and stopped Core. Selection
  preserved the friendly name and generated destination; Escape restored
  focus, Return reopened, and selecting a folder did not create/open a project.
  The sole precreated Unicode child remained empty. All three fixtures exited
  zero; no authentication action was performed and all windows are closed.
  The later Cargo-output correction rebuilt identical debug executable bytes
  `f73661fc06c4cb081ac08ac35808b2983fa9d4de4682b3e2343b6eecab6eac8d`.
- renderer-check-01.validated-build.precommit.json passes the actual built
  React/pinned Chromium checks: declared native/Core doubles, default-only
  Create, cancellation/failure/default races, fresh-form behavior, and six
  light/dark layouts down to 720x450. It is not actual default/Core creation.
  The task verification runner now inventories the new build-helper tests;
  its two selection-safety tests and Ruff pass. No full repository/profile
  replay was justified by these narrow helper/test-only changes.

Actual default-parent creation remains unresolved and must not be called passed.
The normal Windows PowerShell diagnostic (`default-failure-diagnostic-01.py
--attempt 5`) was requested again while other work continued. Its exact
default-failure-diagnostic-05.json report was still absent at 21:40 UTC. It is
reserved for the owner's normal-launch comparison; do not run that attempt
through the agent, repeat speculative actual-default writes, change Core's
canonicality guard, or substitute the observed LocalCache alias. After this
principal-boundary evidence, finish required source/commit-bound evidence and
independent T03 disposition before T04. W1 approval remains a later gate.

The previously interrupted review-site refresh completed successfully this
resume; refresh again after this worksheet update before relying on its text.

### Default verification boundary at 21:43 UTC

Independent build-binding-review-02.json confirms all ten regressions and Ruff
with no new blocking finding in the helper correction. Actual debug/release
builds also passed Cargo-output and PE verification. The narrowly scoped
default-parity-check-01.json then confirmed both actual native resolvers choose
the same real Shell/Core parent; input hashes and parent identity/mtime remained
unchanged. It starts no Core, creates no project, and reads no vault/policy.

The original attempt to launch that read-only comparison as a one-line shell
command failed Python parsing before executing the script. The retained normal
runner replaces the quoting mistake; this was not a product failure or a
passing comparison. Its Ruff check passed before the actual read-only run.

All executable scoped checks for this increment have finished. The required
normal-launch diagnostic report 05 remains absent. The next dependency is an
owner-run comparison, not additional approval: run the existing diagnostic in
ordinary Windows PowerShell and provide its result. Until then actual-default
creation is unverified; T04 and W1 approval are not eligible. Keep the existing
fail-closed storage contract, preserve all adverse reports and unfinished WIP,
and resume this same T03 after comparing that result. Computer Use remains
authorized for isolated fixtures unless the owner withdraws it; all fixture
windows and test processes are currently closed. No Git mutation or remote
operation occurred this increment.

## Owner normal-launch comparison and resumed review, 2026-09-05

The owner supplied diagnostic attempt 05 after the preceding stopped boundary.
The saved `W1.A09.T03.default-failure-diagnostic-05.json` has SHA-256
`a5a829c6df753a37c3f4413e3090072339daa8eca6c91594d72d882a157de617`.
Its exact inventory of 54 Core/runner inputs was compared with the expected
tracked Core inventory plus the three named diagnostic runners; every reported
hash still matches current bytes. No ordinary user vault/policy was accessed.

Both fresh and actual-default targets passed real unchanged Core/DPAPI/SQLCipher
create, close/reopen twice, encrypted database, exact collision rejection and
preservation of existing bytes without silent numbering. The two recorded
`ProjectLifecycleProblem` causes are the asserted duplicate-create negatives
(HTTP 409 / ALREADY_EXISTS), not missing successful-create results. Synthetic
fixtures remain retained. This clears the specific unverified actual-default
creation boundary; taskctl reopened T03 under the existing amendment authority.

The result does not establish the precise cause of previous tool-launched
canonical-parent drift. Earlier adverse reports remain unchanged. Do not weaken
Core's path guard or claim native/renderer/packaging/authentication proof from
this Core diagnostic. Fresh-process reopen of two fixture parents is separately
recorded in `default-core-fresh-two-parent-01.json`; the actual-default report
itself proves repeated close/reopen within the diagnostic session, not a fresh
Core process restart. Independent T03 acceptance review and final commit-bound
verification remain required before T04. The review is continuing without a
new human approval request.
