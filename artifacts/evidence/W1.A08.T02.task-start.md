# W1.A08.T02 acceptance closure

Task: Qualify live-product styling conformance. Claim base:
`46e88d171795b76a2f1588d69756f4184c01a89b` on the active W1.A08 campaign.
Authority: approved ECR-0007/W1.A08, its immutable T02 criteria, accepted
ADR-0004/0005/0006 and current workflow-profile contracts, and unchanged
`RO-UI-ACADEMIC-MINIMAL-1.5`. T01 is DONE with independent R02 approval and
its tested result has been fast-forwarded into local main.

## Reused baseline and bounded work

T01 already supplies shared production primitives, a passing 279-rule style
analyzer with ten exact composition/accessibility exceptions, and real renderer
proof for representative surfaces, tables, both themes, reduced motion,
enlarged text, long profiles, focus, overflow, and lock/recovery states. Its
immutable failed and provisional reports remain historical inputs. T02 must
extend that boundary, not replay T01 implementation or invent a new style system.

| Acceptance boundary | Planned proof |
|---|---|
| Actual functional product, not copied reference | Reuse exact product source/output manifest validation and offline functional navigation; missing, stale, substituted or copied-reference targets fail. |
| Every implemented workspace | Derive and compare the eight registered workspace identities and page-contract mappings; exercise selected representative states at 1440x900, 1280x720 and 720x450 in both themes (48 workspace cases). Add populated Task Center using valid existing contract fixtures. |
| Geometry and interaction | Reuse T01 semantic tolerances and measured typography, control, page, card, form, notice, table, action and dialog boundaries. Include designated lock/recovery/dialog cases, visible focus, non-color states, contained scrolling, post-scale overflow and operable controls. Missing, duplicate, nonfinite or out-of-tolerance cases fail. |
| Retained visual evidence | Capture deterministic privacy-safe product/reference views with case identity, state, theme, viewport, renderer/font identity, product manifest, exact source commit and PNG digests. Validate paths, bytes, inventory and provenance before accepting the bundle. Independent visual review records acceptable composition/rendering variance without waiving semantic or accessibility contracts. |
| Sustainable enforcement | Reuse the existing static analyzer. Connect complete live geometry to the authoritative desktop application check and keep capture persistence/provenance separate from page adapters. Negative fixtures reject omitted workspaces/themes/viewports and altered or stale capture evidence. |
| Performance and integration | Use existing unchanged desktop/component benchmark samples, absolute budgets and 20% regression limits on a clean disposable checkout of the tested source. Run affected lint/types/unit/build/reference/workflow/accessibility/visual checks and obtain independent task, slice and amendment-exit dispositions. |

## Invariants, failures and principal boundary

- Producer identity comprises the full source commit, actual product build input
  and output inventory, approved reference ID/package, pinned renderer settings,
  case/state mapping, measurements and capture hashes. A later evidence-only
  commit must not conceal a changed producer input. Artifact delivery is not a
  new visual baseline or design approval.
- Capture production and verification must reject stale builds, copied HTML,
  missing/duplicate cases, path traversal or redirected output, altered images,
  mismatched renderer/source/reference identity and incomplete writes. Local
  fixtures contain no researcher data and no unapproved network access.
- Real principal proof is the built React product in pinned Chromium, reached
  through actual workspace controls and typed local adapters, plus real file/Git
  persistence for the retained bundle. Parallel mock-only tests are insufficient.
- Domain migrations, credential policy, scholarly authority and native service
  transitions are unchanged; their implementation is not part of this task.
  Existing renderer interaction tests continue to protect those boundaries.
- The protected untracked W1.A04 witness is never read, hashed, staged, changed,
  moved, deleted or ignored to obtain a clean performance result. Qualification
  uses a separate clean checkout instead. No remote push or external deployment.

First tests: missing Task Center/theme/viewport cases; invalid geometry and
focus/overflow; capture path/hash/inventory/source-identity substitution; then
the smallest live populated Task Center and retained-file/Git round trip.

Risk-selected task checks cover these boundaries. The complete repository and
Windows-x64 release matrix remains a W1-exit duty; S01 and amendment-exit review
remain separate required dispositions. Read-only preflight found no new human
authority gate. Any genuinely new scope/reference/security decision still stops
at the existing governed boundary.
