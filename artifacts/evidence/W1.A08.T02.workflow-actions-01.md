# W1.A08.T02 workflow action coverage correction

Predecessor: `5ff382e5f6ecae3953cdc5aae263e5cdd5e81a97`; capture producer
`1517b16aa1fab212c7c28163d3c98ed92fc0d9a5`. Independent visual review of
captures-03 observed native-default Next/Return controls in Project settings.
This is a correction to criteria 2–4, not a new workflow or design decision.

Root cause: WorkflowContextBar used raw buttons outside the sidebar selector,
and qualification enumerated only already-styled `.ro-button` descendants of
the workspace, excluding the shared context above it. A control missing its
shared style therefore also escaped measurement.

Acceptance closure: use the existing shared Button for Previous, Next and
Return, preserving labels, disabled states, authority checks and callbacks.
Enumerate actual visible buttons independently of class in each workspace and
its main-content workflow context; bind the observed inventory to semantic
measurements and reject missing/truncated inventory. Actual computed padding,
radius and minimum target geometry must meet the existing control contract in
all three viewports and both themes. Add a failing component regression before
product edits; replay existing workflow authority/return tests and actual built
renderer qualification, then regenerate source-bound captures for independent
visual review. No new CSS, tokens, exceptions or navigation behavior is needed.

The principal boundary here is built React in pinned Chromium using existing
deterministic adapters, not native authentication or packaging. No real account,
policy, vault or projects are touched. Previous captures and adverse review are
retained. Performance is postponed until the corrected candidate is stable.

Test-first observation: the focused WorkflowNavigation renderer unit command
failed with 1 failure / 5 passes against the unchanged product component.
The failing assertion received `<button type="button" disabled="">Next step ·
Manuscript Studio</button>` without the shared class. This authoring observation
has an uncommitted new regression test and is not an exact-commit qualification
claim; the independent capture finding supplies the prior visual evidence.

First expanded actual-button replay: 48 of 49 Python tests passed, but the full
frame reported six Task Center control minimum failures. Those controls are
existing interactive `.ro-card` workflow summaries, not raw standard action
buttons. They already have the shared card grid/padding/radius and large actual
targets; requiring a declared standard-button minimum was a classifier error.
Preserve their card presentation and require actual height >= 40 plus card grid
layout, while retaining standard/primary declared minimum checks for ordinary
buttons. They remain in the measured control count and card semantic inventory;
none are excluded or given a per-task waiver. No Task Center product edit.
