# Test fixtures

Repository test data must be offline, deterministic, minimal, and accompanied by
machine-readable provenance and redistribution terms. The scholarly corpus in
`scholarly-corpus/` is entirely synthetic; it does not reproduce a real paper,
person, journal, result, or bibliographic record.

Run `python tools/fixture_corpus_check.py --repo .` after any fixture change.
