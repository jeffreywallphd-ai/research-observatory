# W1.A08.T02 quality registration preflight

The exact source-discovery check at `f2b38f64a1330404921220adaff98b9dab926ab5`
reports four previously unlisted committed files: the two new product layout
files, plus `tests/service/test_native_project_contract.py` and
`tests/service/test_project_probe_build.py` from the adopted correction.
No prior inventory entry or governed root may be removed or relaxed.

Before registering the service tests, isolated typing found missing configured
module search paths and three real annotation/name issues: the mock subprocess
runner shadowed unittest.TestCase.run, two class-level fixture attributes lacked
annotations, and the third-party pefile module has no published typing stub.
Rename only the mock attribute to run_process, declare the fixture attributes,
and locally document the missing external stub (import-untyped only). No build,
path-validation, native execution, manifest or assertion logic changes.

After those changes, all ten synthetic probe-build boundary tests pass. Ruff
lint/format and isolated mypy pass with the existing tools/Core/service-test
module paths. These tests patch compilation and PE inspection; no real native
window, service, policy, credentials or project was launched. This does not
relabel earlier native evidence as a new run. Existing actual native proof is
historical and source-bound to its own tested commit.

Next is an inventory-only independently reviewed maintenance candidate, following
the existing additive-registration mechanism. The separate UI range-recognizer
repair remains pending; this note does not authorize or approve it.
