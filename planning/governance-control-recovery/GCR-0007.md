# GCR-0007 — Replace infeasible GCR-0006 adoption binding

## Status

Pending independent packet review and exact-commit human approval. This packet is inert and grants no implementation or ordinary execution authority.

## Demonstrated GCR-0006 infeasibility

GCR-0006 was independently reviewed and exactly approved, but no B00 implementation began. A post-approval feasibility audit found that runtime-v6 requires the adoption-evidence file committed at A to contain `adoptionEvidenceCommit`, while the approved transaction topology defines A as that file's direct-child commit in P→A→F. A truthful A value would therefore need to appear in the blob whose contents determine A's Git SHA. That is a cryptographic self-reference and cannot be constructed.

The independent HIGH finding is frozen in `GCR-0006.feasibility-R01.json`. GCR-0006 remains immutable, historically approved, unimplemented, and inert. No controller, state, evidence, transaction, backlog generation, supplement, amendment, task, Wave, gate, product, main, or remote mutation occurred under it.

## Bounded successor

`GCR-0007.B00` replaces only GCR-0006.B00 execution authority. It preserves the same bounded product outcome: revision-11 reader/schema headroom for a future, separately governed `GRR-0002.S03/GRR-0002.B03` 11→12 repair.

The corrected evidence topology is explicit and non-circular:

1. P is the independently APPROVED GCR-0007 B00 state.
2. The evidence blob contains `approvedStateCommit: P`; it never contains A or F.
3. A is derived externally as P's unique direct child changing only the adoption-evidence path.
4. After A exists, the transaction and successor state bind A by path, SHA-256, and commit.
5. F is derived externally as A's unique direct child changing exactly backlog and GCR-0007 state; no canonical file embeds F.

After independent B00 approval, F may append one exact generation-neutral `GCR-0007` record with transition 11→11, `generation_neutral: true`, and supported ceiling 12. Live control and minimum-tool revisions remain 11. GCR-0006 is never appended as a generation.

## Separate S03 gate

GCR-0007 does not repair post-append amendment validation. A future identity-specific version-5 S03/B03 packet must bind the exact adopted GCR-0007 P→A→F authority, receive independent packet review and exact human approval, and alone may move revision 11→12 and implement the phase-aware repair. It must reject GCR-0006 as installed authority.

## Required proof

- Exact GCR-0006 packet, review, approval, schemas, and HIGH feasibility ledger remain byte-preserved and inert.
- Real-Git C→E→S→L→P→A→F lifecycle proves every parent and exact path delta without self-reference.
- Old revision-11 readers accept the current three-generation backlog and reject the adopted fourth GCR-0007 generation.
- New readers accept only the authenticated three-generation predecessor or four-generation successor and require GCR-0007 plus separate S03 authority at revision 12.
- Generic same-revision generations, every same-revision supplement, direct revision 12, and GCR-0006 substitution fail closed.
- The exact historic W1.A04 submission keeps the same byte-stable S01/S02 denial before and after adoption.
- Durable raw/canonical CAS, Windows replace/write-through, flush, crash, replay, partial-state, substitution, redirection, and recovery tests pass.

## Safety boundary

No GCR-0006 edit or implementation; no taskctl post-append fix; no S03/B03 creation, approval, installation, review, or execution; no W1.A04 append; no B02 reopening; no revision/minimum change; no competing hold; no hold release; no T03, W1, G1, product, experience, local-main, or remote action.

The exact witness remains untracked, unstaged, non-authoritative, and unavailable as submitted evidence.

## Alternatives

- **A — Approve bounded GCR-0007 (recommended):** correct only the impossible evidence binding and preserve the separate S03 repair gate.
- **B — Keep stopped (safe):** retain GCR-0006 inert and W1 unavailable indefinitely.
- **C — Reinterpret or edit runtime-v6 (rejected):** invents semantics or changes exact approved bytes.
- **D — Implement GCR-0006 anyway (rejected):** requires false evidence or an impossible Git object.
- **E — Patch taskctl or append W1.A04 directly (rejected):** bypasses the active hold and recovery authority chain.

## Recommendation

Independently review this inert packet. If approved, request exact-commit human approval for only GCR-0007.B00. Any mismatch retains the current stopped state.
