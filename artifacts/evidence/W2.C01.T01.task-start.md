# W2.C01.T01 — bounded import identity verification

Claim base: `c0da6393fe2f0280b7df314863f963a8d07dab16`.
Authority: the inherited CAP-04.S01.T03 criteria, approved CAP-04.S01 sections
9.3–11, ADR-0027 scientific identity/current rights, and the committed correction
spec. The original task and approval stay immutable. Initial admission correctly
rejected the implementation-only origin; the spec now binds the recorded DONE
approval at `f850468ef679651b34af44611aa39c8d147c6074`, with the same snapshot hash.

## Acceptance closure

- Same scientific identity, replay and changed-input manifests: multi-page
  identity equality plus affected publication tests. One fresh transaction per
  page must contain draft projection, current preparation authority and staged
  rows; no shared connection/cache across pages.
- Current project/draft/accepted parse, worker claim/time, rights and preparation
  identity remain authoritative: between-page revision denial, exact connection
  witnesses, and existing forged-row, current/historical rights and lease tests.
- Cancellation, bounded guards and atomic failure: the activity test must witness
  verification pages inside the guard and the whole identity pass outside it;
  replay late cancellation/authority and publication interruption/rollback checks.
- Shared draft projection retains public page/selection validation, historical
  decisions, 100-row/16MiB bounds and denial behavior: existing focused draft tests.
- Real boundary: fresh protected 100k import performance qualification under the
  unchanged reviewed baseline after code review. Prior failed sample stays FAIL;
  connection-count improvement is not a latency or encryption claim.

Add the multi-page regression and revision-change characterization before product
edits. Keep the private helper body equivalent to the existing projection and
change only the four declared product/test paths. No migration, public contract,
UI, security authority, threshold or qualification-method change. Previously
completed native/renderer/package checks are not rerun for this internal change;
affected persistence/worker checks and performance are fresh. Full Wave checks
remain required at Wave exit. Independent preflight recommended this exact
page-local boundary; commit-bound review follows implementation.

## Development observations (not qualifying evidence)

The two initial cases ran before product edits: transaction count failed with
seven opens instead of four, while between-page revision denial passed (3.898s).
After extraction, 13 hot-path/activity cases passed (15.037s). The final candidate
also retains the original order of page projection followed by the fresh-time
preparation check within the shared transaction. Focused committed checks follow;
development logs remain ignored local artifacts.
