# W2 reference activation: independent control review

Disposition: **APPROVED for the bounded verifier/witness increment only**.

Reviewed candidate: `3581771958db73c345f606162a4b02b34ff8162f`.
Maintenance predecessor: `a8955b86f75d628f2e23c7eac336de66c332b098`.
The independent delegated reviewer did not implement the increment, change its
source, run its producer tests, or commit this disposition. The checkout was
clean at the reviewed candidate before this evidence-only note was added.

## Scope and authority

The five-path increment consists of `tools/ui_conformance.py`, its workflow
profile contract tests, the separate 1.7 presentation witness, the contract
README, and the maintenance note. It repairs the verifier's ability to describe
and authenticate already-approved presentation 1.7. No new product, reference,
workflow, migration, or release authority is granted.

The 1.7 witness binds publication
`14b89c07a435a26bf894242a9e5f3e6429065055` and reference package SHA-256
`dcf31147ee386d48e8ac95725d648ee2b08c74ef0dd42c139c5f78508a90f878`.
The existing `reference_package_at` path authenticates the immutable pre-Wave
owner record, exact proposal, ancestry, single-use publication and package
inventory. The maintenance does not replace this authority with witness hashes.

The declared Source Manager region is appended to an existing required-region
list. Undeclared additions, duplicate or existing regions, changed region order,
workflow order and other content drift remain denied. Strict comparison remains
after the explicit mapping. The original 1.6 witness, approved reference bytes,
1.5 semantic snapshots and generator pins, persisted workflow meaning, activation
and visual baseline are unchanged. Historical 1.6 real-Git adversarial coverage
uses its accepted witness commit; 1.7 has separate real-Git coverage.

## Evidence reviewed

The maintenance owner reported execution at the fixed candidate with the normal
local test identity. The reviewer read the completed output and independently
hashed the retained log; the hash authenticates that observed file, not by
itself the fact of execution.

- `.venv/Scripts/python.exe -m unittest -v tests.contracts.test_workflow_profile_contracts`:
  **13 tests PASS, 110.475 seconds**, no skips reported.
  Log: `artifacts/tmp-reference-control-tests-03.log`.
  SHA-256: `65bf7e2644117daae1bae680b1b1a1d04658699668a597441580dbb2ca8e7743`.
- Cases include exact semantic generation without Git, legacy and pre-Wave
  publication authentication, missing/uncommitted/duplicate witnesses,
  staged/substituted/racing inputs, stale packages, forged publication,
  declared-region boundaries and malformed declarations.
- Owner-reported selected Ruff lint/format and focused mypy checks pass for the
  affected verifier/test selection and its workflow-contract dependency.
  Reviewer independently ran `git diff --check` over the complete maintenance
  range successfully. The final delta from the earlier reviewed control candidate
  only corrects a test-local variable type/name and adds malformed-declaration
  coverage; it does not weaken product or verifier assertions.

No blocking finding was identified. This disposition permits the separately
reviewable consumer-rebinding increment to proceed under existing 1.7 authority.

## Limits and preserved adverse results

This is not approval of consumer activation, rebuilt product/reference outputs,
a new visual baseline, whole-reference conformance, CAP-04.S02.T03 completion,
slice/Wave qualification, or release. Those checks and dispositions remain due.
The original native source-configuration Save/focus/reopen proof also remains
pending outside this maintenance increment.

The owner reported an accidental broader quality run with **55 errors across
13 unchanged source files**, and no error in maintenance files. Preserve that
adverse result as unresolved Wave-wide qualification; this review makes no full
repository quality PASS claim. Earlier development/failing attempts and the
prior 12-test run remain historical evidence, not substitutes for the final
13-test candidate run.
