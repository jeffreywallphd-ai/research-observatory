# Compatibility pointer for repository governance

Historical slice plans may cite this path. The repository authority is now:

- `docs/governance/repository-governance.md` for source precedence, mismatch handling, and change routing;
- root `AGENTS.md` for concise AI operating rules;
- `docs/README.md` for document routing; and
- `planning/README.md` for planning lifecycle.

The external setup kit's root `START_HERE.md` is a bootstrap guide and is not a repository authority.
