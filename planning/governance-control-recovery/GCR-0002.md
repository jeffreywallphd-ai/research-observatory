# GCR-0002 — Enable the next sequential recovery supplement

## Status

Pending independent packet review and exact-commit human approval. This proposal is inert and grants no execution authority.

## Demonstrated mismatch

After `GRR-0002.B01` received independent approval, the exact packet-bound `W1.A04.B00` retry failed atomically because the historical implementation candidate no longer equals current `HEAD`. No backlog bytes changed. A second review found that the next mandatory same-hold supplement is itself unrepresentable: the installed supplement reader and backlog schema support control revision 8 at most, while every supplement must strictly raise the minimum control revision.

The same review also confirmed an immutable safety boundary from `GCR-0001`: `artifacts/evidence/W1.A04.B00.json` is a trigger-only, untracked witness and may never become submitted W1.A04 evidence. A lawful B02 must authenticate those bytes but record a committed packet snapshot instead.

## Bounded prerequisite

`GCR-0002.B00` is a control-reader generation bootstrap under the existing `HOLD-W1-GRR-0002`. It does not create or replace a hold. It keeps live backlog control at revision 8 through implementation and independent B00 review. Only after B00 is approved may its adoption append one exact 8→9 control-generation record.

The revision-9 reader will add v3 supplement schemas and support only the next exact `GRR-0002.S02/GRR-0002.B02` installation. S02 remains inert until separately independently reviewed and exactly approved. B02 will own the historic-candidate and committed-snapshot repair; GCR-0002 does not authorize either behavior by itself.

## Controlled lifecycle

The repository-owner governance-mismatch protocol is available because the active hold continues to fail closed and the established revision-8 supplement lane cannot represent its own required successor. After an independent APPROVED packet review, the repository owner may exclusively create `GCR-0002.approval.json` at the exact reviewed packet commit. Its tracked-clean introduction commit on `codex/w1-windows-local-runtime`, with only the exact trigger witness untracked and unstaged, is the sole B00 execution base.

`tools/gcr2ctl.py` will implement exact submit, review, resubmit, adopt, and validate commands. Candidate scope is compared from the approval-introduction commit against the packet allowlist. Attempts, evidence, findings, closures, and reviewed state are append-only. The backlog remains revision 8 until a separately committed pre-adoption evidence record and direct-child, two-path adoption transition update only `planning/backlog.yaml` and `planning/governance-control-recovery/GCR-0002.B00.state.json`.

## Safety boundary

GCR-0002 grants no S02 approval or start, B02 execution, W1.A04 append, bootstrap review, task materialization, claim, T03 recovery, hold release, W1 resume, G1 approval, main integration, or remote push. It cannot promote, stage, track, mutate, delete, or replace the trigger witness. It cannot edit S01/B01, reopen B00/B01, create a competing hold, permit an 8→8 transition, or add a general historical-commit bypass.

## Recommendation

Approve the exact packet only after independent review. Keeping W1 stopped remains safe but leaves the mandated S02 lane impossible to install. Direct S02 creation, an 8→8 transition, reuse of `gcrctl.py` as an unreviewed generic controller, or promotion of the trigger witness would violate current authority.
