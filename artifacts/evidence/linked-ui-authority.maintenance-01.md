# Linked UI correction authority — bounded control maintenance

Predecessor: `9d6a5a12df7d55d81420128f70b4620dee282b19`.
Risk: Tier 2, evidence/control authority. W1.C04.T01 product work and all test
processes are quiescent. This increment changes no product behavior or approval.

## Reproduction and intended delta

W1.C04.T01 selected verification attempt 04 passed its product checks but failed
the UI gate: its independently completed W1.A05.T04 origin has authenticated
human-approved amendment/reference authority, not the ordinary task's
`human-and-agent-review` field. The existing adapter accepts that origin while
the UI gate cannot represent its inherited authority. Preserve this failed
attempt (`artifacts/tmp/W1.C04.T01.verify-04.json`, SHA-256
`456f855bd7cde87552657083f998c4a458d9da36853e75eb4425db4c21a3043a`).
The separate physical first-Tab pass remains bounded native evidence, not a
waiver of this failed control.

Recognize only a named, adopted, authenticated amendment's completed original
approved-reference implementation, retaining its exact packet, original UI
contract, independent review, and the correction's unchanged current reference.
Do not invent or modify an original review field. Ordinary unsupported origins
remain denied. Current correction conformance and independent review still apply.

The correction's six-path product scope cannot admit control repair. Reuse the
existing bounded-maintenance candidate/evidence/review carrier as separate
control-only work. Attribute every otherwise-out-of-scope change in the full
correction history to an exact independently accepted three-commit chain. A
reviewed filename is not permission for later edits to that file. Preserve
legacy callers' pre-correction cutoff, all adverse records and frozen task/spec
bytes. No new controller, amendment, schema version, scope or refactoring.

## Selected checks and non-goals

- RED/green ordinary-versus-amendment authority and invalid approval/contract
  cases; existing live claim, origin, reference and scope denials.
- Real Git candidate/evidence/review persistence; self/adverse review, missing
  delivery, mixed product, extra paths, hidden add/revert, later edits and merges
  remain denied. Exact scope must fail without authenticated review.
- Affected UI-gate/task-evidence tests, Python quality, and real public gate on
  the committed reviewed delivery; independent control disposition before local
  integration or task submission.

No product edits, broader frame-verifier repair, new human decision, credential
access, protected witness access, reference change, historical rewrite or push.
The original task base and all six admitted product/test paths remain fixed.
