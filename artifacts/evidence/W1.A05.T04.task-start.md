# W1.A05.T04 task-start acceptance closure

## Frozen authority

- **Task / claim base:** `W1.A05.T04` claimed by `codex` from
  `bd8d752a0fcec1f40b1a8abe59b793c783946e4e` on
  `codex/w1-windows-local-runtime`.
- **Approved scope:** W1.A05 task T04 and slice S02 in the exact approved
  ECR-0004 packet. Implement the application-wide Application Settings
  destination and its Security & sign-in experience; do not invent security
  policy in the renderer.
- **Governing experience:** `RO-UI-ACADEMIC-MINIMAL-1.4`, especially
  `design/ui-reference/application-settings.html`, its canonical approval, and
  the page/coverage contracts.
- **Governing implementation contract:** the T03 typed native commands and
  strict decoders for status, Hello availability, transition prepare,
  password-recovery prepare, and transition commit.
- **Dependencies:** independently completed W1.A05.B00 and W1.A05.T03.
- **Non-goals:** no university/cloud identity, no project-policy changes, no
  PIN/password/biometric capture, no renderer unlock or transition authority,
  no automatic Hello-to-password/no-login fallback, and no new sign-in mode.

## Current implementation and predecessor boundaries

- `ApplicationRuntime.tsx` keeps the current project and workspace in renderer
  state and exposes Project Settings, but has no distinct Application Settings
  workspace. Its profile modal still calls the deliberately retired
  `application_lock_configure` command, which returns
  `RO-SIGN-IN-TRANSITION-REQUIRED`; this is the inherited activation-reference
  mismatch T04 must close.
- The current product route inventory has 32 assembled pages and omits the
  approved `application-settings` destination while the v1.4 reference has 33.
  The implementation must add the route without renaming or displacing the
  existing Project Settings destination.
- `applicationLock.ts` strictly decodes the T03 v1 snapshots, provider
  availability, unlock attempts, and two-phase transition results. These exact
  contracts, reason codes, and timeout values are the predecessor fixture; the
  UI must not weaken them with permissive casts or inferred success.
- The native boundary owns provider verification, same-SID recovery,
  compare-and-swap persistence, audit, lock state, and transition handles. The
  renderer owns only presentation, user intent, deliberate confirmation, and
  focus/announcement behavior.
- The canonical reference shows `No login` as the explicit default, separate
  Application and Project Settings destinations, provider status, behavior
  preview, recovery disclosure, and protection-reduction warning.

## Material acceptance-closure map

| Dimension | Observable outcome and invariant | Planned proof |
|---|---|---|
| Application vs Project Settings | A separately labeled Application Settings destination opens `Security & sign-in` for this application/current Windows account; Project Settings remains a separate project-scoped destination. | Renderer workspace unit/interaction tests assert both destinations, headings, scope text, and unchanged Project Settings behavior. |
| Current/default mode | The native snapshot is the only current-mode authority. The page labels `No login` as default and displays exact current mode, profile, timeout, configuration state, and policy revision without treating a draft as committed. | Strict model tests plus interaction tests with native snapshots for `none`, password, Hello, migrated, and invalid states. |
| Provider availability | Hello begins as checking and displays every typed availability class. Only native `available` permits selecting/enabling Hello; no state silently falls back. | Decoder tests and UI tests for checking, available, not-present, not-configured, policy-disabled, busy, unavailable, failed, and malformed/transport failure. |
| Preview | Startup, manual, inactivity, restart, recovery, and unchanged project protection update from the selected draft while clearly remaining a preview. Timeout choices are enabled only for protected modes; zero preserves manual/restart semantics and disables inactivity lock. | Pure preview-model tests for all modes and timeout boundaries; rendered labels checked against the governed reference. |
| Transition authority | Renderer sends only target mode, normalized optional profile, approved timeout, native transition handle, and deliberate confirmation. It accepts success only from a strict decoded `committed` result whose snapshot matches the target. | Mocked invoke-sequence tests plus existing decoder substitution tests; reject malformed, mismatched, stale, expired, conflict, and unrecognized results. |
| Two-phase state | A prepared protection-reducing transition shows warning/confirmation only after native proof. Cancel commits `confirmed: false`; confirm commits `true`. Non-reducing transitions may commit only after native preparation succeeds. A second transition cannot overlap. | Interaction tests assert exact call order/arguments, busy exclusion, warning timing, cancellation cleanup, and no displayed-policy mutation until committed native snapshot. |
| Strict result combinations | `sourceMode`, `targetMode`, `warningRequired`, outcome, and reason code must form a legal T03 combination. Protected-to-none and recovery can never suppress the post-proof warning; `sourceMode: null` means invalid-state recovery, never No login. | Strengthen decoder/model tests with substituted warning/source/target/reason fields and require fixed denial for every illegal combination. |
| Adverse or ambiguous outcome | Cancelled, denied, unavailable, busy, failed, conflict, expired, invoke rejection, or invalid payload never creates renderer authority. A prepare failure keeps the prior snapshot. A commit transport failure is ambiguous and must reconcile through native status before copy claims committed or unchanged; all paths announce fixed redacted text and restore focus. | Table-driven transition-result tests and focused DOM interactions for prepare/commit failures, response loss plus status reconciliation, and cancellation. |
| Recovery | Password recovery is explicit, invokes only the dedicated same-SID native recovery-prepare command, never appears as automatic fallback, and still requires warning plus deliberate commit confirmation. If proof fails, configuration stays locked/invalid. | Locked and settings interaction tests assert explicit action, command separation, confirmation order, denial/cancel handling, and unchanged snapshot. |
| Locked-surface secrecy | Locked/recovery UI contains no project identity, path, command query/result, research content, credential, PIN, or biometric. It names the configured provider and reports only bounded availability/recovery guidance. | Static locked-view denial assertions using deliberately sensitive project/profile/query fixtures, plus provider-specific primary-action/status tests. |
| Position preservation | Entering Application Settings records the current product workspace/project context. Cancel/return and every adverse result restore or retain the prior workspace and focus; the settings route is not a scholarly workflow step. Lock still clears sensitive position as required. | Renderer interaction tests enter from home, Project Settings, and an open-project context; return asserts prior workspace/project projection and focus. |
| Governed experience/accessibility | Radio-card semantics, default text, logical tab order, visible focus, live success/error announcements, no color-only state, warning dialog focus containment/restoration, reflow/responsive layout, light/dark themes, and reduced motion conform to v1.4. | Semantic renderer tests, focused keyboard/dialog tests, existing UI reference/conformance/accessibility checks, build output inspection, and stylesheet checks at responsive/reduced-motion boundaries. |
| Manual-lock compatibility | No-login mode must not display an actionable application Lock control because native locking is intentionally disabled there; protected modes keep manual lock. | Renderer state tests assert hidden/disabled lock for `none`, available lock for password/Hello, and unchanged native no-op defense. |
| Evidence truth | Narrow task checks prove renderer behavior, strict contract use, native command wiring, reference conformance, and accessibility. Complete packaging/performance/cross-capability qualification remains at W1 exit. | Criterion-linked task manifest records selected Vitest, TypeScript/lint/build, focused Rust/native integration where wiring changes, and affected UI/security checks; broader matrix explicitly deferred. |

## First tests before product code

1. Add pure Application Settings model tests for all modes, Hello availability
   classes, previews, exact transition messages, and strict no-fallback behavior.
2. Add rendered/interaction tests proving the separate route, preserved return
   position, exact prepare/commit sequences, post-proof warning, cancellation,
   failure focus restoration, and recovery-command separation.
3. Extend locked-view tests for provider-specific actions/status and sensitive
   data exclusion.
4. Add the narrowest real-boundary check available: a deterministic native
   JSON harness using the real `ApplicationLockManager`/`PolicyStore` and
   production serde shapes, consumed through the production TypeScript
   decoders/controller. Cover enable, protected switch, proof then confirm,
   `commit(false)`, unavailable-Hello explicit recovery, invalid-policy
   recovery, conflict/expiry, and simulated commit-response loss/status
   reconciliation. Follow with an assembled desktop build and governed UI
   conformance check; do not represent parallel source-string checks as this
   boundary proof.

## Deferred broader coverage

- The complete W1 Windows-x64 packaging, performance, accessibility,
  cross-capability, restart, recovery, and security matrix remains due once at
  W1 exit.
- A real configured Windows Hello success prompt remains hardware-conditional;
  this task must still prove every typed UI state and preserve the existing
  release-authoritative native probe evidence.

## Preflight and gate

- **Read-only adversarial preflight:** incorporated. It confirmed the product
  and v1.4 experience authority, added strict result-combination, ambiguous
  response-loss, no-login manual-lock, route-inventory, and real-boundary proof
  rows, and found no missing product/design decision.
- **Mandatory pre-implementation control gate:** the approved amendment task
  identity `W1.A05.T04` cannot currently be represented by the CAP-only UI
  evidence and backlog-schema path grammars, and the materialized task omitted
  its required `experience_change` projection. Resolve this before product UI
  with one generic bounded Tier-2 maintenance increment, exact v1.4 lineage,
  risk-selected tests, and independent control review. This does not expand
  approved authority or require new human design approval.
