# W2 reference publication — bounded adapter maintenance

Predecessor: `3addadbf686df1222cf89491a27503af7942a991` (owner decision and ADR
acceptance). This is authority-preserving pre-Wave maintenance, not a new task
state, controller, human gate, Wave approval or product implementation.

## Mismatch and bounded correction

The design-first workflow requires reference approval before the final Wave is
frozen. The existing validators recognized only in-Wave amendment publication
or an already-approved Wave/slice. The exact owner-approved 1.7 proposal therefore
had no truthful executable publication route. Independent control-design review
by `/root/w2_ux_plan_review` confirmed the gap and the narrow adapter approach.

One shared read-only validator authenticates the committed owner design record,
its introduction/hash, exact proposal commit/path/package, complete canonical
regular-file inventory, ancestry, unchanged authority history, supersession and
single use. `reference_package_at` and historical ECR resolution reuse it.
Only the six existing approval-metadata fields and resulting manifest hashes
may differ from the reviewed proposal. No arbitrary publication transformation
is accepted. Missing `deferred_surfaces` is allowed only for the new exact
authority shape and only if absent in its authenticated proposal; existing
approval schemas remain strict. The source proposal, old approvals, inherited
ADR-0026 catalog and W2 ledger/state remain unchanged. No visual baseline changes.

The owner record was committed before publication. All eleven mutable W2 slices
now bind 1.7; seven selections, 33 tasks, dependencies and 530 engineering-hours
(including the sole two-hour support allocation) are unchanged. Architecture
and reference acceptance do not approve the Wave or its tasks.

## Observed development checks and retained failures

- Initial reference integrity passed, but `planctl wave validate W2` failed
  on the dirty active-reference historical binding. Inspection also established
  the unsupported pre-Wave authority route; no failure was relabeled a pass.
- The new synthetic real-Git success regression initially failed because the
  existing strict schema lacked that route. The adapter makes that case pass.
- A multiparent fixture originally resolved an unchanged earlier publication,
  so it did not exercise its intended boundary. It now supplies the purported
  merge publication explicitly; the multiple-parent denial is asserted.
- An extra unlisted file initially escaped a check restricted to the original
  approval commit. The validator now verifies the exact requested revision's
  complete inventory/content as well as the original authority. This adverse
  observation and its regression are preserved, not waived.
- Non-authority proposal-status prose/implementation-rule substitutions from
  the first working draft were removed. Final publication preserves every
  nonmetadata proposal byte, including its historical proposal-status prose.
- The existing historical 1.6 approval-shape regression initially read the new
  active 1.7 record and failed. It now reads the actual immutable 1.6 publication
  commit; its exact authority-field assertion is unchanged. Other prior product
  fixtures and visual baselines are not repaired or relabeled by this increment.
- Independent draft review found two material historical-boundary gaps. A later
  repair could hide invalid content at the original publication; two sibling
  publications could consume one record before an ordinary merge. Both new
  regressions failed before remediation. The validator now authenticates the
  original publication as well as the requested revision and counts parsed
  record introductions across all reachable approval history. Inherited ordinary
  merges remain valid. All seven focused test methods passed in 25.892 seconds
  in development; committed qualification and independent closure follow.

Focused qualification: new real-Git positive/tamper regressions; existing strict
legacy, amendment and Wave/slice approval regressions; affected lint/type/ADR
checks; exact committed reference authority/integrity; complete W2 planning and
review-site validation; independent commit-bound control/packet review. Later
evidence records actual committed results. W1 product, desktop renderer, native,
performance and packaging suites are not replayed for this metadata/control delta;
W2 implementation retains their affected qualification obligations.

Rollback is a normal reviewed revert preserving this adverse evidence and the
owner record, not a history rewrite or forced baseline change. No protected
witness access, privacy-hook change, remote action or real-project access occurred.
