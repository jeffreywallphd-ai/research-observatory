# W1.A09.T02 acceptance closure

Claim base: `cc1d0407e6882b724e628caa136dfe4b9569410c`; owner `codex`.
Authority: the immutable ECR-0008 packet at
`2160cc9917282d80848189dc9669fe3827979246`, its A09 approval and the
five exact backlog criteria. T01 is independently DONE. ADR-0008/0009/0010,
ADR-0018 and ADR-0026 govern the unchanged process, authentication, generated
contract, protected-generation and semantic-history boundaries.

| Acceptance boundary | Invariant and selected proof |
|---|---|
| Catalog route | Only exact generated GET `/workflow-profiles/catalog`, absent body/ETag/idempotency key; wrong methods, query/trailing path, headers and unknown endpoints denied before I/O. Native tests first, then generated client through actual compiled supervisor/Core. |
| Lineage route | Exact six-field typed JSON with duplicate/unknown/missing field denial, bounded body bytes, canonical UUIDv7, ancestors/descendants, cursor0–10000/pageSize1–100/maxDepth1–16; bool/fraction/string coercion denied. Real retained nonempty lineage plus wrong-project/revision denial, not merely HTTP200. |
| Objective | Correct the generator to reuse bounded narrative with minimum1 and nonblank. Preserve LF/CR/TAB and the existing stricter TS/native UTF-16 4000 limit and invalid-control/NUL/DEL denial. Native also rejects whitespace-only creation. Characterization/failing TS/native tests precede fixes. OpenAPI and semantic catalog/schema literals remain unchanged. |
| Startup path | Historical resource probe proved safe Windows verbatim spelling rejected by canonical-string comparison. Compare canonical path to pinned `dunce::simplified` spelling only; preserve file type, exact basename, canonical and redirected-path denial. Plain/verbatim positives and wrong-file/noncanonical/symlink/junction negatives. Do not alter debug fallback policy or claim content-signature authentication. |
| Readiness/recovery | Reuse exact decoded native service status; status-only observation on Projects mount, no automatic restart of failed Core. Wait for authentic ready before the first catalog read; separate starting/loading/catalog failure/input validation/project-operation failure. Explicit catalog read retry or bounded Core start retry preserves input and never replays a project mutation. |
| In-flight state | Single admission for mutations; cancel catalog updates on service change/unmount and suppress late mutation delivery to replaced/locked forms. Preserve existing native protected-generation and process-cancellation checks; repeat affected lock tests. A supervisor harness alone is not proof of Tauri generation gating. |
| Principal boundary | Current generated client -> current-source compiled native async supervisor -> actual disposable supervised Core with isolated test vault/encrypted database; catalog, multiline create, open, retained intent/workflow, nonempty lineage, close/reopen/restart, not-ready/failure/explicit recovery and invalid request denials. No actual researcher/profile data or browser-stub substitution. |

Read-only adversarial preflight by `agent:/root/t01_publication_checkpoint`
confirmed this bounded design and added explicit duplicate-key/coercion/retained
relation negatives and the distinction between supervisor and Tauri proofs.
No new authority gate was found. The old rejecting diagnostic remains unchanged.

Existing parity residual: pure Python model validation accepts DEL and counts
Unicode code points, whereas TS/native already reject DEL and count UTF-16 units.
This task preserves the stricter existing desktop boundary and fixes LF/CR/TAB;
it must not claim complete accepted-input equivalence or silently change Core
models/OpenAPI scope. Record this limitation in evidence.

The two existing supervisor `nonminimal_bool` warnings will be corrected without
changing their logic as part of affected native checks. The two unchanged
`application_lock.rs` test-helper `needless_borrow` warnings remain T03/T04
follow-up; no warning suppression or erased failed T01 report.

Deferred: T03 owns the shared native folder control, dev/production default
parent preselection, dialog cancellation/lock/close and stable destination.
T04 owns accumulated real packaged/native qualification; returned A08 owns
sidebar/footer and effective-spacing corrections. Full repository/Windows W1
matrix remains at Wave exit. No token/reference/baseline changes, profile
migration, default-policy expansion or automatic project retry is authorized.

Safety: synthetic disposable fixtures only; no remote action, real profile
mutation or access to the protected unrelated `W1.A04.B00.json` witness.

First adverse-to-passing observations: new TS multiline test failed before
transport, all three new route/nonblank native tests failed, and the new actual
Windows path test rejected the equivalent verbatim form. After the narrow
corrections, all17 supervisor tests and43 selected contract/workspace tests pass.
The new browser test first timed out because Projects never observed native
status; after reuse of the status boundary it passes readiness, separate failed
catalog/retry, preserved multiline input, no automatic mutation replay and
late-result suppression on navigation. These are not full native/UI closure.

The first actual-native proof drafts had invalid harness assumptions: lineage
pages have no projectId field, and Intent setting revisions are not canonical
aggregate lineage nodes. Retain those nonpassing observations; the corrected
fixture appends two synthetic canonical evidence revisions via the real unit of
work while the encrypted project is closed. No product response/schema was
changed to satisfy the harness. The real lineage contains six nodes including
the retained predecessor. Missing and wrong-project revisions correctly return
empty integrity-review/export-denied projections, not HTTP errors. The first
corrected real-native run passed catalog, multiline creation, exact intent and
selection reopen/restart, lineage and substituted-request denials in14.062s.
Final commit-bound replay and expanded path/lock checks remain due.

Additional retained observations: the new external packaged-path test initially
used the wrong synthetic executable basename and correctly received integrity
denial. After matching the unchanged published target-triple basename, actual
plain/verbatim acceptance and NTFS-junction redirect denial pass. Windows denied
the test's symbolic-link creation privilege (1314), so that optional case is
explicitly unavailable, not a passing link test. No elevated creation or actual
fixture executable launch was attempted; all new external fixtures are retained.

The first expanded native library run passed63/failed1/ignored1. The unrelated
legacy migration fixture expected Migrated but loaded Valid. Its test root uses
only PID plus per-process sequence; filename metadata demonstrated same-PID
fixture policy files already created on2026-08-30. Production policy code and
test assertions are unchanged. Replay in a fresh child-process temporary
namespace is required, retaining this failure and the preexisting fixture-root
collision weakness for T04 qualification. Do not erase old temporary data or
describe a rerun as evidence that the ordinary test isolation defect is fixed.

## Bounded verification correction at candidate 2327ab37

The commit-bound outer verification-01 retains all12 selected results and exact
pre/post source hashes. Eleven checks passed, including64 native library tests
(one separate renderer witness intentionally ignored), production Clippy,
generated/renderer tests, types and actual protected Core. The six-test
browser group passed5/failed1: the full existing product-frame assertion reported
exactly six Projects neutral-theme failures. The separate product-frame-01
report retains all measurements; no other frame assertion failed.

Predecessor `tools/desktop_app_check.py` Git blob at2327ab37 has SHA-256
`59ffb27295eed4cb2be3430e27566b88b8d98884e71f7742efe7ee5ed8ec0ab4`.
Its measurement picked the first panel/card, while the unchanged validator
requires a neutral `--surface-1` background. Projects now correctly reuses the
existing success-tone service panel before Create's neutral panel, and shared
CSS correctly applies `--success-soft`. The bounded generic correction measures
the first visible neutral/untoned panel/card, including the surface itself; no
color-based selection, tonal fallback, manufactured token value, Projects ID or
title-specific selector is permitted. Missing neutral surfaces still fail.
Focused browser negatives preserve wrong-background denial in both themes,
hidden/missing denial and surface-self eligibility. All existing geometry,
focus, motion, theme assertions and adverse reports remain unchanged.

This is acceptance-linked evidence maintenance within the frozen cross-proof
path inventory, not new product/design authority. Read-only independent
assessment by `agent:/root/t01_publication_checkpoint` confirmed this boundary;
commit-bound independent disposition is still required before integration.

The outer verifier already bound the complete build-to-run source interval.
The standalone native test additionally now hashes native inputs before/after
Cargo and passes that exact map to the generated-client runner, which rechecks
it before launch and at completion. Missing/substituted bindings fail before
creating any project fixture. No principal boundary is replaced by a stub.

Ancillary type observations are retained: the first manual generator mypy call
lacked the Core module path; adding it exposed existing unrelated
`selective_recalculation.py:834` unused-ignore/attr-defined diagnostics. The
affected generator passes with Core module discovery and imported diagnostics
silent, retaining imported type analysis. This is scoped generator typing, not
a passing claim for all Core source. Full existing Core typing remains a W1
qualification obligation; no source ignore or assertion was weakened.
