{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Offline desktop window using only application-defined typed Core-supervision commands.",
  "windows": ["main"],
  "permissions": []
}
