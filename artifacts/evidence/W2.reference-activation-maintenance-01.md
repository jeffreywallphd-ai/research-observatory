# W2 approved-reference activation maintenance

Predecessor: `a8955b86f75d628f2e23c7eac336de66c332b098`.
Route: bounded evidence-control maintenance under AGENTS.md and
`docs/automation/workflow-efficiency.md`; no product/reference approval change.

The active reference is the approved 1.7 package published at
`14b89c07a435a26bf894242a9e5f3e6429065055`, package SHA-256
`dcf31147ee386d48e8ac95725d648ee2b08c74ef0dd42c139c5f78508a90f878`.
Activation and both build-manifest producers still bind 1.6. The existing
presentation verifier also admits only amendment publication authority and
root metadata mapping; it cannot represent the approved appended Source Manager
region. These are control defects, not authority to revise the published design.

This first increment adds a separate exact-package 1.7 witness, authenticates
the already supported pre-Wave design authority, and permits only explicit new
regions appended to existing page contracts. Byte/order comparison remains
strict for every other field, including workflow semantics. The legacy 1.6
Git-bound adversarial fixture uses its exact accepted witness commit instead of
the later active reference. A new parallel fixture exercises design publication.

No immutable approval, old witness, semantic source/generator/catalog, or
baseline is changed in this increment. No task or campaign transition occurs.
Consumer activation, builds, fresh baseline publication and conformance remain
pending independent control review. No supplemental refactoring is included.

The initial new mapping test failed because the prior verifier had no way to
declare approved required-region additions. Selected regressions cover exact
1.6 metadata mapping, 1.7 region addition, undeclared/duplicate/moved additions,
workflow ordering and duplicate fields. Real Git tests cover missing/uncommitted,
staged/substituted/racing inputs, stale packages and forged publication for both
authority forms. Required checks bind the committed control candidate; broad
product/native/provider tests are outside this verifier-only increment.
