# Windows packaging

Owner: Research Observatory maintainers
Boundary: Windows installer inputs, update-channel metadata, and lab deployment policy.

This directory stores source configuration only. Generated installers, signing
keys, signatures, and packaged binaries must never be committed.
