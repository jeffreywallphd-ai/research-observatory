# Decision feedback archive

`planctl apply-feedback` stores immutable exported review responses here by capability. The archived JSON preserves reviewer notes and detailed rationale, including Other choices. Canonical selected options are materialized into the capability plan; archived feedback is evidence of why the choice changed.

The current export schema is `../decision-feedback.schema.json` version 1.1.
