# W1 current-candidate packaged-Core performance evidence maintenance

Status: implemented development candidate; independent commit-bound review and
fresh committed verification remain required. This note is not package approval,
a benchmark result, Wave acceptance, a signing decision, or a release decision.

## Authority and bounded scope

Predecessor: `c0bb3a6b5e7e21531b0634fd1c3030596fd10717`.
AGENTS bounded automation/evidence-control maintenance applies: the existing
qualifier conflated immutable historical comparison identities with current
execution identities. An authenticated clean current tool failed the historical
tool equality check before execution; proposal mode cannot qualify a candidate.
Restoring qualification of independently reviewed current artifacts does not
authorize a new product scope, threshold, controller, amendment, or refactor
allowance. Changes are limited to `tools/core_sidecar_performance_check.py`,
`tests/desktop/test_core_sidecar_performance_check.py`, and this note.

The immutable `verification/baselines/core-sidecar-performance.json` remains
byte-identical, SHA-256
`599ca300100627ae517ebd60cfdca2a4b3045dec9f7e2d2c6e22aa76b17471f8`.
Its `.sha256` authority file remains byte-identical, SHA-256
`f48ace224750cb243923245c61eb9f398660becef7b634dbab645622bcf19680`.
The baseline source commit, historical tool/package provenance, historical
measurements, hardware, and method were not rewritten or reinterpreted.

## Restored control boundary

The optional paired `--current-package-evidence` and `--current-package-review`
arguments are canonical committed evidence locators, not caller-selected hash,
artifact, method, or threshold overrides. Strict evidence binds the current
build candidate, executing tool, complete Git-derived build/execution input
inventory, unchanged comparison baseline/method, fixed artifact root/report,
manifest/inventory, entrypoint, build contract, target/version, count and bytes.
Input rows retain separate raw and committed-blob digests. Only UTF-8 text
checkout line-ending equivalence may differ; binary or substantive substitutions
are denied, and the executing tool still requires exact committed bytes.

A separate APPROVED review must name a distinct reviewer, have no blocking
findings, and bind the exact evidence bytes and build candidate. Both records
must be unchanged, committed, append-only, and chronologically descended from
the build candidate. Subsequent measurement commits may change only evidence
and named planning projections. Every intervening commit is checked, preventing
source add/revert escapes. Actual independent repository review supplies this
authority; the qualifier neither creates nor self-approves it.

Current authority is captured, locked with all admitted source inputs, reread
under lock, and reauthenticated through final publication. Existing full package
verification, immutable package ACL/handle snapshots, strict real-principal
handshake/lifecycle checks, interruption tombstone, and guarded atomic PASS
publication remain in place. The report separates current approval references
and execution provenance from the immutable comparison baseline reference.
Omitting the new pair preserves legacy historical-identity denials. Partial
pairs or pairing with proposal/measure-only are nonqualifying and invalidate a
stale PASS without measurement.

The unchanged method remains one unmeasured warmup followed by exactly seven
retained lifecycle samples; hardware must match. The readiness median 3000 ms,
shutdown p95 5000 ms, idle working-set p95 268435456 bytes, and existing 20-percent
nonregression bounds remain enforced without rounded-boundary admission.

## Development observations and selected proof

Before tool edits, clean-predecessor characterization authenticated the baseline
and current tool, then failed with the historical-tool mismatch (Python exit 1).
Raw log: `artifacts/tmp/W1.core-performance-current-candidate.characterization-red-01.log`,
SHA-256 `ac79af4cefe05dedf1bdcc421f42c681a477ccf24a15a3764b75babb452a0b43`.
The newly added approval tests were run before implementation: five
missing-interface errors, retained at
`artifacts/tmp/W1.core-performance-current-candidate.tests-red-01.log`, SHA-256
`49862526acd7db178155a185c83b91c17cfb801570383e9a45f3c1eea9022c8c`.

Development command:
`.venv/Scripts/python.exe -B -X utf8 -m unittest tests.desktop.test_core_sidecar_performance_check -v`.
Successive retained runs passed 17, 21, and finally 23 tests. Final run:
30.252 seconds, no skips; log
`artifacts/tmp/W1.core-performance-current-candidate.development-03.log`, SHA-256
`703151d8ed59fb2f7d9e36e07d4d9e514ac03a451f7a65f39851bebfd5fa823c`.
Earlier logs remain `development-01.log` and `development-02.log` under the same
prefix. Real temporary Git cases cover positive metadata descendants, omitted
and substituted input rows, missing/uncommitted/modified authority, nonapproved
or self-reviewed records, wrong candidate/digest, source add/revert and record
rewrite, unsafe references, and under-lock/late substitution. Synthetic
measurement callbacks prove routing and retained sample counts, not performance.
The existing owned temporary-package Windows ACL denial test remains real.

The unchanged `quality_check.execute_quality` command construction was applied
to the two affected Python files: format, lint and configured no-namespace mypy
all PASS. Machine report:
`artifacts/tmp/W1.core-performance-current-candidate.quality-01.json`, SHA-256
`dcd3c13e74a101aec6ec4de82eed8e291f5f1b0c9c1f85b526d383650f20cd1b`.
Its log SHA-256 is
`4072c69f976e06e881237eeaf8fcec3fea596becbfc56908a8496ead7618e03b`.
Development also observed temporary Windows mapped-file formatter refusals,
one missing fixture annotation and scoped layout/lint findings. Formatting was
retried after the temporary handle condition cleared, the annotation/layout
were corrected, and the final unchanged quality commands passed. No scanner,
hook, OS setting, or check policy was changed to resolve them.

## Deferred qualification and limits

No real package lifecycle, build, benchmark, signing, ordinary-profile access,
native UI, or broad profile was run for this control repair. Those boundaries
require the separately authorized final-candidate qualification and authentic
current package evidence plus independent review. Unit fixtures are explicitly
synthetic and do not establish installed dependency identity, real package
provenance, resource results, principal behavior, or W1/G1 acceptance.
Parent-owned disjoint native verification-fixture maintenance is not reviewed
or approved by this note. Protected witness content and the excluded sibling
checkout remain outside scope; no artifact-directory inventory was performed.
