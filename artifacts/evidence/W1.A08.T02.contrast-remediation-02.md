# W1.A08.T02 accent text contrast remediation

Predecessor: `68a53134713a083ef18760899bf6232e6d2b31da`.
Product predecessor: `497cca81efff033449a9b794af76d60f2f13e0d7`.
The original claim base and approved presentation 1.6 remain unchanged.
This supplements the acceptance closure for independent visual-review-05
finding W1.A08.T02-visual-05-F01; prior reviews and failures are preserved.

## Failure and correction

The actual Application Settings eyebrow inherited brand-700 on canvas in dark
mode, measuring 3.975814:1. The keyboard-visible Skip to project home link
inherited inverse text on brand-700, measuring 3.936220:1. Both are normal text
requiring 4.5:1 under approved STYLE_GUIDE section 9 and task criteria 3/4.
The reference's illustrative accent use cannot waive its normative AA contract.

Before the product correction, the actual built-product test failed on both
dark consumers, with each visible and the skip link genuinely keyboard-focused.
The light cases passed. The unchanged existing Task Center and directory-picker
adapters provided the isolated local fixture; Local profile opened the actual
Application Settings page. No product markup or user data was substituted.

An earlier test attempt sampled immediately after Shift+Tab and saw an
unfinished transform transition, including with reduced motion. The corrected
sampler waits for three stable animation-free geometry observations, bounded
by 60 frames, without a wall-clock sleep. This made both actual contrast
failures reproducible (one test, two failing dark subcases, 2.351 seconds).
Independent preflight then identified a sampler weakness, not a new product
defect: containment alone could admit a hidden zero-size node. The final test
requires nonempty rectangles, visible/displayed ancestors and effective opacity
of one. It covers the actual default 1280x720 viewport in both themes; the
broader capture matrix supplies separate viewport coverage.

The product correction changes only the skip-link background to existing
brand-600 and adds a low-specificity dark eyebrow rule using existing brand-400.
No token, reference, label, target, action, focus order, geometry, disabled
predicate, domain state, security policy or native behavior changes.

## Risk-selected proof

The first rebuilt correction passed all eight layout/contrast tests in 15.493
seconds, including previous sidebar, panel-flow, current-navigation and shared
context-action regressions. Static style analysis passed 283 rules with the ten
existing exceptions unchanged. The pinned production build assembled four
functional product artifacts and 42 separate reference artifacts. Desktop lint
and type checking passed. Python lint initially found two overlong lines in the
new test; wrapping them preserves behavior. The final visibility-enhanced
eight-test replay passed in 15.260 seconds, with Python lint and types passing.

This note is not independent closure or task approval. Fresh exact-producer
captures, supplemental visible keyboard evidence, unchanged-budget performance,
independent visual disposition, restoration classification, formal task review,
slice review and amendment exit remain separate obligations. Complete repository
and Windows release qualification remains W1 exit work. No ordinary account
projects, policy, vault, native window or credentials are accessed.
