# Immutable interruption-note compatibility

Predecessor: `7ef91f94a4213972960e9321142c0e7138e90984`. Risk tier2 under
AGENTS bounded maintenance; no task, authority or release transition.

`W2.C01.T01` submission rejected the preserved same-Wave resume note even though
its product correction and full performance qualification passed. Full-history
checking correctly prevents hiding the note by renaming/reverting. Independent
preflight found no existing lawful delivery route for that note. Do not relabel
its old commit as maintenance, rewrite history or rerun unchanged product checks.

Admit only same-Wave `WN.resume-YYYYMMDD-NN.md` interruption notes in the existing
evidence directory, through Git-aware validation. Require a valid date, one
immutable addition, a sole parent and a100644 blob. Reject rewritten/deleted,
symlink/executable, wrong-Wave, malformed/nested/traversal paths and mixed or
reverted unauthorized history. These notes carry no scope, approval, disposition,
criteria or execution authority. Existing exact product-path and independently
reviewed maintenance-chain requirements remain unchanged. No new controller,
manifest state, gate, schema or product behavior.

Selected proof: first reproduce the rejected checkpoint through the public UI
gate; then real temporary Git fixtures exercise that gate and task-evidence
validation, immutable-note and unauthorized-history denials. Replay affected
linked-correction authority/scope tests plus lint/format/types. Independent
control review must bind this exact delta before correction submission resumes.
The initial regression failed as expected:1case,1.799s, exact scope error;
`artifacts/tmp/W2.C01.T01.checkpoint-scope-red-01.log` is retained.

This is bounded compatibility-defect restoration, not structural cleanup or
optional technical-debt refactoring; no supplemental refactoring is included.
All original task/approval/adverse records and prior benchmark bytes remain.
Rollback is an additive revert of this control increment; that restores the
submission hold without changing product data or historical evidence. Product
benchmark/source/runtime inputs are unchanged; its7ef91f94 execution remains
historical evidence at that candidate, never relabeled fresh at a later commit.
