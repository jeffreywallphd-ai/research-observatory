---
document_type: governance-recovery-supplement
record_schema_version: 1.0-recovery-supplement-proposal
recovery_request_id: GRR-0001
supplement_id: GRR-0001.S01
title: Repair consecutive-amendment materialization under the active recovery hold
status: pending-human-approval
execution_state: non-executable
classification: approved-bootstrap-latent-control-defect
target_wave: W1
prepared_by: codex
requested_approver: repository-owner
---

# GRR-0001.S01 — Repair consecutive-amendment materialization

> **Pending exact-commit human approval; no execution authority.** This inert supplement records new evidence found only after W1.A03.B00 was independently approved. It does not authorize a controller edit, backlog mutation, amendment materialization, task claim, T03 recovery, hold release, W1 resume, G1 approval, merge, or remote push.

## 1. Decision requested

Approve one append-only supplemental recovery bootstrap, `GRR-0001.B01`, under the existing `HOLD-W1-GRR-0001`. B01 may install the minimum representation and review transition for a post-approval recovery-bootstrap defect, correct the consecutive-amendment semantic invariant, and add focused adversarial tests. It must then receive independent control/security approval before the unchanged W1.A03 materialization command is retried.

The supplement does not rewrite `GRR-0001`, its approval, B00 attempts R01-R05, ECR-0002, W1.A03, W1.A01, W1.A02, the W1 approval, or any prior Git object. It adds ordered authority after the approved B00 because the installed lane cannot lawfully reopen an approved bootstrap and cannot create a second active recovery hold.

The exact target authority resumed after B01 is also frozen: ECR-0002 packet commit `ed0bf716f8586e078c6fe3b8ac7e2885a6eb98c4` at SHA-256 `835d0cbac43427a1cfd7406a8693b8af0ebf9321d009778c359165de9f7a3b8b`; W1.A03 approval introduced at `95c007b58619dfd82b5f443eea3d8e71e7efe082` at SHA-256 `340107c778bbf2c5ad77bc9150272215b87ea714a575fa035c42886c1f21d2bd`; and W1.A03.B00 candidate `d0faa070a85055fa2a278e05bedc57c05701aabe`, evidence SHA-256 `83e7295355f6a3377247b4eb0fca8d852dd670d968856f6212c110f60dd64a36`, frozen reviewed state `6dbe0f6ba6996818f60dc0d27ef4f6e56aa5ff8f`, and approved projection `12c06ed648d1153f6ed60e873224fa0637fea4f7`.

## 2. Trigger and atomic stopped state

At clean commit `12c06ed648d1153f6ed60e873224fa0637fea4f7`, W1.A03 is `APPROVED`, its B00 is independently `APPROVED`, it has zero materialized tasks, W1 is `PAUSED` with ordinary `wave` scope, `CAP-02.S04.T03` is `BLOCKED`, and `HOLD-W1-GRR-0001` is `ACTIVE`.

The next governed command was:

```powershell
.venv\Scripts\python.exe tools\taskctl.py --file planning\backlog.yaml amendment materialize W1.A03 --agent codex
```

It failed atomically with:

```text
Refusing to save invalid backlog state:
- W1.A02: terminal lifecycle did not restore ordinary Wave scope
```

The backlog remained byte-identical at SHA-256 `2a14a5b45158d035514ed9a5ff4f170dc8a508df941f1b5395a2121a7713ef1e`.

The defect is in the shared live-scope invariant: materializing W1.A03 correctly changes W1 to `amendment-hold`, but validation then treats that current scope as proof that already-adopted W1.A02 never restored ordinary scope. The current focused materialization test models only one amendment. The hostile third-amendment fixture proves append and bootstrap submission but stops before materialization.

## 3. Why current authority is insufficient

- `ECR-0002 / W1.A03.B00` authorizes only `tools/ui_change_gate.py` and `tests/foundation/test_ui_change_gate.py`, and B00 is already approved.
- `W1.A03.T01` authorizes controller work only after materialization, activation, and claim; doing that work early would bypass the failed gate.
- `GRR-0001.B00` included control paths but is already immutably approved. `recoveryctl bootstrap-resubmit` accepts only `CHANGES_REQUESTED` or `BLOCKED`, so that approval is not continuing mutation authority.
- The W1.A02 B00 scope addendum is constant-bound to W1.A02 and a generated document; it is neither reusable authority nor a post-approval remediation transition.
- A new ordinary ECR cannot precede unresolved W1.A03, while a second active GRR hold is rejected and the current hold cannot be released before W1.A03 adoption.

Direct edits, old-controller replay, manual backlog mutation, repeated W1 approval, or treating the earlier chat approval as broader authority are prohibited.

## 4. Selected design: GRR-0001.B01

The selected route adds one supplemental-bootstrap identity under the existing immutable recovery request and hold. The implementation must:

1. represent a hash-bound supplemental packet, exact human approval, bounded B01 evidence, append-only review attempts, and terminal B01 disposition without altering B00 history;
2. keep the active hold, W1 pause, W1.A03 approved/unmaterialized boundary, and T03 block continuously enforced;
3. correct the taskctl invariant so an adopted predecessor remains valid while a strictly later same-Wave amendment legitimately owns `amendment-hold`;
4. continue requiring ordinary `wave` scope when no later executable same-Wave amendment owns the hold;
5. reject gapped, reordered, cross-Wave, non-adopted-predecessor, multiple-executable-amendment, stale, dirty, forged, wrong-hash, or self-reviewed states atomically;
6. prove exact W1.A03 materialization creates only packet-bound `W1.A03.T01`, leaves W1 paused, T03 blocked, and GRR-0001 active, and grants no activation or claim; and
7. require an independent control/security approval of B01 before retrying materialization.

The authorized implementation surface is limited to:

- `AGENTS.md`;
- `docs/automation/codex-tracking-guide.md`;
- `docs/automation/project-automation-guide.md`;
- `docs/governance/delivery-control-model.md`;
- `planning/README.md`;
- `planning/backlog.schema.json` and the exact taskctl/recoveryctl state transition in `planning/backlog.yaml`;
- `planning/governance-recovery-approvals/GRR-0001.B01*` and `planning/governance-recovery-approvals/GRR-0001.S01.json`;
- `planning/governance-recovery-requests/README.md`;
- versioned supplemental approval/evidence/review schemas under `planning/governance-recovery-requests/`;
- mechanically generated `planning/review-site/**`, `planning/status-summary.md`, and `docs/planning-implementation-plan.md`;
- `tools/recoveryctl.py`, `tools/taskctl.py`, and directly affected `tests/foundation/**`; and
- `tools/backlog_views.py`, `tools/plan_review_check.py`, and `tools/plan_review_site.py` only where the new governed state must be rendered or validated.

The two operational-guide changes may document only the exact approved supplemental-bootstrap route and its fail-closed stop conditions. No product/runtime, UI gate, approved reference, task acceptance criterion, verification threshold, completed outcome, Wave approval, release gate, or prior object may change.

## 5. Verification and independent review

The B01 evidence must map every packet criterion and list the exact approval-to-candidate diff. Risk-selected checks must include:

- focused taskctl materialization and semantic-validation tests for legitimate A03 ownership plus every denial listed above;
- recoveryctl history, supplemental-authority, independence, hash/path, stale/dirty, and atomic-no-write tests;
- canonical backlog/schema validation at the candidate, frozen submission, and reviewed state;
- exact W1.A03 authority and B00 history validation;
- quality, generated-view, review-site, and Git diff-hygiene checks affected by the changed paths; and
- a read-only replay showing the unchanged W1.A03 materialization transition validates, without persisting it before B01 approval.

An independent reviewer must bind the exact candidate, frozen submission, evidence hash, packet hash, findings, and closures. B01 may be approved only with no open blocking finding.

## 6. Exit boundary

After B01 approval, retry only:

```powershell
.venv\Scripts\python.exe tools\taskctl.py --file planning\backlog.yaml amendment materialize W1.A03 --agent codex
```

Successful materialization may create only `W1.A03.T01` and `amendment-hold`; it may not activate the amendment or claim the task. Continue afterward through the unchanged W1.A03 activation, claim, evidence, independent task review, amendment-exit review, adoption checkpoint, GRR release, explicit W1 resume, and controlled T03 recovery gates.

## 7. Alternatives

1. **Approve B01 (recommended).** Repairs the minimum demonstrated invariant and adds a reusable, reviewed post-approval recovery-bootstrap remediation path without weakening the hold.
2. **Keep the repository paused.** Safe and requires no mutation, but W1 cannot progress and W1.A03 remains unmaterialized.
3. **Design GRR-0002 with hold supersession.** Possible but broader: it must represent transfer between active holds and a new predecessor model solely to fix one invariant.
4. **Edit taskctl or backlog directly.** Rejected because it bypasses the approved bootstrap/task boundaries and defeats the recovery controls.

## 8. Rollback

- Before approval, reject or retain this inert supplement; the current clean stopped state remains authoritative.
- An adverse B01 review must remain append-only and use a strict-descendant remediation submission.
- Before B01 approval, do not retry W1.A03 materialization against canonical backlog state.
- If B01 cannot preserve the exact base authority, B00 history, active hold, W1 pause, W1.A03 boundary, and T03 block, stop without mutation.
- Never delete, replace, or rewrite a GRR, supplement, approval, B00/B01 attempt, ECR, amendment, task review, evidence record, checkpoint, or Git object.

## 9. Independent packet-review history

`R01` reviewed exact packet commit `92de47c422a75a8060ef86b50564fe92f3da2217` and returned `CHANGES_REQUESTED`. It verified the exact four-file inert diff, structural schema validity, declared hashes, GRR/B00 authority, trigger reproduction and atomicity, stopped state, backlog/GRR/ECR validation, generated views, 148-page review site, and diff hygiene. It opened two blocking findings: the target ECR-0002/W1.A03/B00 authority was described but not hash-bound, and the proposed B01 scope omitted two operational guides whose current text permits only B00. This strict-descendant remediation adds the exact target authority object to schema, packet, proposal, and review page; adds only `planning/governance-recovery-requests/README.md` and `docs/automation/codex-tracking-guide.md` to B01 scope; and preserves the adverse R01 ledger. No executable or backlog byte changes.

## 10. Exact approval condition

Approval is legally available only after this proposal, schema, packet, and human review page are committed at one clean immutable commit; all declared hashes and authority references validate; and an independent control/security packet reviewer approves that exact commit.

Required human statement:

> Approve GRR-0001.S01 at packet commit `<40-character SHA>`, authorizing only supplemental bootstrap GRR-0001.B01 and its hash-bound governance/controller/test scope under HOLD-W1-GRR-0001. Keep W1 paused, W1.A03 unmaterialized, and CAP-02.S04.T03 blocked through independent B01 approval; then retry only the packet-bound W1.A03 materialization transition.

That statement authorizes creation of one immutable supplemental approval record. It does not itself implement B01, change the backlog, materialize or activate W1.A03, claim T01, recover T03, release the hold, resume W1, approve G1, merge to main, or push remotely.
