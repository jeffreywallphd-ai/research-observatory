# GCR-0006 — Install revision-11 reader headroom for a separately governed S03/B03

## Status

Pending independent packet review and exact-commit human approval. This proposal is inert and grants no execution authority.

## Demonstrated post-B02 mismatch

`GRR-0002.B02` is independently approved at exact commit
`b6c0366ef165a4ebeb3c10042d5ecc5379833a8c`. The first lawful attempt to
submit the already approved historic `W1.A04.B00` candidate failed atomically.
The installed recovery reader classifies the newly constructed `W1.A04`
amendment as fabricated under both `GRR-0002.S01` and `GRR-0002.S02` because
the supplement authority check always applies its pre-append absence rule,
including during the authorized post-append save.

The failure left the canonical backlog, active hold, paused Wave, blocked task,
pending gate, absent amendment, and trigger witness unchanged. The defect is
blocking nevertheless: the independently approved B02 scope cannot be repaired
or extended in place, and the current revision-11 reader and backlog schema have
no remaining version headroom for a sequential supplement.

## Bounded prerequisite

`GCR-0006.B00` is a one-time reader-generation bootstrap under the existing
`HOLD-W1-GRR-0002`. It may add support for control ceiling 12 and the exact
future version-5 `GRR-0002.S03/GRR-0002.B03` formats while live control and
minimum-tool revisions remain 11.

After independent B00 approval, adoption appends one exact, generation-neutral
`GCR-0006` record with transition 11→11, `generation_neutral: true`, and
supported ceiling 12. It does not append a supplement or amendment. Existing
revision-11 readers must reject that fourth-generation state; the new reader
must accept only the authenticated pre-adoption or adopted state. A later,
separately reviewed and exactly approved S03/B03 alone may move 11→12 and repair
the post-append supplement-authority phase distinction.

## Controlled lifecycle

After an independent APPROVED packet review, the repository owner may
exclusively create `GCR-0006.approval.json` for the exact reviewed packet
commit. Its tracked-clean introduction commit on
`codex/w1-windows-local-runtime`, with only the exact trigger witness untracked
and unstaged, is the sole B00 implementation base.

The future `gcr6ctl` lifecycle must freeze strict candidate, evidence, state,
review, approval projection, adoption evidence, and finalization commits. Every
attempt, finding, closure, and disposition is append-only. Self-review, open
blocking findings, stale or forked authority, wrong branch or head, altered
predecessor history, unsupported generation topology, and witness deviation
must fail without mutation.

## Interruption-safe adoption

Adoption uses an exclusive lock, write-ahead manifest, and exact backlog/state
successor pair. It binds raw and canonical predecessor bytes, the independently
approved B00 projection, active hold, revision-11 stopped boundary, branch,
actor, exact witness, neutral 11→11 generation, ceiling 12, and final two-path
Git boundary.

Publication must use replace-existing/write-through semantics on Windows,
flush successor files, manifest, published files, and parent directories, and
validate the exact pair before cleanup. Recovery is idempotent and may yield
only the exact approved predecessor or exact adopted successor. Ordinary
controllers fail closed while transaction artifacts or a split pair exist.

## Required end-to-end proof

The implementation and independent B00 review must prove:

1. the exact C→E→S→L→P→A→F Git topology and bounded path deltas;
2. old and new readers accept the unchanged three-generation revision-11 state before adoption;
3. only the new reader accepts the authenticated four-generation neutral adoption;
4. revision 12 is rejected without a separately approved exact S03 11→12 authority;
5. version-1 through version-4 supplement behavior remains unchanged;
6. the exact historic W1.A04 submission still fails after adoption with the same two pre-append diagnostics and no mutation; and
7. crash, replay, redirection, substitution, dirty-workspace, stale, forked, adverse-review, wrong-hold, wrong-generation, and wrong-witness cases fail closed.

## Safety boundary

GCR-0006 does not fix the post-append validator. It grants no S03 packet or
approval, S03 start, B03 action, B02 reopening, W1.A04 append or bootstrap,
amendment materialization, task mutation, T03 recovery, hold release, W1
resume, G1 action, product or experience change, local-main integration, remote
push, rollback, generic same-revision transition, or bypass.

The witness `artifacts/evidence/W1.A04.B00.json` remains exact at SHA-256
`4a9d944ff95972b449b617bc384306c7023e79d31d6b427e6b6f4678cd58b22c`,
untracked, unstaged, non-authoritative, and unavailable as submitted evidence.

## Alternatives

- **A — Approve bounded GCR-0006 (recommended):** install reader headroom at revision 11, then govern S03/B03 separately.
- **B — Keep W1 stopped (safe):** preserve the current fail-closed state indefinitely.
- **C — Reopen B02 or patch taskctl directly (rejected):** this would change immutable approved authority or execute outside it.
- **D — Reuse v4, install 11→11 as a supplement, or jump directly to 12 (rejected):** each breaks sequential authority or older-reader denial.
- **E — Append W1.A04 manually (rejected):** this bypasses the active recovery hold and commit-bound controller checks.

## Recommendation

Independently review this inert packet. If it is approved, request exact-commit
human approval for only `GCR-0006.B00`. Do not prepare or approve S03 until
GCR-0006 is independently approved and durably adopted. If any exact authority,
generation, ceiling, older-reader, transaction, stopped-boundary, or witness
criterion fails, retain the current revision-11 stopped state without mutation.
