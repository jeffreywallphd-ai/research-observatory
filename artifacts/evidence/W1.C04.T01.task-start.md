# W1.C04.T01 — truthful status-failure recovery

Claim base: `7c48c6d9f64459b849519829d91b3f9290addef6`.
Authority: original DONE W1.A05.T04 and its frozen linked-correction spec;
ADR-0018; application-lock architecture; approved Academic Minimal 1.6 lineage
and its application-wide sign-in contract. No new scope, refactoring or approval.

## Acceptance closure

| Material boundary | Proof selected |
|---|---|
| Criteria 1-2: separate settings, truthful mode/provider and recovery | Existing settings unit/interaction tests; new locked-view unit and built-renderer cases for all modes and unknown initial status. |
| Criteria 3-4: no fabricated authority, policy change or sensitive content | First-failure mode captured only from accepted native evidence; cleared tree; no No-login/unknown authentication action; genuine invalid-policy recovery retained; existing proof/confirmation and denial tests. |
| ADR-0018 latch and ordering | Failure → later status/event stays locked; failure → accepted native locked-invalid → later unlocked still stays locked; explicit-unlock proof remains the only latch-clearing transition. |
| Criterion 5: experience/accessibility | Existing shared card/control styles; both themes, narrow built renderer, focus and announcements; affected UI/reference checks. No new visual baseline. |
| Real principal boundary | Fresh fixture-isolated desktop menu and restart replay. Bounded tracing distinguishes status admission/body/settlement; process exit alone does not prove UI correctness. |

Independent read-only preflight confirmed the restorative origin and identified
the intervening-locked-status latch regression. Added tests before product edits.
Seven new unit assertions failed against the predecessor. Built-renderer tests
also reproduced false manual/provider statements and false corruption recovery;
the initial manifest mismatch and ambiguous selector were test setup failures,
not product evidence. The unmodified native failure and cancelled full attempt
remain preserved separately.

Diagnostic A at `3821830f26df2332711af51b7e765828f7cc6f04` reproduced the
native menu stall while confirming truthful No-login recovery. The committed
native-A note preserves its adverse UI result and successful process cleanup.
A source-dispatch guard failed before changing only the status command's
dispatch attribute to asynchronous; that guard is not native liveness proof.
The B replay must demonstrate menu dismissal and continuing renderer/native
responsiveness, with unchanged policy authority, timeout and failure latch.

Independent review also identified protected-mode monitoring failure while the
native session is still unlocked: native correctly denies reauthentication with
`RO-LOCK-AUTH-NOT-LOCKED`. Two renderer assertions reproduced missing restart
guidance. The shared monitoring-failure explanation now includes a restart
alternative for protected modes, explicitly retaining configured sign-in
requirements. A built-renderer denial replay checks unchanged snapshot, cleared
tree, failure latch, no provider fallback, and retained button focus. Native
denial is unchanged and covered by its existing state-machine tests.
That built-renderer replay also failed focus restoration for both protected
modes: disabling the pending unlock button lost focus. Restore focus only after
that explicit unlock action settles, preserving the recovery-confirmation
focus owner and leaving successful unmount unchanged.

Development checks are not final committed-candidate qualification. Native
authentication, production packaging, benchmarks, full W1 cross-capability matrix
and independent Wave review remain at their required gates. No ordinary profile,
project, credential, policy mutation or signing authority is inferred.

## Diagnostic B disposition and next bounded observation

The proposed B responsiveness hypothesis above failed at
`f1e060403b22ffa3ce776880b02c00ed1943d5a4`; see the native-B note. Preserve that
failure. Moving the command body after router return did not fix delivery.
Whole-application restart did restore live No-login behavior with no project
reopened and exact synthetic policy bytes unchanged. The unsuccessful async
attribute and its source-only guard are withdrawn, not counted as a repair.

The next observation retains synchronous baseline dispatch and adds only
fixture-gated, owned-window numeric system-menu entry/exit and synchronous-sender
diagnostics. It neither changes normal menu processing nor adds native commands,
permissions, payload inspection or policy authority. This tests the remaining
pre-router reentrancy hypothesis; source analysis alone has not established it.
Menu availability remains an explicit W1 finding even if truthful failure and
restart recovery satisfy the correction's restorative criteria. No timeout,
latch, authentication denial or release obligation is relaxed.

## Native06 result and production restoration

The fixture-only C replay at `a2b42814614bf6b7b46f1abfb654a92261f8c079`
restored status delivery during two observed menu cycles, with healthy UI and a
successful follow-on native project-page action. The native-C note preserves
the separate limits: visual Escape dismissal did not establish immediate native
loop return, and the experimental single-pending-slot overlap is unqualified.

Retire that experimental pending/token mechanism rather than promote its busy
fallback. The bounded production route uses a stateless private scalar message
for only the same public space-triggered system-menu action. Validate the entire
command as `SC_KEYMENU` plus its four low flag bits and space only; replay
directly once without re-admission. Preserve other standard messages unchanged,
failed-post default handling, same-window/thread ownership and normal destruction.
No new protected authority, renderer IPC, policy, timeout, latch or experience
contract. Independent scope review confirms this is restoration within the
frozen correction, not a general input/menu redesign.

Add strict message-class and repeated-input regressions, then verify the shared
production/fixture handler at a committed candidate. Fresh native checks must
cover live status, actual post-dismissal keyboard/focus behavior, standard menu
actions and closure/restart. Existing fault-injection recovery/latch tests remain
mandatory even when the menu no longer triggers a monitoring failure.
