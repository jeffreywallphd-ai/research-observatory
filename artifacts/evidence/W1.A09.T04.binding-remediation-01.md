# W1.A09.T04 — R01 evidence-helper remediation

Authority: the unchanged five W1.A09.T04 criteria and approved ECR-0008 packet
at `2160cc9917282d80848189dc9669fe3827979246`. R01 candidate is
`6a078f982815e547a57f4b2c670e50661b2d0bf3`; independent ledger is
`W1.A09.T04.review-R01.json`. The supported changes-requested transition
retains both medium blocking findings. No product, controller, reference,
permission, historical report, parent authority or release criterion changes.

The first review command supplied a note different from the ledger and was
rejected before mutation. The same command without that override completed.

Missing acceptance rows and immediate causes:

- **W1.A09.T04.R01.F01 / criterion 3:** exact string exclusion and resolution
  were mistaken for Windows identity safety. Deny noncanonical spelling and
  excluded identity before lookup; reject reparse/symlink ancestors and linked
  files before content access. Hold existing Windows read/directory guards and
  check metadata around the bounded read. Exercise protected-name aliases with
  mocks only; real filesystem cases use synthetic files only.
- **W1.A09.T04.R01.F02 / criterion 3:** a Git diff/index query was mistaken for
  direct candidate authentication, and reports were hashed but not authenticated.
  Check each selected report and tracked source snapshot against the explicit
  candidate blob before parsing or trusting it. Use the same bytes for parsing,
  raw hashes and Git-clean identity; retain field-specific LF hash semantics.
  Test valid input, assume-unchanged, skip-worktree and changed selected report
  in a fresh retained dummy Git repository. Keep generated/runtime artifacts
  explicitly separate and ignored, never claim they are candidate source blobs.

Focused tests precede helper edits. The original R01 ledger already reproduces
both defects without protected access. New tests additionally exercise the
corrected read/binding interface. Original observations, R01 and all adverse
reports remain immutable; a new final-binding report will supersede only the
mechanical proof, not relabel old executions.

Risk selection: targeted Python/helper tests, real dummy-Git boundary, lint,
fresh candidate binding and affected backlog/generated-view checks. Reuse R01's
independently verified product/native/Core/presentation/performance evidence
because those inputs do not change. Full product suites and Windows release
qualification remain at their previously recorded boundaries. Independent R02
must replay both findings and incremental changes. Governed UX and migration
are not affected. This is bounded maintenance, not a new approval/controller.
