# W2 resume-history portability maintenance

Predecessor: `3bc1c1828efab50efc730bf3907c3036eff58a2f`.

W2 readiness passed, but the subsequent atomic campaign-start publication was
rejected with `NOT_COMMITTED`: W1.R07–W1.R13 and the latest W1 projection compared
historical portable resume bindings against the new checkout instead of their
authenticated PAUSED predecessor. No campaign change was saved.

Bounded restoration: resolve historical comparisons using the worktree bound by
the existing, hash-checked ancestral predecessor. Preserve all historical bytes,
ancestry/hash/actor checks and current execution-identity checks. This is a
validator defect restoration, not product scope or structural refactoring.
No runner progress enhancement or optional cleanup is included.

Proof selection: extend the existing real-Git resume fixture with relocation and
substitution denials, replay existing ancestry/hash/append-only checks, then obtain
independent exact-commit control review. No historical W1 product suites are
needed. Current execution in the wrong checkout must still be rejected.

The regression failed before the correction with both historical-worktree
errors. Development replay passed the relocation/substitution case and existing
ancestry/hash/append-only checks (three tests); focused Ruff checks passed.
Commit-bound qualification and independent disposition remain required before
using the correction for campaign publication.
