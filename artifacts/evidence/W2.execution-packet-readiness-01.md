# W2 complete execution packet — readiness and decision handoff

2026-09-19. Authored publication/control/planning candidate:
`14b89c07a435a26bf894242a9e5f3e6429065055`.
This note and regenerated review pages are an evidence/view-only follow-up;
they do not approve W2 or change its authored packet, backlog or product.

## Decision already received

The [owner decision](W2.design-architecture-owner-approval-01.md) accepted
ADR-0027/0028/0029 and the exact reference 1.7 proposal at
`67bc9e3b6215504e78fa30dbf41cc0a664977679`. That prerequisite is complete:
the ADRs are Accepted and all eleven W2 slices bind the published reference.
The retained proposal and ADR-0026's inherited 1.5 catalog are unchanged.

Published reference: `RO-UI-ACADEMIC-MINIMAL-1.7`, package SHA-256
`dcf31147ee386d48e8ac95725d648ee2b08c74ef0dd42c139c5f78508a90f878`.
The active approval controls status; retained proposal-status prose is not a
new pending design decision. Existing product visual baselines were not changed
or requalified by publishing the reference. W2 implementation must prove its
affected conformance through the normal approved-reference/baseline workflow.

## Committed qualification and independent disposition

HEAD and authored inputs stayed fixed at the candidate throughout qualification.

| Boundary | Fresh check and observed result |
|---|---|
| Exact approval, inventory, content, ancestry and single use | Seven methods in `tests.foundation.test_reference_design_approval` PASS, including both independent-review regressions. |
| Existing approval formats | Six selected `UiConformanceTests` methods PASS: strict malformed-history shapes; actual 1.4, 1.5 and 1.6 exact shapes; actual 1.5 authority resolution; sibling proposal-consumption denial. Combined run: 13 tests, 29.818 seconds, exit 0. |
| Governed reference | `python tools/ui_reference_check.py --report artifacts/tmp/W2-reference-committed-integrity-01.json`: PASS; approved, reproducible; 54 governed files, 33 product pages, 35 HTML documents, 14 workflows; no errors/warnings. |
| Protected architecture paths | `python tools/adr_check.py --base 40a269e0168433d95cf6e316ee3aae48ad8e08fe --head 14b89c07a435a26bf894242a9e5f3e6429065055`: PASS, 29 indexed ADRs, 80 changed paths. |
| Affected Python | Ruff check/format PASS on the helper, new tests, `ui_conformance.py`, `planctl.py` and existing UI tests; Mypy PASS on the helper/new tests with `--follow-imports=silent`. |
| Complete planning and generated site | `python tools/planctl.py wave validate W2`: exit 0; two capability plans, eleven slice plans; site PASS covering 19 capabilities, 111 slices, 337 tasks and 492 HTML pages. |
| Current execution boundary | `python tools/taskctl.py next --profile LOC --platform windows-x64`: stopped at W2 pre-Wave approval, as required. No campaign or task claim. |

The [maintenance note](W2.reference-publication-maintenance-01.md) preserves
development failures and the bounded remedy. Independent reviewer
`/root/w2_ux_plan_review` APPROVED the exact committed adapter/publication/rebinding
increment, independently replayed both historical findings (two tests PASS in
5.603 seconds), and verified the committed package hash. Both findings are closed:
later repair cannot hide an invalid original publication; sibling consumption is
denied while an inherited ordinary merge remains valid. No incremental blocker.

Independent reviewer `/root/w2_source_packet_preflight` APPROVED the same
committed planning/publication delta. It confirmed all eleven bindings, seven
unchanged selections, 33 tasks totaling 530 engineering-hours and the sole included
two-hour runner-progress allocation. W1 has 68 DONE tasks (48 normal, 15 amendment,
five corrective), 15 APPROVED slices, COMPLETE campaign and APPROVED G1. G1's
[limited prototype acceptance](G1.owner-approval-01.md) remains limited, not full
runtime qualification. W2 is PENDING with no campaign or claims.

No completed W1 product suite was replayed. Renderer/native, packaging, security,
performance and full Wave qualification remain W2 implementation/exit duties,
not outcomes of these planning/control checks. This follow-up needs only review
of its generated/evidence delta and final committed authority/clean-checkout
confirmation; it does not require repeating unchanged product or control suites.

## Privacy-hook chronology and exact admission

The maintenance note's statement that no privacy-hook change had occurred describes
the development boundary before the subsequent commit attempt. Preserve it as
that earlier observation, not as a claim about the completed publication sequence.
The original commit attempt was blocked by the normal credential scanner: one
`generic-api-key` match in the published manifest's stylesheet digest. No raw
potential-secret value was printed during diagnosis.

An independent exact-file review confirmed that the match is the canonical
staged `assets/tokens.css` checksum, not authentication material; the stylesheet
had zero credential findings. The new published manifest was separately admitted
by exact path, regular-file mode, 6,422-byte blob and complete redacted finding
fingerprint. This did not extend the old proposal admission to a new blob.
All 131 prior admissions and historical-preservation records were preserved;
one exact entry was appended. Reviewed registry SHA-256:
`083f413052c165a54134932360361e89a27b513194575865d605673a845a2dad`.

The prepared snapshot was compared before activation: guard, baseline policy,
scanner configuration and sealed parser were unchanged. Only the original
checkout's worktree-specific hook binding was activated to the new sealed registry;
earlier snapshots and other checkout bindings remain intact. Normal commit hooks
then PASSed 73 changed entries with zero findings, and the message check passed.
No scanner-rule exemption, new account/path disclosure, protected-witness access,
push-routing change or remote action occurred. Local registry/runtime details
remain ignored. Linked-checkout hook compatibility remains the previously recorded
setup limitation; commits used the original checkout's normal hooks, not a bypass.

## Next human decision: immutable W2 execution approval

Review the [complete W2 page](../../planning/review-site/waves/W2.html)
(`planning/review-site/waves/W2.html`), with the
[CAP-04 contribution](../../planning/review-site/CAP-04/index.html),
[CAP-05 contribution](../../planning/review-site/CAP-05/index.html) and
[initiation/qualification plan](../../planning/W2-initiation.md).
The architecture/reference prerequisite and W1/G1 dependency are satisfied.
Final clean-checkout integration and the evidence/view-only disposition are
recorded with the final immutable candidate in the handoff, not inferred here.

Recommend approving the complete packet: seven decisions, eleven slices,
33 tasks and 530 estimated engineering-hours, including the bounded two-hour
automation improvement. Alternatively request a specific plan revision (revalidate
its affected packet) or defer (leave W2 pending). There is no subset execution.
Approval starts a fresh 79.5-hour supplemental-refactoring ceiling (15% of the
approved estimate), not an obligation to spend it or carry forward W1 allowance.

After explicit approval of the final reviewed commit, use this existing sequence:

```text
python tools/planctl.py --repo . wave approve W2 --by "<owner>" --commit <final-reviewed-full-sha>
python tools/planctl.py --repo . wave ready W2 --require-approved
```

Then start the single W2 campaign through taskctl and claim only its eligible
next task, expected to be **CAP-04.S01.T01 — reference-format parsers**. This note
does not execute those commands, freeze the packet or approve implementation,
G2 release, later Waves, external service use or a push.
