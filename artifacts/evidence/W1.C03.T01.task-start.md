# W1.C03.T01 — Supporting-tool return guidance

Claim base: `79ac074c47ba680928770b94764a1a6b434d3432`.
Origin: completed `CAP-03.S06.T03`; its contract and history remain immutable.
Authority: committed `W1.C03.T01.spec.json`, original slice §9.3,
ADR-0026, workflow-profile contract, Academic Minimal 1.6 style §6 and
Project Home/Research Intent/Model Center page contracts. Existing dependencies
CAP-03.S06.T02 and CAP-01.S02.T01/T02 are DONE. Ordinary W1 stays PAUSED
until independent correction acceptance.

## Acceptance and proof

| Material boundary | Required behavior | Selected proof |
|---|---|---|
| Not started | Explain that the workflow needs explicit start and lead to the existing Project Home action. Reopening Research Intent does not fabricate progress or a return. | Component and assembled-application regression; fresh native reproduction. |
| Pending handoff | Distinguish a pending supporting request from an expired return. Publish a return only after the exact valid response. | Delayed assembled request, both themes, named pending status and exact return. |
| Identity and races | Bind project ID/root, context/request generation, intent revision/hash, selection revision/hash, profile, source/current stage revision/hash, and target workspace. Old success/error/finally cannot affect a newer request or navigation. | Deferred success/rejection, workspace/project replacement and existing substituted-witness denials. |
| Researcher authority | Only the existing explicit Start action starts the workflow. Navigation never accepts intent, completes a step, or grants policy authority. | Command inventory and durable-progress assertions before/after navigation and explicit start. |
| Experience | Keep primary ordering/state distinctions, shared buttons, status semantics, keyboard focus and both themes. No new tokens/layout system or reference change. | Existing navigation tests, focused assembled interaction, workflow/accessibility conformance and native keyboard replay. |

Regression/characterization tests precede product edits. The exact admitted
files are ApplicationRuntime.tsx, WorkflowNavigation.tsx,
WorkflowNavigation.test.tsx and tests/desktop/test_model_center_interactions.py.
Reuse the existing request guard; add no schema, migration, permissions, model
adapter, background queue, optional styling or supplemental refactoring.

## Verification boundaries

Commit the product candidate and task-owned unchanged-reference UI contract,
then run fresh selected desktop lint/type/test/build, assembled Model Center
interactions, workflow/accessibility and affected Python quality checks. Replay
the real synthetic React/Tauri/Core not-started → explicit-start → supporting
return path with the preflighted isolated native harness. Mocks and static
checks do not establish that native path. No ordinary project/vault is used.

Independent commit-bound task disposition and corrective integration are
required before resume. CAP-07.S01 combined integration/handoff and fresh full
W1 qualification remain separate; this task does not approve either or G1.
Read-only implementation preflight found the four paths sufficient. No new
human gate or authority conflict was identified.
