# W1.A09.T04 — independent review and return preparation

This is an acceptance map and review preparation, not a review disposition,
amendment adoption, A08 activation, ordinary Wave resume or release decision.
The final task manifest must bind the tested candidate and every referenced
observation. No unfinished check is converted to a pass by this document.

## Authority and review sequence

The immutable ECR-0008 packet/execution/addendum at
`2160cc9917282d80848189dc9669fe3827979246` govern A09. The four-task contribution
is `W1.A09.S01`, Native project UX and truthful presentation qualification.
Its task criteria and eight amendment exit criteria remain byte-identical to
that approval. T01–T03 have independent approved dispositions; T04 must receive
its own frozen-candidate review before the contribution/exit can be accepted.

The original A08 packet, approval, task history and adverse findings remain
historical authority. Only the already approved presentation-reference overlay
changes its effective later qualification reference to Academic Minimal 1.6.
Semantic workflow authority remains Academic Minimal 1.5; token-source identity
remains 1.4. No token, workflow, permission, migration or security redesign is
authorized here.

## Acceptance-linked evidence inventory

| Obligation | Evidence and independent-review question |
|---|---|
| F01: truthful reference observations | T01 final evidence and independent reference/control checkpoint; T04 presentation-check-01 replays 66 reference captures, tokens, routes, workflows and accessibility. Are fresh 1.6 observations distinguished from earlier adverse 1.4/1.5 history and from product observations? |
| UX03/UX06: catalog/lineage native parity | T02 final evidence and review; T04 verification-integration-01 runs real compiled native → disposable Core catalog/project/lineage and denial cases. T04 units/native union retains strict generated/public contract tests. |
| UX04: multiline objective parity | T02 generated/native/Core proof plus T04 generated client/workflow and actual native/Core integration union. No schema/domain literal widening. |
| UX05: readiness-aware recovery | T02 proof plus T04 units renderer-recovery and actual default-None startup/native UI observations. Read failure is distinct from mutation failure; no hidden mutation replay or fallback catalog. |
| UX02: no directory/path-name typing | T03 final evidence and independent binding/review; T04 native-ui-observations-01 creates from a friendly name with the supplied default parent, restarts into no project, selects the existing folder with the actual Windows picker and explicitly reopens. Selection alone does not open/create. |
| Actual persistence/security | T03 real DPAPI/SQLCipher/default/collision reports plus T04 Core lifecycle and generated native/Core tests; T04 native UI preserves name/objective across a fresh native/Core process. GUI observation alone is not database-byte/collision evidence. |
| Cancel/lock/close and modal ownership | Exact T03 native-ui-observations-04 and final binding report, subject to current production-code equivalence; T04 production/harness native union replays lifetime, stale-generation and cleanup tests. Default-None T04 app-close/restart observation is not authentication proof. |
| Test composition correctness | T04 fixture-publication-adverse-01 retains failing staged-child rename; native-fixture-verification-01 characterizes/fixes sharing and bounds lifecycle receipt/resume. Current production directory picker remains unchanged. Same-account malicious writers are not isolated by this fixture. |
| Packaging/build | T04 native-builds-01 and production-native-build-01; packaging-production-02 independently inventories/builds the real frozen sidecar and executes only its safe --check; verification-integration-01 tests missing-runtime failure/restoration; resource-resolution-01 uses actual release Tauri resource_dir/runtime_config and rejects absent resources without debug fallback. |
| Experience/accessibility | T03 renderer/native keyboard/long-path/reduced-motion proof plus T04 presentation-check-01. The product-frame check uses declared native/Core doubles; the real GUI observations are separately labeled. F02/UX01 geometry is expressly not closed. |
| Performance | performance-02 retains the approved checker/baseline replay after the blind window/builds closed: all 12 browser samples and 20 component samples, both absolute and unchanged 20% thresholds passed. The original failed report-destination invocation is retained separately with no metrics claim. Candidate/product-input binding remains mandatory; no new baseline or discarded slow sample. |
| Semantic compatibility | T01 immutable 1.5 snapshots/compatibility witness and persisted predecessor evidence; T04 semantic-predecessor integration tests and reference/workflow conformance. No migration, persisted identity relabeling or reordered workflow. |
| Return control | T04 authority-return-check-01/diagnostic-01 retain the stale synthetic-fixture failure; check-02 binds the approved packet/frozen parent with unchanged controller. Maintenance-review-01 independently reviews the narrow fixture/privacy correction. |

Final source-binding evidence must distinguish original observation HEADs/raw
input hashes from the later delivery commit. In particular, output-only
redaction changes to verification helpers happened after some initial runs;
those original helper hashes are not current-byte attestations. Retain the
adverse observations and the explicit privacy corrections, and either replay or
establish the exact bounded difference before submission.

The performance checker was invoked with its existing `--allow-dirty` option:
the source state truthfully lists pending T04 work/evidence and the protected
untracked witness filename. That witness was not read, hashed, ignored or
modified to manufacture cleanliness. The benchmark binds the built product
manifest/runtime and fixture/baseline hashes; unchanged source/artifact
equivalence must connect these observations to the delivered candidate. Its
complete JSON was retained under artifacts/tmp as required by the checker,
then copied as the same parsed JSON value into performance-02 evidence without
changing samples, thresholds, machine metadata or source-state disclosure.

## Verification-method reconciliation

T03 evidence and its R01 reviewer used the stronger phrase “single packaged
renderer/native/Core lifecycle.” Approved T04 AC2 instead requires the
accumulated actual renderer/native/Core union **plus** clean Windows
build/packaging smoke; execution-01 and slice AC1 do not require that all of
these be one frozen-executable composition. The reviewer has corrected that
over-specific method description in packaging-scope-review-01, append-only.
Original T03 evidence/review remain unchanged; no approved criterion is waived.

The actual proof is composed: source-built real renderer/Tauri/Core lifecycle,
real protected persistence, production native release compilation, frozen
sidecar build/--check/failure recovery, and actual release resource resolution.
Unmodified-production/frozen-Core startup is **not proved**. It remains part of
later required W1/release qualification, using an available disposable Windows
profile/VM or another properly authorized isolation arrangement. Never launch
ordinary account policy/vault or add a production override to manufacture that
claim.

## Usability and retained ownership

The fresh no-context participant receives only the isolated working copy and
the same nine neutral tasks. Its transcript/assessment must be recorded
separately from automated verification and from the original study. Tool input
failures are not proof of novice confusion or application malfunction. The
fixture denies support preview/export to avoid ordinary app-data access; that
limitation cannot be reported as a demonstrated production support defect.

Study IDs U01–U06 are distinct from ECR UX02–UX06. Retain
`W1.usability-blind-01.follow-up-01.md`: U01/U04 have mechanical integrated
creation/folder evidence, but blind success must be observed independently;
U02/U03 require truthful guided-context/error qualification, not merely project
creation success. U05 is an open P2 historical wording-comparison obligation
owned by CAP-03.S02.T02/W1; U06 is an open P3 lifecycle/session presentation
ambiguity, not a persistence-state defect. Neither is silently closed or
automatically deferred to W5. Govern the necessary decision/remediation at a
lawfully eligible boundary outside frozen A09 scope.

F02/UX01 remain explicitly owned by **A08.T02 after return**: effective paragraph
separation and full-height sidebar, using real product geometry/captures in both
themes at 1440×900, 1280×720 and effective 720×450, long navigation and reduced
motion. A09 does not change those styles or mark those findings closed.

## Exact exit and checkpoint preparation

The companion `W1.A09.T04.return-preparation.json` binds the exact frozen
correctionReturn. It is inert preparation, not an adoption checkpoint. After
all task criteria pass and T04 is independently approved:

1. Independently review the four-task contribution against all four slice
   criteria using the accumulated proof, retained adverse findings and exact
   source/authority bindings. Record the disposition through the supported
   amendment completion/exit mechanism; do not invent a second controller.
2. Create and commit `W1.A09.exit.json` with the eight packet exit criteria,
   exact current candidate/branch and campaign states, named passing checks,
   task review bindings and the slice disposition. Submit via
   `taskctl amendment submit W1.A09 --agent codex --from <exit-evidence>`.
3. An independent reviewer uses the frozen exit round and
   `taskctl amendment review W1.A09 --reviewer <independent-reviewer> --from <ledger>`.
   Resolve blocking findings append-only. Do not represent T04 approval as exit
   approval.
4. Only after the independently approved exit state is committed, prepare a
   separate adoption evidence record binding that exact
   `reviewedCompletionCommit`, exit round/ledger/evidence and correctionReturn.
   Commit it and use
   `taskctl amendment adopt W1.A09 --agent codex --from <checkpoint-evidence>`.
5. Verify the returned hold is **unchanged PAUSED A08**, not ordinary Wave scope
   or an active parent lease. Explicitly activate A08 and recover its approved
   T02 via supported commands at the actual current branch/HEAD/worktree.
   Finish/review/adopt A08 before ordinary W1 resume or CAP-07 recovery.

No command above is executed by preparing this document. All current preceding
reviews and later state preconditions must be revalidated at the transition.
The full repository/Windows cross-capability matrix, independent W1 review and
human G1 approval remain due; correction adoption cannot satisfy them. Local
main must not receive ancestry with unmet A08 gates, and no remote push is
authorized.

Review pages: `planning/review-site/enablers/ECR-0008.html`,
`planning/review-site/enablers/ECR-0007.html`, and
`planning/review-site/waves/W1.html`.
