# GCR-0003 — Restore revision headroom for the exact S02 lane

## Status

Pending independent packet review and exact-commit human approval. This proposal is inert and grants no execution authority.

## Demonstrated post-adoption mismatch

GCR-0002 was adopted at exact commit `72e2989440292ca30f8b5d4717aa61c56c5b9f48`, raising the live control and minimum-tool revisions from 8 to 9. The approved GCR-0002 design requires a separately reviewed and exactly approved `GRR-0002.S02/GRR-0002.B02` after adoption. However, the version-3 S02 schema fixes its transition and activation boundary at 8→9, while both `validate_preappend_supplement_boundary` and `command_supplement_start` require the live revision to equal the packet predecessor. A schema-valid S02 can therefore start only at revision 8, but its governing approval gate is legally available only after the repository has reached revision 9.

The controller fails closed before backlog mutation. W1, the active hold, W1.A04 absence, T03 block, G1, and the trigger witness remain safe. The defect is nevertheless blocking: no current approved controller can represent the next required supplement without a rollback, direct edit, reopened bootstrap, competing hold, or transition bypass.

## Bounded prerequisite

`GCR-0003.B00` is a one-time controller-generation bootstrap under the existing `HOLD-W1-GRR-0002`. It preserves all GCR-0002, GRR-0002, S01/B01, ECR-0003/W1.A04, review, evidence, and Git history. It keeps the live backlog at revision/minimum 9 through implementation and independent B00 review. Only after B00 is independently approved may a durable direct-child adoption append one exact 9→10 generation while the version-3 supplement formats remain unchanged as the evidence of the failed design.

The new reader and backlog schema will support through revision 11 and add new version-4 supplement packet and approval formats for only the later exact `GRR-0002.S02/GRR-0002.B02` 10→11 transition. This restores one revision of headroom and follows the previously successful generation-then-supplement sequence. The v4 S02 packet remains inert until its own independent packet review and exact-commit human approval. GCR-0003 does not approve, install, start, submit, review, or implement S02/B02.

## Controlled lifecycle

After an independent APPROVED packet review, the repository owner may exclusively create `GCR-0003.approval.json` for the exact reviewed packet commit. Its tracked-clean introduction commit on `codex/w1-windows-local-runtime`, with only the exact trigger witness untracked and unstaged, is the sole B00 base.

The packet authorizes a new `tools/gcr3ctl.py` lifecycle with submit, independent review, strict-descendant resubmission, adopt, recover, and validate operations. Candidate scope is compared from the approval-introduction commit to the exact allowlist. Every attempt, evidence record, finding, closure, review ledger, and state projection is immutable and append-only. Self-review, a blocking open finding, stale or forked authority, wrong branch or head, altered predecessor history, and any witness deviation fail without mutation.

## Interruption-safe adoption

GCR-0003 adoption exclusively creates its own lock, two durable successor files, and a prepared manifest under the version-3 transaction schema. The manifest and runtime envelope bind the exact revision-9 raw and canonical backlog/state predecessor pair, the approved review projection, the adoption-evidence commit, the active hold, adopted GCR-0002 generation, exact witness, branch, actor, 9→10 revisions, supported ceiling 11, publication order, and final two-path Git boundary.

Backlog hashes are accepted only for `planning/backlog.yaml`; state hashes are accepted only for `planning/governance-control-recovery/GCR-0003.B00.state.json`. Windows publication uses replace-existing/write-through semantics and flushes successor files, the manifest, published files, and parent directories. Every supported reader fails closed on a prepared transaction, partial publication, missing marker, substitution, stale authority, dirty workspace, or split pair. Cleanup occurs only after the exact successor pair validates.

`gcr3ctl recover` is idempotent and is also an automatic mutation preflight. It can only restore the exact predecessor pair or complete the exact successor pair. Real child-process tests must terminate after every preparation, durable-write, marker, replacement, validation, flush, and cleanup boundary. The finalization commit must be the direct child of the adoption-evidence commit and change exactly the canonical backlog and GCR-0003 state paths.

## Required end-to-end proof

The controller test must begin from a real Git worktree at the exact adopted revision-9 authority, not a synthesized revision-8 fixture. No supplement-boundary function may be mocked or bypassed. The proof must:

1. introduce and independently approve the exact GCR-0003 authority;
2. execute and independently approve B00 while live revision remains 9;
3. adopt the exact 9→10 generation and prove revision-9 readers fail closed;
4. separately introduce, independently review, and exactly approve a v4 S02 packet;
5. start only sequential `GRR-0002.S02/GRR-0002.B02`, raising 10→11; and
6. prove that no S02 approval/start, B02 action, amendment materialization, task claim, W1 resume, hold release, G1 action, main integration, or remote action occurs within GCR-0003.

Adversarial real-Git cases must cover stale, forked, wrong-hold, wrong-generation, wrong-predecessor, unsupported, non-sequential, adverse-review, dirty-workspace, and witness-substitution inputs with byte-identical denial.

## Safety boundary

GCR-0003 grants no S02 packet approval or start, B02 execution, W1.A04 append or bootstrap action, amendment materialization, task mutation, T03 recovery, hold release, W1 resume, G1 approval, local-main integration, or remote push. It cannot edit or replace the version-3 schemas, GCR-0002, GRR B00/B01, S01/B01, ECR-0003/W1.A04 authority, prior evidence/reviews, or the trigger. It cannot create a competing hold, roll control backward, permit a same-revision supplement, or add a generic historic-candidate or dirty-worktree bypass.

The witness `artifacts/evidence/W1.A04.B00.json` remains exact at SHA-256 `4a9d944ff95972b449b617bc384306c7023e79d31d6b427e6b6f4678cd58b22c`, untracked, unstaged, non-authoritative, and unavailable as submitted evidence.

## Alternatives

- **A — Approve bounded GCR-0003 (recommended):** adopt 9→10 with reader/schema support through 11, then require a separate v4 S02 approval for 10→11. This preserves strict revision increase and append-only history.
- **B — Keep W1 stopped (safe):** preserve the current fail-closed boundary indefinitely; S02/B02 and W1 remain unavailable.
- **C — Reinterpret v3 as generation-neutral at live revision 9 (rejected):** this would make S02 installation cease to raise the minimum control revision and would silently alter the approved v3 meaning.
- **D — Roll back, edit v3 in place, reopen B00/B01, or bypass the predecessor check (rejected):** each would violate immutable authority and the same-hold append-only recovery rules.

## Recommendation

Independently review this inert packet. If it is approved, request exact-commit human approval for only `GCR-0003.B00`. Do not prepare or approve S02 until GCR-0003 has been independently approved and durably adopted. If any exact authority, sequencing, headroom, older-reader, transaction, or witness criterion fails, retain the current revision-9 stopped state without mutation.
