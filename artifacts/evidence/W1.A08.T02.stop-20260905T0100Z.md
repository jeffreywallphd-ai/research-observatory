# User-directed stop checkpoint — 2026-09-05 01:01 UTC

Working handoff only, not task evidence or a completion/review disposition.
The user requested a stop within two minutes. All agents stopped and no
qualification, submission, review transition, or integration was attempted.
Backlog remains the authority for task and campaign state.

## Resume position

- Repository: `C:/workspace/research-observatory`.
- Campaign branch: `codex/w1-windows-local-runtime`.
- HEAD: `ac87084929c02b55089c40e22b4a7b3f047db7c5` (T02 claim/acceptance closure).
- Local main: `46e88d171795b76a2f1588d69756f4184c01a89b`.
- T01 is DONE, independently approved R02, and integrated into local main.
- T02 is claimed IN_PROGRESS; W1.A08 remains ACTIVE and W1 remains PAUSED
  under the approved amendment hold. No workflow-state mutation was made for
  this short user stop. Check/renew the existing owner leases on resumption.
- Preserve every uncommitted edit below. No new implementation commit exists.
- Never read, hash, stage, edit, move, delete, or ignore the protected untracked
  `artifacts/evidence/W1.A04.B00.json` to obtain a clean check.

## Saved uncommitted work

Root owns `tools/product_style_check.py` and
`tests/desktop/test_product_style_check.py`:

- One-time capture publication, internally derived filenames, exact metadata,
  finite JSON, bounded RGB(A) PNG chunk/CRC/decompression validation, no-overwrite
  and incomplete-run handling, guarded Windows persistence, and explicit
  immutable delivery-commit authentication (separate from producer commit).
- Producer source/build/reference/Git snapshot and capture/verify CLI wrapper
  drafted. These wrappers are NOT live-qualified and depend on the unfinished
  `product_style_qualification_matrix` implementation.
- Latest root checks: 23 focused Python tests PASS; Ruff lint and format PASS.
  No mypy, complete affected replay, live capture, or performance run yet.
- Read-only independent preflight reproduced an OPEN material reader race:
  changing a previously verified PNG during the final Git inventory query lets
  `read_capture_bundle` return success. Before qualification, add a regression
  test and lock/revalidate the complete reader snapshot (directory, manifest,
  PNGs), not just each file independently. Reviewer changed no repository files.
- Still add actual junction/symlink boundary tests where platform permits;
  stronger decompressed-pixel-length negative is useful. Existing tests cover
  CRC/truncation/completion/trailing data, real Git input dirtiness, co-tampering,
  duplicate/missing/extra inventory, interruption, and directory replacement.

Agent `/root/t01_f02_live_geometry` owns saved edits to
`tools/desktop_app_check.py` and `tests/desktop/test_desktop_app_check.py`:

- Source-derived exact eight-workspace mappings, pure
  `qualification_capture_contract(repo)` with 108 metadata rows (48 workspace
  cases plus six designated cases, each product/reference), and pure
  `product_style_qualification_errors(matrix)` fail-closed validator.
- Valid 48+6 fixture and negatives for identity/theme/viewport/mapping,
  geometry/focus/overflow/theme/motion/designated/dialog contracts.
- Latest focused pure unit PASS (one test, 0.014 s). No live browser run.
- Remaining: implement `product_style_qualification_matrix(repo, capture=None)`
  returning `(errors, report)`, populated Task Center adapter, deterministic
  renderer/font/time/random/network/input-stability witnesses, and integrate
  static analysis plus complete matrix into authoritative desktop checks.
- Callback contract: `(metadata, png_bytes)` immediately after each viewport
  screenshot; exact keys are caseId, surfaceId, stateId, theme, viewport,
  role, referencePage, width, height. Capture both product/reference for every
  row, without requiring product/reference pixel equality.
- Existing fixture `tests/desktop/fixtures/task_center_interactions.js` supplies
  populated Task Center through the real adapter/project-open path. Other T01
  adapters cover project/audit, diagnostics, and lock/reconciliation states.

## Clean qualification checkout prepared

`C:/workspace/research-observatory/.local/qualification-worktrees/w1-a08-t02`
is detached at `ac87084929c02b55089c40e22b4a7b3f047db7c5`, with clean Git status.
Advance it safely to the eventual exact tested candidate before qualification.

- Worktree-local node_modules, 58 packages copied offline, zero downloads.
  All 161 dependency links were checked to remain inside the worktree.
- Pinned Node 24.19.0 folder-only junction; pnpm 11.20.0. React 19.2.8 SSR,
  Vite 8.2.1 import, and TypeScript 7.0.2 version smoke checks pass.
- Root `.pnpm-store` lacked react-dom 19.2.8. Actual existing local cache
  `C:/Users/redacted-user/AppData/Local/pnpm/store` succeeded under escalation after an
  initial sandbox SQLite access failure. Install used offline frozen lockfile
  and `--ignore-scripts`; no external fetch or source/baseline change.
- No build or benchmark yet. Do not reuse root node_modules or use
  `--allow-dirty` as authoritative performance evidence. Run existing unchanged
  12-repeat desktop and 20-batch shared-table benchmarks, preserving every
  sample and existing absolute/20% relative thresholds. Never reset baselines.

## Next sequence

1. Read AGENTS, current backlog, accepted task-start worksheet and this handoff;
   resume the same campaign/lease, preserving all saved edits.
2. Close the reader-race preflight, finish the live harness and wrapper contract,
   run narrow Python/lint/type tests, and inspect exact source inventory.
3. Commit coherent implementation, clean-build functional product and reference
   fixture, capture into a new absent directory, commit delivery, and verify
   against its external immutable Git ID. Retain failed attempts; never relabel
   them as passing. No live evidence has been generated by these new functions.
4. Complete affected visual/accessibility/workflow/performance checks and
   independent T02 review, then S01 and amendment-exit reviews/checkpoint/adoption.
5. Explicit normal W1 resume and CAP-07.S01.T02 recovery follow adoption. W1 and
   its release gate are not complete and later-wave work is not authorized.

Review site: `planning/review-site/waves/W1.html`; styling packet:
`planning/review-site/enablers/ECR-0007.html`. Both remain existing generated
views; no planning/backlog mutation occurred during this stop.
