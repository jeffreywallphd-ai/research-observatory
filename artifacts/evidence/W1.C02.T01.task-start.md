# W1.C02.T01 — Restore current model-authority expiry checks

Claim base: `34d100a61c0d70643e92343d0a8b2496496313ea`.
Inherits the unchanged CAP-07.S01.T02 contract and W1 approval through the
[correction spec](W1.C02.T01.spec.json). The original task remains DONE.
[Slice preflight F01](CAP-07.S01.preflight-01.json) records the reproduced defect:
registry time was sampled before authority lookup, allowing one dispatch after
permission and host witnesses had expired. A later denial cannot undo dispatch.

Acceptance closure before product edits:

| Boundary | Required proof |
|---|---|
| Eligibility freshness | Expiry during inventory/permission lookup, or while later manifests are assessed, removes expired candidates with truthful stale reasons. Unexpired matching and deterministic ordering remain valid. |
| Routing consumption | Expiry during pre-dispatch lookup gives zero adapter calls; expiry during final publication gives no returned/persisted output. Permission and host expiry are independently tested at the exact exclusive expiry boundary. |
| Replay | Expiry during the final catalog check denies output without another dispatch or rewriting the original successful history; the immediately-before-expiry control still succeeds. |
| Identity/persistence | Existing project, actor, catalog/task/manifest hashes, rights/policy revisions, cumulative budgets, atomic journal/circuit and original-request invariants remain unchanged. Shared tests run against memory and real SQLCipher with fixture keys. |
| Compatibility and scope | No schema, migration, permissions grant, provider/network access, public API or UX change. Existing contract, Core principal and protected-repository tests cover the adjacent boundaries. |

Add deterministic advancing-clock tests and run them against unchanged product
code before correction. Then run focused registry/routing contracts, protected
integration, Core service/principal, quality and architecture checks at the
committed candidate. Fresh unchanged performance checks remain required; no
historical measurement is reused or rounded. Full repository/platform/native
qualification remains at W1 exit. No native/UI test is required for this
internal timing correction, which changes no user-facing contract.

This is ordinary restoration of current-authority behavior, not structural
refactoring or a new budget allocation. Independent slice review confirmed
the four-path scope and original-task binding before admission.

## Red characterization and fixture correction

Before product edits, the 102-case registry/routing/protected run failed with
16 new expiry regression failures and one unchanged benchmark failure
(26.077 ms p95; pre-commit diagnostic only). Unexpired controls passed.
After the timing fix, the first focused 54-case run left one existing cooldown
test failing: it advanced only the gateway clock to 2600 ms, left registry time
at 1500 ms, and expected success with witnesses that expired at 2000 ms.
The fixture now uses one public clock, first proves cooldown alone cannot renew
authority, then refreshes both witnesses before checking successful circuit
reset. This strengthens the existing contract rather than removing its assertion.
