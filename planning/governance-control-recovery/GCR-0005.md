# GCR-0005 — Recover the exact GRR-0002.B02 R01 adverse-review projection

## Status

Pending independent packet review and exact-commit human approval. This packet is inert and grants no execution authority.

## Demonstrated control defect

`GRR-0002.B02/R01` was frozen for review at commit `962f92ff831c9a3d87a7d6ba796c8194e70b6c2c`. Independent review produced the canonical `CHANGES_REQUESTED` ledger at `planning/governance-recovery-approvals/GRR-0002.B02.review-R01.json`, SHA-256 `4b92f7b3a6a621e25c919f8571d7a87617966ae76a7dcbc4f3dcdd05af563e09`. The ledger was preserved append-only at commit `8b784cabc5d5d996c00fd2fbcc8f22a1ad05b5bb`.

The exact `recoveryctl supplement-review` command fails before writing. The approved B02 scope-addendum loader authenticates the immutable installed-candidate backlog, then incorrectly requires the live backlog to remain byte-identical to those pre-submission bytes. The lawful `supplement-submit` transition necessarily changed only B02 from `IN_PROGRESS` to the exact frozen `REVIEW/R01` projection, so the authority loader now rejects the state it created. The backlog remains byte-identical at SHA-256 `431ac0390c7aa1b1be229f741cea0b00fb73cfd24713fdcb0e8cc6a13595c7a1`; W1 remains paused under `HOLD-W1-GRR-0002`.

The defect cannot be repaired by ordinary B02 remediation because `supplement-resubmit` is unavailable until the adverse R01 disposition is represented, while the normal review transition cannot represent it. A new S03/B03 is also invalid because the backlog permits only the latest supplement to remain non-approved. Editing the backlog, running a modified controller outside its committed authority, changing the ledger's reviewed-state binding, or skipping R01 would bypass the append-only control.

## Bounded recovery

`GCR-0005.B00` authorizes only a one-time projection of this exact R01 ledger into the current backlog. It preserves the candidate, evidence, ledger, control revision, hold, paused Wave, blocked task, absent amendment, pending gate, and witness. It introduces a separate exact `gcr5ctl` lifecycle with commit-bound evidence and independent review. Only after B00 itself is independently approved may its crash-safe application derive `GRR-0002.B02=CHANGES_REQUESTED`, append the exact R01 attempt and review, clear `current_submission`, and regenerate the seven deterministic backlog views.

The bridge does not fix or close `GRR-0002.B02-R01-F01`. After application, ordinary in-scope B02 work must correct the loader, add the real-Git lifecycle and denial tests, create a strict-descendant candidate and fresh remediation evidence, and use `recoveryctl supplement-resubmit` for R02. Independent R02 approval remains mandatory before W1.A04 recovery can begin.

## Application safety

The application uses an exclusive lock, durable successor backlog, prepared manifest, raw and canonical compare-and-swap, Windows replace-existing/write-through publication, deterministic view generation, exact-state validation, idempotent recovery, and cleanup only after validation. Interruption tests must cover every preparation, replacement, generation, validation, flush, and cleanup boundary. Recovery may yield only the exact frozen predecessor or the exact ledger-derived successor; stale, forked, substituted, dirty, redirected, or accompanied state fails closed.

## Alternatives

- **A — Approve exact GCR-0005 (recommended):** preserve the adverse review and restore ordinary strict-descendant B02 remediation through an explicit, independently reviewed projection.
- **B — Keep recovery stopped (safe):** preserve the current fail-closed boundary indefinitely; B02, W1.A04, and W1 remain unavailable.
- **C — Patch the loader or backlog before recording R01 (rejected):** changes the reviewed boundary or bypasses the failed controller transition.
- **D — Start S03/B03 or skip R01 (rejected):** violates the single-latest-nonapproved-supplement and append-only review controls.

## Approval gate

Approval is legally available only after an independent `APPROVED` packet review at the exact packet commit with no open blocking finding. Human approval must name that commit and authorize only `GCR-0005.B00`. Approval does not authorize the backlog projection before B00 implementation itself has commit-bound evidence and independent approval.
