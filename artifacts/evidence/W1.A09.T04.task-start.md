# W1.A09.T04 — accumulated correction qualification

## Claimed authority and scope

Claim base: `cd4838e9c64fdbf3adb8f326781f95404a0c945d`, after independent T03 R01 approval. The approved ECR-0008 execution packet at `2160cc9917282d80848189dc9669fe3827979246`, its UX addendum and immutable five T04 criteria govern this work. Presentation reference is approved Academic Minimal 1.6; scholarly workflow semantics remain 1.5. ADR-0017/0018 and current application-lock architecture retain account-optional default None, protected-generation cancellation and user-scoped encrypted persistence.

T01–T03 have independent commit-bound approval. This task qualifies their accumulated interfaces and prepares slice/exit/return evidence; it cannot approve itself, adopt A09, activate paused A08, close A08 styling findings or approve G1. The exact frozen A08 correction-return relation remains unchanged. No remote action, researcher-data access, credential entry, ordinary policy/vault access or Windows account/security change is authorized.

## Acceptance-closure map

| Criterion / boundary | Material risk and selected proof |
|---|---|
| AC1 finding ownership | Bind F01 and UX02–UX06 to T01–T03 evidence and integrated replays. Keep F02/UX01 assigned to paused A08.T02. Keep study U01–U06 distinct from ECR UX IDs and retain the follow-up record's open dispositions. |
| AC2 current renderer/native/Core | Actual friendly-name/default-only Create, closed creation, explicit native selection/Open, saved identity/content, Close and fresh application/Core restart. Real UI and Core use isolated synthetic storage, with every substitution disclosed. Replay affected component/native/contract/lifecycle/security tests and actual source-built integration. |
| AC2 failure/recovery | Collision preserves bytes/destination; selection/cancel creates nothing; denied/reparse/install paths and late protected results fail closed. Reuse T03 actual modal lock/close proof where bytes remain unchanged, otherwise refresh affected observations. Cold-start/hang and restart observations must be bounded and truthful, not attributed to an unproven cause. |
| AC2 native fixture publication | Existing fixture pins share reads only and may block Core's staging rename. Add a failing real staged-child publication characterization before changing that helper. Preserve deny-delete/identity checks, test parent-rename denial and reparse detection, and remove any claim of same-account race-free confinement. |
| AC2 fixture restart / default None | Existing fixture rejects occupied roots and seeds protected manual-lock policy. Introduce only a test-composition lifecycle mode with absent policy for real default-None initialization and narrowly identity-validated resume of its own retained fixture. Keep protected modal-test modes unchanged; no production switch, general occupied-folder reuse or new IPC authority. |
| AC2 packaging/build | Fresh production sidecar build/inventory and frozen `--check` with no system Python; current release native compilation and actual Tauri resource representation/no debug fallback. A frozen fixture entry may exercise real Core with an explicit isolated vault, but is a qualification artifact, never the production sidecar. |
| AC2 experience/performance | Current built React both themes, long paths, reduced motion, accessibility/reflow and existing approved performance methodology. Reference observations and functional/native product observations remain distinct. Sidebar/effective paragraph-spacing closure waits for A08 return. |
| AC3 evidence/compatibility | Bind exact source/build/input/candidate hashes, retained baseline and semantic predecessor evidence, selected checks and unchanged public/authority contracts. Independent adversarial slice review owns integrated disposition; retain every failure and no weakened valid tests. |
| AC4–5 return authority | Prepare the exact exit criteria and correctionReturn checkpoint for later supported submit/review/adopt. Authenticate the frozen PAUSED A08 record and original approvals. No ordinary W1/CAP-07 activation before A08 adoption, and no task completion portrayed as release approval. |

## Independent preflight and first characterization

Read-only preflight by `agent:/root/t03_native_replay` inspected the existing seams. The production resource-path representation correction is present and does not explain the old hang. Native fixture Create/Open/Close confinement exists, but read-only directory sharing and absent resume support need characterization before integrated persistence can be claimed. The planned correction is bounded test-harness maintenance, not changed production storage/security authority. Existing application-lock all-test-target Clippy needless-borrow warnings also require narrow replay/correction before qualification; do not remove assertions or change lock semantics.

Run the smallest failing/characterization cases first, then implement only proved necessary fixture/test corrections. Broader accumulated union and clean build/smoke are explicitly required by AC2 here; the complete repository/Windows Wave-exit matrix remains a later W1 duty. No new controller, approval identity, GRR/GCR or reference change is introduced.

## Proof limits and platform handling

The ordinary production desktop and frozen Core `--supervised` read the account's real policy/vault. Do not launch them in this account as an allegedly isolated test. The existing seams can safely compose real renderer/native/Core fixture lifecycle with real production build/`--check` and release resource/no-fallback checks. That is not unmodified-production startup under clean ordinary defaults. Any requirement for that stronger claim needs an already available disposable Windows profile/VM or a separately authorized equivalent; record the exact residual and exhaust safe affected work before a platform handoff. Do not create an account, change OS settings, weaken Core canonicality, add a production vault override, or invent platform evidence.

All synthetic fixtures remain scoped under ignored artifacts/tmp with stable identities and bounded names. Never access the unrelated protected W1.A04.B00 witness or the sibling planning workspace. Fresh blind novice retesting remains a separate no-context participant task after the relevant integrated corrections, not a substitute for security, packaging or release qualification.

## Qualification maintenance observations

The initial accumulated units replay retains a sandbox-owned Git clone denial and stale-product-manifest rejection. Neither was treated as a pass; a current-source rebuild and intended Windows-user replay passed without safe.directory changes. The native union and real native/Core/package-failure/recovery union passed independently.

The paused-correction authority test retained in authority-return-check-01 fabricated A09 by renaming an old A08 packet, keeping superseded 1.5 experience hashes. The current validator correctly rejected it. An unchanged-validator counterfactual using the exact approved A09 packet and its frozen paused predecessor passed. The bounded maintenance increment changes only that test fixture to the immutable approved packet/baseline; all positive and injected-history/active-wave rejection assertions remain mandatory. No live authority, controller, schema or historical record is changed; independent review is required before integration.

Publication privacy review found that Python repr-style doubled-backslash build logs escaped the initial redactor, and Git ownership errors include local principal identifiers. Three synthetic redaction tests first failed in three subcases (escaped account path, escaped repository path, principal diagnostic), then all three passed after the output-only correction. Before publication, identifiers in the new package report's buildOutput and initial units report's ownership diagnostic were replaced with explicit placeholders. Original outcomes, errors, timestamps, artifact inventories and observed input hashes are retained; raw local identifiers are not preserved for publication. The package/verification runner redaction changes occur after their initial runs, so source equivalence for the unchanged verification logic must be recorded rather than falsely claiming the old helper hash matches current bytes.

The first read-only release-resource runner attempt stopped before any Tauri/Core process launch: the intended Windows user could not traverse the earlier sandbox-owned production package fixture (WinError 5). No ACL was changed and no existing artifact was replaced. A separate fresh current-user package build is used for the resource-resolution proof; the original packaging-smoke observation remains valid only for its own launch context.

Independent pre-publication review also identified repr-style identifiers in the new integration report's captured subprocess output. The same output-only redaction was applied there, preserving every result and input inventory. The original adverse observations are retained with identity placeholders, not erased. The independent reviewer will recheck the revised reports before any commit.

Final units03 and native02 replay the current output-redacted verifier rather than treating the earlier helper hashes as current. The independent source audit authenticates current inventories and unchanged protected-modal production mechanisms; integration02 supplies the same final-helper replay for native/Core/packaging. Original observations remain append-only.

The fresh blind novice completed all nine attempts, independently created/saved/reopened its own study without path entry and closed normally. Its transcript and separate assessment retain unresolved affordances and explicitly distinguish fixture-denied Task Center GET/support preview from production failures. No product hints or project context were supplied. The full nine-task path is not claimed successful.

The first performance invocation used an unsupported artifacts/evidence destination; the existing checker denied publication and returned no metrics. The exact failure is retained. Repeating the unchanged checker at its required artifacts/tmp destination after the blind window/builds closed passed all original thresholds and retained every sample. The copied evidence preserves the parsed report, including its truthful --allow-dirty disclosure and original observed HEAD; later candidate equivalence is required. No protected witness access, ignore rule, baseline/engine/tolerance adjustment or sample removal occurred.
