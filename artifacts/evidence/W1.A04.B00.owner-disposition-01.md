# W1.A04.B00 retained draft: owner-directed disposition

On 2026-09-19 the repository owner explicitly requested inspection of the long-lived
untracked `artifacts/evidence/W1.A04.B00.json` and an appropriate disposition before
starting CAP-04. This bounded instruction superseded the earlier no-read/no-move
rule for this inspection and archival operation only.

## Findings

- The 6,520-byte JSON file was created on 2026-08-24 (filesystem UTC metadata).
  Git reports it untracked and has no commit history for that path.
- It is a `task-criterion-evidence` draft for `W1.A04.B00`, naming candidate
  `214ac1aac53b4396ee29f7a935ddcac2a34618b6`. Its own disposition remains
  `pending-review` / `independent-control-security-review`; its test claims are
  historical draft claims, not newly verified results or accepted task evidence.
- Its raw SHA-256 is
  `4a9d944ff95972b449b617bc384306c7023e79d31d6b427e6b6f4678cd58b22c`, exactly the
  fingerprint retained by the historical recovery validators.
- [GOV-MAINT-0001](../../planning/governance-migrations/GOV-MAINT-0001.json)
  records why it survived: older controls both required this local witness and
  rejected it as untracked during clean-state transitions. It was deliberately
  retained as incident evidence rather than committed as approved evidence.
- The backlog now records `W1.A04` as `SUPERSEDED`, with no bootstrap, campaign,
  tasks or completion evidence. Current `taskctl` validates that terminal
  reservation from immutable approval/history and does not require the old file.
  Retired-controller tests use isolated synthetic witnesses instead.

## Disposition

Preserve the original bytes in `.local/retained-history/W1.A04.B00.json`, an ignored
local archive, and remove the obsolete active-directory placement by moving the
file. Do not delete, commit, publish, or promote the draft to accepted evidence.
Verify the exact hash and byte count after the move. The operation is recoverable
from that archive; ordinary Git backups do not include it.

This resolves the permanent untracked-file clutter without erasing historical
evidence or rewriting any approval, adverse finding, validator, privacy hook,
backlog entry or released result. The existing `/.local/` ignore rule is sufficient;
no broad evidence ignore or new hook exception is introduced. Historical records
describing the file as untracked remain unchanged and true of their original time.

Archive verification on 2026-09-19: the move completed without overwrite; the
destination has the same 6,520 bytes and raw SHA-256, the original path is absent,
`git check-ignore` confirms the archive exclusion, and the root checkout is clean.
Independent read-only review confirmed no current live workflow requires the
old placement. Only the disposition/documentation is intended for version control.
