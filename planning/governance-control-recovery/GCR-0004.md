# GCR-0004 — Recover the exact GCR-0003 R01 adverse-review projection

## Status

Pending independent packet review and exact-commit human approval. This packet is inert and grants no execution authority.

## Demonstrated control defect

`GCR-0003.B00/R01` was frozen for review at commit `702ffbc587cca2ec05567d86dc9fd0fa0a25b4a5`. Independent review produced the canonical `CHANGES_REQUESTED` ledger at `planning/governance-control-recovery/GCR-0003.B00.review-R01.json`, SHA-256 `cdfdb2f9fc122cb1a3be3d4546542dfc108a35c96995a340d32b5ae3510ba93b`.

The exact `gcr3ctl review` command authenticated the submission and ledger, derived the adverse state, and then failed before writing because `governance-control-recovery-runtime.v3.schema.json` permits `READY`, `REVIEW`, `APPROVED`, `ADOPTION_FINALIZATION`, and `ADOPTED`, while the approved controller emits `CHANGES_REQUESTED` or `BLOCKED` for adverse reviews. The frozen state remains byte-identical at SHA-256 `0828cb7a52ff5f739dcfbc49832e7b2437f997fced38bac917098081368328e4`; the ledger remains exact and untracked; W1 remains paused under `HOLD-W1-GRR-0002`.

The defect cannot be repaired inside ordinary GCR-0003 remediation. The GCR-0003 packet hash-freezes runtime-v3 at SHA-256 `7918c0d255074fdbfd1c400aa9e4008ad6d8efa91584a3010d59612f4cc57124`, and `gcr3ctl.load_authority` rejects changed packet files. Its history validator also requires the normal R01 projection to be the unique direct child of `702ffbc` changing only the ledger and state. Editing the frozen schema, inserting implementation bytes into that projection, reinterpreting the review result, or discarding the adverse ledger would invalidate approved authority or fabricate history.

## Bounded recovery

`GCR-0004.B00` authorizes only a one-time bridge for this exact failed R01 transition. It preserves runtime-v3 and the adverse ledger byte-for-byte, introduces a successor runtime envelope and an exact `gcr4ctl` recovery lifecycle, and teaches `gcr3ctl` to recognize only the independently approved GCR-0004 bridge. The bridge records explicit recovery authority instead of pretending that the delayed transition was the original direct-child projection.

After GCR-0004 B00 implementation receives independent approval, its application must:

1. authenticate the exact frozen reviewed state, exact adverse ledger, GCR-0003 packet/approval/review/candidate/evidence chain, GCR-0004 approval and approved B00 review, active hold, stopped W1 boundary, and protected witness;
2. preserve `reviewedStateCommit=702ffbc587cca2ec05567d86dc9fd0fa0a25b4a5` and every byte of the R01 ledger;
3. derive the GCR-0003 R01 attempt, findings, empty closures, and `CHANGES_REQUESTED` state exclusively from that ledger;
4. record the exceptional bridge explicitly in a successor runtime envelope bound to the exact GCR-0004 approval and application evidence;
5. finalize as one exact commit adding the existing R01 ledger and updating only the GCR-0003 state; and
6. keep control/minimum revision 9, the active hold, W1 pause, T03 block, W1.A04 absence, G1 pending state, witness, product, main, and remote state unchanged.

GCR-0004 does not close either R01 finding. After the bridge, ordinary `gcr3ctl resubmit` creates R02 from a strict-descendant candidate with fresh evidence and explicit closures. F01 requires a real immutable v4 S02 packet-review ledger chain; F02 requires clean schema EOF hygiene. GCR-0003 still requires independent R02 approval and its original 9→10 adoption. The later S02 packet still requires a separate independent review and exact human approval.

## Application safety

The bridge uses an exclusive lock, prepared manifest, durable successor state, raw and canonical compare-and-swap, Windows replace-existing/write-through publication, file and directory flushes, exact-pair validation, idempotent recovery, and cleanup only after validation. Child-process interruption tests cover every preparation, write, marker, replacement, validation, flush, and cleanup boundary. Recovery can yield only the exact frozen predecessor or the exact ledger-derived successor; a split, substituted, stale, forked, dirty, or redirected state fails closed.

## Alternatives

- **A — Approve exact GCR-0004 (recommended):** preserve the adverse review and restore the already approved GCR-0003 remediation path through an explicit, independently reviewed bridge.
- **B — Keep recovery stopped (safe):** preserve the current fail-closed boundary indefinitely; GCR-0003, S02/B02, and W1 remain unavailable.
- **C — Edit runtime-v3 or reinterpret `CHANGES_REQUESTED` as `READY` (rejected):** invalidates the hash-frozen GCR-0003 authority or rewrites the review result.
- **D — Commit the ledger/state manually, skip R01, or weaken exact topology checks (rejected):** fabricates or bypasses append-only review history.

## Approval gate

Approval is legally available only after an independent `APPROVED` packet review at the exact packet commit with no open blocking finding. Human approval must name that commit and authorize only `GCR-0004.B00`. Approval does not authorize the bridge application before B00 implementation itself has commit-bound evidence and independent approval.
