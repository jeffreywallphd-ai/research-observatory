# W1.C01.T01 — protected development launcher correction

Claim base: `cfa7ec4492d13781128e01ad5795f2b48c5c9a26`.
Authority: inherited, unchanged seven criteria of `W1.A09.T03`, adopted
`W1.A09`; exact origin snapshot in `W1.C01.T01.spec.json`. ADR-0005 and the
adopted authored-product boundary retain narrow Tauri privileges and offline
`product-dist`, not reference HTML or a permissive loopback origin.

## Acceptance closure

- Criteria 1–2, 4–5, 7: picker component, generated names, explicit project
  action, lifetime/cancellation, layout and accessibility are untouched.
  Authenticate their source boundary and original reviewed evidence; do not
  relabel historical Windows observations as fresh runs.
- Criteria 3 and 6, affected seam: standard `dev.cmd`, root `pnpm dev`, and
  desktop `pnpm dev` must select the already-qualified embedded origin. Exercise
  actual pnpm/cmd forwarding in disposable fixtures, retaining preparation
  ordering, failed preparation, CLI exit and ordinary arguments. Observe the
  installed Tauri CLI's processed configuration using an inert runner, comparing
  default negative control with script-derived corrected arguments.
- Authority fields: unchanged `frontendDist`, absent `devUrl`, local main label,
  exact embedded origin with no port/userinfo, unchanged CSP/capabilities. Replay
  the narrow Rust origin allow/deny test and desktop configuration/security tests.
- Failure/compatibility: sidecar preparation failure prevents CLI dispatch;
  CLI failure propagates; no switch is required from callers. The old explicit
  workaround now duplicates a flag rejected by Tauri; document its retirement
  and characterize rejection instead of adding another launcher abstraction.
- Real boundary: actual installed pnpm and Tauri CLI subprocesses, isolated
  dependency-free Cargo metadata, fixture-owned temp/home and inert runner
  exit 73. No application, WebView, Core, native dialog, credentials or ordinary
  user storage is launched/accessed. This proves CLI configuration, not fresh
  end-to-end native-dialog behavior.
- N/A: schema/database migration, model runtime, new UI states, governed styling
  and default-window geometry are not changed by this correction.

First tests precede the package-script edit. Qualifying checks run again at a
stable committed candidate. The product assembler includes package/docs inputs,
so rebuild once to refresh its manifest. Full browser/style/LOC profiles are
not task-level coverage; fresh native/principal and complete cross-capability
qualification remain W1-exit duties. No acceptance criterion is waived.

Read-only independent preflight by `ui_guard_planning` confirmed the three-file
scope and these checks; it is advisory, not final review or approval. No new
authority gate was identified. Ordinary W1 remains PAUSED during this linked
correction; the original task and amendment remain completed and immutable.

## Pre-implementation characterization

`python -m unittest tests.desktop.test_desktop_app_check.DevelopmentEntryTests -v`
was run twice before changing the product script. First run: four tests,
three failing entry subcases plus four errors caused by fixture configuration
observation and transient Windows cwd cleanup. The fixture now passes its
frontendDist explicitly to the observer, normalizes pnpm's argument quoting,
and gives only its owned temporary directory a bounded cleanup retry.
Second run: four tests in 6.881 seconds, exit 1, four expected failures only:
the script-derived real CLI supplies a loopback devUrl, and each of batch/root/
desktop entry omits the embedded-origin flag. The negative/duplicate control and
preparation-failure test pass. These are development characterization results,
not qualifying candidate evidence. No product/Core process was launched.
