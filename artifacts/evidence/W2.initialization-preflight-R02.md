# W2 initialization precommit review — R02

2026-09-19. Successor to [R01](W2.initialization-preflight-R01.md), which remains
unchanged. **All four planning findings closed in the working draft.** This is
not commit-bound disposition, accepted architecture or W2 execution approval.
HEAD remains `eaa3f93c53b40c7baa47fc3a522dc4936802c2ce`.

## Independent focused replay

- `/root/w2_source_packet_preflight` closed CAP04-PRE-01 and CAP04-PRE-02:
  effective import-draft identity now carries accepted corrections/rights/options;
  replay rechecks authority and distinguishes changed decisions. Both alternatives
  now retain immutable assertions, conflicts and protected provenance/rights.
- `/root/w2_document_packet_preflight` closed CAP05-PRE-01 and CAP05-PRE-02:
  PDF worker source allocation and all viewer buffers count against the unchanged
  budget. Batch previews use explicit independent per-document atomic acceptance,
  with preserved successes and revalidated unaccepted items after interruption.
- Both reviewers found no remaining blocker or new material incremental risk in
  their replay. They did not change files, run product suites, alter hooks or
  access protected witness contents. Required implementation tests remain planned,
  not passed. Product scope, estimates and thresholds are unchanged.

Post-remediation narrow preflight: CAP-04 plan, ADR registry and exact limited
backlog/estimate/reference invariants pass. Unchanged CAP-05 and slice/reference
checks remain the earlier preflight observations, not fresh qualification.
The review site was regenerated for the changed CAP-04 alternatives. Its fresh
post-remediation deterministic check passed: 19 capabilities, 111 slices,
337 tasks and 492 pages; local report
`artifacts/tmp/W2-selections-review-site-preflight-02.json`. The new alternatives
are visible on the CAP-04 page. The proposed manifest's exact reviewed bytes are
unchanged. Working-diff whitespace checks pass; the staged raw check still flags
only the inherited intentional Markdown hard breaks recorded in the selections
note. Active reference and product-code paths are unchanged.

## Safe resume point

The permission guard denied activating the prepared privacy hook pending explicit
owner approval. The old hook remains active, the draft remains uncommitted and
no remote action occurred. The exact admission covers only the proposed reference
manifest blob and its independently reviewed stylesheet-checksum finding. It
does not change scanner rules, baseline, earlier admissions or push routing.

After that authority is received:

1. Verify the prepared sealed snapshot still matches the independent review;
   activate that exact snapshot through the documented installer procedure.
2. Stage only the authored W2 planning/evidence and generated review-site allowlist,
   including these remediation changes; commit with normal hooks enabled.
3. Hold the committed candidate fixed, run risk-selected planning validation and
   obtain independent commit-bound disposition. Do not claim precommit checks as
   qualification or rerun completed W1 product suites.
4. Resolve normal clean-worktree/integration prerequisites without reading,
   hashing, staging, ignoring, moving or deleting the protected witness. Then
   present exact ADR/reference/W2 approval through the normal route. G1's earlier
   limited approval is not W2 execution authority.

W2 remains pending with no campaign or task claim. The first implementation task
remains **CAP-04.S01.T01 — reference-format parsers**, only after complete packet
approval and readiness. This record does not approve that transition.
