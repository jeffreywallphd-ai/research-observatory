# W2 initialization — design and architecture approval handoff

2026-09-19. Proposal candidate: `67bc9e3b6215504e78fa30dbf41cc0a664977679`.
This append-only note resolves the checkout hold in
[the preceding disposition](W2.initialization-committed-review-R01.md); it does
not approve W2, change the proposal, or authorize implementation.

## Preparation completed

- Created the clean `codex/w2-planning` checkout at the proposal candidate.
  The original checkout and protected untracked witness remain untouched; no
  witness content was read, hashed, copied, staged, ignored, moved or deleted.
- Prepared and independently reviewed identical sealed privacy controls for
  the new checkout. Activated only its worktree-local hook binding; the original
  checkout still resolves its original hook. Guard/policy/scanner/parser pins,
  prior admissions and preservation authority remain unchanged. No push routing
  changed and nothing was pushed.
- A subsequent attempt to commit this note through the linked-checkout hook
  was blocked. Direct execution of the same sealed staged checker passed one
  entry with no findings; that does not replace a successful commit hook. The
  existing guard rejects ambient Git redirection, while linked-checkout hooks
  supply Git directory context. No guard or hook was bypassed or weakened.
  Use the original ordinary checkout's unchanged hooks for this note, then
  fast-forward the clean isolated checkout for review/integration. Linked-hook
  compatibility remains a narrowly scoped setup issue, not product qualification
  or authority to change privacy rules.
- Fresh `planctl wave validate W2` passed: two capability plans, eleven slice
  plans, and review-site validation covering 19 capabilities, 111 slices,
  337 tasks and 492 HTML pages. Authored planning inputs and HEAD stayed fixed.
  This is planning validation, not runtime or approved-reference qualification.
- Generation produced checkout-specific links in four historical pages plus
  line-ending-only output changes. These generated changes were discarded from
  this isolated checkout, restoring exact committed bytes and clean status.
  An attempted local archive assertion failed before copying; no archive was
  created. The four-page restore nevertheless ran, followed by cleanup of only
  this turn's generated output after canonical/staged diffs were empty. No
  authored or historical committed content changed. The successful validation
  describes generated relocated output; it does not claim byte-identical output
  across checkout locations. No generator repair or W1 suite was undertaken.
- `taskctl next --profile LOC --platform windows-x64` confirms W2 is stopped at
  pre-Wave approval. The independent gate review confirms all 68 W1 tasks DONE,
  all 15 W1 slices APPROVED, W1 completion APPROVED/campaign COMPLETE, and G0/G1
  APPROVED. G1's documented prototype limitations remain, not a full-green claim.

## Exact next human decision

Recommend approving [ADR-0027](../../docs/adr/ADR-0027-keep-scholarly-source-replay-separate-from-private-authentication.md),
[ADR-0028](../../docs/adr/ADR-0028-isolate-windows-connectors-and-parsers-behind-narrow-brokers.md),
[ADR-0029](../../docs/adr/ADR-0029-preserve-document-revisions-and-mediate-source-viewing.md)
and the exact [reference 1.7 proposal](../../planning/W2-reference-proposal.md)
at the candidate above. Reference package SHA-256:
`abdb30a94510ceb6452019133f62189a38c77deda3ecebdf0d0dabdfdeebff6f`.
The proposed reference retains its expected pending-status validation errors;
it is not yet an approved reference. Independent planning findings are closed.

This is the design/architecture prerequisite, **not final W2 approval**. The
earlier suggestion to combine final Wave freezing with these approvals omitted
the required order: approve reference, publish it, update material plan bindings,
then freeze the complete packet. CAP-04.S05 and other affected plans still bind
1.6. Their subsequent material rebinding cannot be treated as administration of
an already immutable Wave. `/root/w2_source_packet_preflight` independently
confirmed this correction against the design-first and immutable-Wave rules.

Approval authorizes normal ADR acceptance/reference publication and affected
planning updates only. Alternatively request a specific revision or defer;
either leaves W2 pending. After approval, update those bindings, commit and
independently review the final packet, then request its single pre-Wave approval
at the final full commit. That later approval covers all seven binding decisions,
eleven slices/33 tasks and 530 engineering-hours including the sole two-hour
support allocation. No implementation claim or new campaign exists yet.

Final Wave approval/resumption command shape, **not authorized by this note**:

```text
python tools/planctl.py --repo . wave approve W2 --by "<owner>" --commit <final-reviewed-full-sha>
python tools/planctl.py --repo . wave ready W2 --require-approved
```

The [W2 review page](../../planning/review-site/waves/W2.html) remains a navigation
surface; canonical plans, recorded approvals and scoped evidence retain authority.
