# W2 initialization selections — increment02

Planning-only successor to `eaa3f93c53b40c7baa47fc3a522dc4936802c2ce`.
No W2 implementation, campaign, approval, W1 requalification, remote mutation
or active-reference change. The preceding bounded review is preserved in
[R01](W2.planning-boundaries.review-R01.json).

## Delta and preserved boundaries

- Itemize33 tasks: CAP-04=236 and CAP-05=294 engineering-hours,530 total.
  W2-RUNNER-PROGRESS is one2-hour subset of CAP-04.S01.T01, explicitly declared
  in its mutable backlog/slice contract. Its optional deferral cannot stall the
  parser or hide partial runner changes. No controller/schema work is selected.
- Add CAP-05.S01.T01 → CAP-04.S05.T02 so hostile document inspection cannot
  precede isolation. Both this edge and the support declaration used taskctl's
  validated CAS writer, each asserting predecessor equality except its exact
  allowed fields. Product criteria, identities, states and history are unchanged.
- Select the seven proposed capability recommendations. Their `accepted` decision
  state means planning selection only: capability/Wave approvals remain pending;
  ADR-0027/0028/0029 remain Proposed. Do not infer human approval or readiness.
- Consolidate binding choices into those three ADRs: import identity, source
  replay/rights, reconciliation, actual worker/IPC limits, exact offline assets,
  authenticated viewer ranges, acquisition, selectors and human corrections.
  Probe observations and limits are in [W2 feasibility](../../planning/W2-feasibility.md).
- Prepare inert reference1.7 with one Source Manager panel/contract. Preserve
  active1.6, exact inherited workflow1.5 bindings, product code and visual baselines.
  Full-reference copy follows the existing governed package shape; no new styles,
  navigation page, workflow sequence, automation framework or runtime dependency.

## Selected verification and observations

Precommit planning checks: two capability assessments and eleven slice plans
pass; site generated from canonical plans. New candidate-bound checks and
independent disposition follow the commit, not this narrative.

Proposed-reference generator/inventory validation succeeds with35 HTML documents,
33 product pages and14 workflows. The active-reference checker has exactly the
two expected proposed-status errors, no other errors/warnings, and reproduces
the generator. It is deliberately **not** an approved-reference PASS.
Package SHA-256:
`abdb30a94510ceb6452019133f62189a38c77deda3ecebdf0d0dabdfdeebff6f`.

Focused Chromium145.0.7632.6 / Playwright1.58.0 observations: new panel keyboard
order, canonical visible focus, disabled-enable explanation and keyboard details
work in both themes at1440 and1280 widths. Screenshots were generated and the
dark1280 panel inspected. At720, the full reference overflows due to inherited
`html { min-width:1100px }`; the unchanged approved1.6 page reproduces that limit.
Those two cases are retained as **INHERITED_REFERENCE_LIMIT_NOT_PASS**, not
waived product accessibility. W2 product conformance must still prove actual
narrow/enlarged-text reflow. Report:
`artifacts/tmp/W2-reference-previews/report.json`.

Preserved exploratory errors: first preview import named a nonexistent helper;
the initial focus assertion tested `outline` instead of the actual governed
box-shadow token (the mistaken assertion was inadvertently repeated once);
the first proposed package used an extra HTML fragment unsupported by existing
inventory/generator closure. These were corrected in the proposal/probe, not by
weakening product checks or changing a validator. The genuine narrow-reference
overflow remains visible. No real trust/permission or native behavior was tested.

The first commit attempt was blocked by the unchanged privacy hook: the initial
reference-folder copy included old generated previews/reports and an unused
illustrative bitmap. Those copies were moved to ignored task-local storage and
unstaged; the approved originals remain untouched. No page references the bitmap.
The proposal now contains only its required textual source/generated package;
new focused observations remain local and are not stale inherited baselines.
The manifest was regenerated after that cleanup. A subsequent pinned credential
scan classified its existing CSS-token asset checksum as `generic-api-key`.
Independent reviewer `/root/w2_ux_plan_review` confirmed the exact finding is the
approved stylesheet checksum, not authentication material. The prepared sealed
registry admits only that manifest blob and complete finding fingerprint;
the staged privacy check passed all 93 entries. Scanner rules, privacy baseline,
prior admissions and push routing remain unchanged. The permission guard denied
activation pending explicit owner authority. The original hook remains active;
no commit or configuration change occurred. The exact admission request is pending;
no bypass is authorized.

The raw Git whitespace check flags only the copied workflow catalog's intentional
two-space Markdown line breaks, inherited unchanged from 1.6. Preserve that
catalog; do not report the raw check as a clean PASS. Shared JS differs only in
CRLF/LF encoding, with canonical text equality verified; no behavior change.

The resolver was repeated only for the changed DOCX input, not an unchanged W1
suite. Neither resolver result is an installed runtime or licensing/security proof.
Completed W1 suites, full product/native/platform profiles and W2 execution are
not rerun/started here. Existing cleanup/integration requirements and protected
witness restrictions remain; no witness content was accessed or newly ignored.

## Remaining boundary

Independent contribution preflight assessed architecture, all contribution
handoffs/estimates and the reference delta. Four adverse findings are preserved in
[R01](W2.initialization-preflight-R01.md); focused remediation closure is recorded
in [R02](W2.initialization-preflight-R02.md). These working-draft reviews are not
commit-bound disposition. Commit and verify after the exact hook authority is
resolved, then complete commit-bound review and the normal ADR/reference/W2 approval route.
Do not claim the existing dirty-worktree/integration prerequisite has disappeared.
Later runtime/LPAC/provider/document/native qualification remains in the planned
tasks and G2, not replaced by these planning probes or G1's exception.
